<!-- END:nextjs-agent-rules -->

# RemyNest

The single authoritative Claude/agent workflow lives in **[CLAUDE.md](CLAUDE.md)**.

`/docs` is the source of content truth — start every session with
`docs/handoffs/HANDOFF_CURRENT.md`, then read only the docs relevant to the task.
Do not maintain a separate instruction system here.
