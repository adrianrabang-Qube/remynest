# REMYNEST MASTER STATE

> **The single authoritative source of truth for RemyNest project state.**
> Update this file after EVERY completed task (see "Maintenance protocol" at the bottom).
> Rule: if this file, CLAUDE.md, or HANDOFF ever conflict with the **source code**, the
> **SOURCE CODE WINS** — reconcile this file to the code, then note the divergence.
> Every ✔ below was **verified against actual source** in the 2026-07-09 synchronization audit
> (10-agent, file:line-cited). Items are re-verified against code before being trusted, not from memory.

Last Updated: 2026-07-16
Branch: main — **the SOLE active production branch**, currently `b1b004c` (== `origin/main`, in sync; RC-certification `b646449` + 4 post-cert commits [avatar tier, production polish, final QA, storage architecture] are all pushed/live; `main` auto-deploys). **Historical branches are preserved for reference only — see `docs/PROJECT_HISTORY.md` for the full branch archive + engineering-history timeline + the active-development policy (all future work branches from `main`; historical branches never receive new development).** (The remainder of this line is pre-push history.) At the 2026-07-13 session start main was `b646449`: The remainder of this line describes the pre-push history: latest then = **RC2 Security Hardening** (production security posture — HTTP security headers [CSP/HSTS/XFO/nosniff/Referrer/Permissions] + API rate limiting + PHI/log hardening + 3 orphan-route removals + OWASP fixes; NO feature/UI/architecture change), on top of **Memory Intelligence Engine V2** (`761d3e0`; ADDITIVE capability + data layer — adaptive importance / relationship weighting / deterministic decay / reinforcement / cached classification / event clustering / forgotten detection / configurable combined ranking; ONE central config `lib/remy/memory-intelligence/config.ts`; pure engines + a service-role side table `memory_intelligence` [never alters `memories`]; NOT wired into any live path, so the execution path / provider layer / Ask Remy / story pipeline / billing / quotas stay byte-unchanged) — on top of AI-Subscriptions-Quotas-Dashboard (`5b6a607`) + AI-Usage-Billing-Observability (`88dd366`) + Live-Conversation-Integration (`3c3a7a5`) + Production-Provider-Activation (`a92e9f7`) + Provider-Registry-Activation (`689f917`) + OpenAI-Provider-Adapter (`e7b572c`) + the full conversation-platform + companion stack (see git log)
Commit: BRANCH ARCHIVAL & ENGINEERING-HISTORY PASS (2026-07-13; documentation-only — NO branches/tags/code/history changed, nothing pushed/force-pushed). Classified all 8 local branches: `main` = ACTIVE (canonical production, == origin/main == b1b004c); 6 fully-merged HISTORICAL branches (living-workspace-v1/v2, hardening/v0.9.1-production, feat/capacitor-mobile, qa/playwright-phase1) + 1 ARCHIVED safety checkpoint (backup/cognition-v2-pre-sync); `cognition-v2` = HISTORICAL-SUPERSEDED (2 unique commits — native local notifications, feature on main via CocoaPods reimpl; SPM deliberately abandoned). All 7 non-main branches PRESERVED (not deleted). Reviewed 13 release tags (accurate; genesis→v0.9-foundation), recommended 3 additional milestone tags for the untagged June-July production programme (operator to create). New permanent record: **`docs/PROJECT_HISTORY.md`** (evolution timeline genesis 2026-04-15 → production RC 2026-07-13, branch archive, tag audit, active-development policy: main-only production; historical branches are reference-only + never receive new work). On top of the RC certification + the 4 post-cert commits (avatar tier, polish, QA, storage architecture) — see git log + PROJECT_HISTORY. Prior RC-certification commit line:
Commit(prev): RC — FINAL RELEASE CANDIDATE CERTIFICATION (independent verification; NO code change — docs only). Full RC certification: validation GREEN (tsc/lint/build); critical invariants verified intact (protect-by-default auth, private memory-media, purchase gating, LA5.1 Art-17 migration fix, Stripe 500-retry, no committed secrets, .env.example↔code); independent **7-lens multi-agent certification (8 agents, 0 errors) — ALL 7 principal lenses GO/96, board decision GO, overall 96/100, ZERO verified engineering blockers.** **DECISION: RemyNest Release Candidate is CERTIFIED for production deployment.** Residual = operator/legal/product go-live activation only (push commits, apply migrations, set env, Storage backup + test restore, uptime/alerts; /terms jurisdiction + DPO; submission package). On top of LA7 — see git log. Prior LA7 commit line:
Commit(prev): LA7 — Final Production Launch Readiness Audit (verification pass; NO runtime/product/architecture change). Final full-system audit across every subsystem + the ~24 critical flows → **RemyNest has NO remaining ENGINEERING blockers for production deployment** (Engineering ~96 / Overall ~90 / Infra ~88 / Deploy ~90). The 9-lens audit + 7-lens review hit the subagent session limit → completed inline (repo precedent). One real config-drift finding FIXED: `.env.example` documented the WRONG Stripe price-id names (STRIPE_PRICE_PREMIUM vs the real STRIPE_{PREMIUM,FAMILY,ENTERPRISE}_{MONTHLY,YEARLY}_PRICE_ID ×6) + omitted the store URLs → corrected `.env.example` + reconciled `check-production-env.mjs` (checkout already validates + 400s on an unset price, so config not a crash). 3 caller-less API routes (send-notification/build-relationships/timeline) deferred as LOW post-launch cleanup (not removed — unseen-caller risk). Residual = operator/legal/product only. tsc/lint/build green. On top of LA6 — see git log. Prior LA6 commit line:
Commit(prev): LA6 — Disaster Recovery & Business Continuity (documentation-first; NO runtime/product/architecture change). Produced the operational DR/BC layer: `docs/DISASTER_RECOVERY_PLAN.md` (dependency map, RTO/RPO, SPOF, backup strategy, ownership, severity, automation roadmap) + `docs/runbooks/` (index + 5 runbooks: db/storage recovery, deploy/migration rollback, dependency outages, secrets/rotation, operational incident response) + `scripts/check-production-env.mjs` (operator env-integrity check; never prints values) + reconciled 2 stale snapshots. Inline 6-lens review (subagent limit) verified DR claims against code + FIXED a false claim (only 2 of 13 migrations have rollback blocks, not all). DR ~82 / BC ~80; no new engineering blocker; residual = OPERATOR (Storage backup + test restore [HIGH], uptime/alerting activation, restore drill). On top of LA5.1 — see git log. Prior LA5.1 commit line:
Commit(prev): LA5.1 — Apple Guideline 1.2 UGC moderation (CLOSES the CRITICAL LA5 iOS blocker; maintains existing architecture; NOT a social platform). Minimum production-ready report/block/leave framework: migration `moderation_reports` + `user_blocks` (RLS insert-own+select-own only, no update/delete → status service-role only; reporter identity never exposed; FKs reporter→CASCADE, reported/memory→SET NULL; NO must-have-target CHECK [would abort a reported user's account delete = Art 17 regression]); server actions (session-derived, structured/never-throw, service-role scoped, degrade-if-absent) reportUser/reportContent/blockUser/unblockUser/leaveWorkspace/listSafetyOverview gated by getSharedCarePeopleIds; block enforcement on inviteCaregiver + acceptInvite (never removes care access); Safety Center `/settings/safety` + ReportDialog + Report button on care-search results; GDPR export enrollment (v1.2) + email masking. Multi-agent-verified (6 lenses, all satisfiesApple12=true; 1 blocking defect + nits all fixed). OPERATOR: apply the migration before iOS submission. On top of LA5 — see git log. Prior LA5 commit line:
Commit(prev): LA5 — Apple App Store & Google Play Compliance (store-review compliance audit + SAFE fixes only; no architecture/business-logic/subscription/schema change; frozen reminder untouched. 6-lens audit [28→21 verified] scored Apple ~74→~80 / Play 57; independently multi-agent-reviewed [behaviour preserved by all 6 lenses, 0 blocking regressions, 1 must-fix + 3 nits all applied]. Fixes: completed de-medicalization [deleted cognitiveDeclineSignals.ts + Routine-Changes card; neutralized Memory-Activity cognitiveActivity→loggingActivity]; JSON-LD LifestyleApplication + dropped cognitive-care copy; Apple-1.2 EULA zero-tolerance abuse clause; de-drugged reminder placeholder; Sentry processor disclosure; cancel-before-delete caveat; data-rights contact → admin@ across all legal pages; Android allowBackup=false. STILL the #1 iOS blocker: the UGC report/block MECHANISM [feature]. On top of LA4 — see git log)
Repository Verified: **YES** — 2026-07-09. Basis: the 10-agent file:line-cited source audit, plus a targeted source re-verification this sync pass (access_level enforcement, AI stack, Nest, audio/PDF-UI, reminder-edit, security-headers/rate-limiting all reconfirmed against actual code). **Source code is authoritative over all documentation** (this file, CLAUDE.md, HANDOFF, architecture docs); reconcile docs forward when they diverge.

==================================================
## PROJECT STATUS
==================================================

Launch Readiness: **~90%** (launch-scope build). Every core system is implemented + verified in source; the remaining ~10% is overwhelmingly **operator / product / legal**, plus one engineering feature (UGC report/block).

Overall Project Completion: **~70%** (counting the full post-launch vision — voice, transcription, animated Remy, Android-as-product, Semantic Search V2, etc., which are intentionally deferred).

Current Milestone: **App Store Submission Readiness** (roadmap phase 4 — Productization is done).

Current Sprint: **Launch hardening & App-Store compliance.**

Current Task: *(none active — **iOS launch visual/privacy assets PREPARED (2026-07-15, iPhone-only v1)**: branded icon replaced the Capacitor template (opaque 1024², legible small), branded sand splash (all 3 files + config bg), PrivacyInfo +Audio Data AND real target membership (was never bundled), TARGETED_DEVICE_FAMILY=1, screenshot spec at `docs/launch/APP_STORE_SCREENSHOTS.md`; operator: Build 18 archive → TestFlight → ASC privacy answers → screenshots; Build-17 pbxproj hunks preserved unstaged. Before it, **Together Time (Activity #5, the FINAL activity) COMPLETE (2026-07-15)**: private reusable 3–8-moment sets w/ fixed non-AI prompts (probe-gated migration **`20260715210000_together_time.sql` — OPERATOR STEP**), best-effort last_opened_at (readers never forced to write), dedicated photo+audio moment loader, GDPR v1.8, registry flipped. **ALL FIVE ACTIVITIES LIVE — after post-ship QA, feature work PAUSES for iOS App Store launch prep.** Before it, **Voice Memory discoverability COMPLETE (2026-07-15)**: operator-approved sixth Nest quick-action row → `/memories/new?voice=1` (same form, recorder rendered first; no auto-record; validation untouched). Before it, **Voice Memory v1 is COMPLETE (2026-07-15; operator-approved narrow voice-deferral lift)**: browser-native MediaRecorder recordings as normal memory attachments (no plugin, no schema; pipeline/quota/signing/GDPR inherited; mic released on stop/discard/unmount; 5-min limit; no autoplay; feed chip + existing detail player). OPERATOR: native rebuild for the updated mic-permission string + add Audio Data to PrivacyInfo.xcprivacy/App-Store answers + real-iPhone record/playback confirmation. Before it, **Music Memories Spotify link import COMPLETE (2026-07-15; import-only)**: strict-validated track links → official oEmbed only (auth+rate-limited route; fixed outbound host; html string-parsed then discarded) → canonical track URL persisted (probe-gated migration **`20260715180000_music_memories_spotify.sql` — OPERATOR STEP**; pre-application writes retry without the column); GDPR v1.7; text-only source indicator; NO OAuth/player/artwork/links. Before it, **Music Memories (Activity #4) COMPLETE (2026-07-15; v1 song-association, NO audio)**: `song_memories` metadata + optional ordered memory links (probe-gated migration **`20260715150000_music_memories.sql` — OPERATOR STEP**), Story-Builder-pattern authz + reused generic helpers (import-only), GDPR v1.6, hub/wizard/detail/edit/delete, registry flipped to available; audio remains the documented deferral (Phase 2/3 operator-gated). Before it, **Memory Match (Activity #3) COMPLETE (2026-07-15)**: view-over-photo-memories pair game (probe-gated migration **`20260715120000_memory_match.sql` — OPERATOR STEP**), puzzle-pattern authz + per-photo attachment verification, GDPR v1.5, tap-only accessible board (seeded deck, autosave w/ completion-cancel, id+seed remount), `match.completed` platform event, registry flipped to available. Before it, **Story Builder (Activity #2) COMPLETE (2026-07-15)**: ordered-view-over-memories `stories` table (probe-gated migration **`20260715090000_story_builder.sql` — OPERATOR STEP**), puzzle-pattern authz (own-context, write-gated, save-time workspace verification of every memory id), GDPR v1.4, hub/wizard/reader/edit with button-driven accessible reordering, registry flipped to available. Before it, **Memory Puzzles Phase 1 is COMPLETE (2026-07-14, 1A–1D)**: schema (operator-applied probe-gated migration `20260714090000`) + pure core + server actions + GDPR v1.3 → create flow (memory picker / upload-creates-a-memory / crop-as-metadata / difficulty) → DOM-tile play engine (magnetic snap, tap/keyboard access, ghost/hint, autosave+resume, replay/favourite/delete, no timers/scores) → completion (photoReveal dissolve, `Remy.emit("puzzle.completed")`, reflection seam for future AI). OPERATOR: apply the puzzles migration. Before it, the platform: **Remy's Activities platform (2026-07-13, COMPLETE; platform only — no gameplay/engine/schema):** registry-driven `/activities` landing (`lib/activities/registry.ts` = single source of truth; status IS the feature flag: available/coming-soon/future) + `ActivityCard` (3 statuses) + the `/activities/puzzles` Phase-1 mount point (honest intro shell) + a Library `SECTIONS` row + brand skeleton/empty state; Remy intro via `<RemyStage context="welcome">`; de-medicalized copy. Registry: Puzzles available · Memory Match + Story Builder coming-soon · Music Memories + Family Activities future. tsc/lint/build green (69/69). **STOPPED at the approval gate** — Phase 1 (puzzle gameplay per the approved architecture) must not start without explicit approval. Before it: the **Storage Capacity Architecture (2026-07-13, COMPLETE; behaviour-preserving, no Stripe/schema/UI change):** capacity is now a COMPOSED ENTITLEMENT — `lib/storage/capacity.ts` sums capacity grants (plan base today; future booster/promo/grandfathered grants via the `extraGrants` seam); `getStorageUsage` derives the limit from it (byte-identical) + exposes `StorageUsage.capacity`. The operator's storage-pack proposal was evaluated and the SKU deliberately DEFERRED post-launch (one purchase dimension at launch; booster = single SKU later, gated on operator approval + usage data + a designed lapsed-pack retention state). CLAUDE.md single-source-of-truth note superseded. tsc/lint/build green. Before it: the **Final QA Pass (2026-07-13, COMPLETE; verified defects only):** 5 fixed — reminder double-tap dispatch guard (`PendingSubmitButton`, freeze-compliant proven-defect carve-out: double-tap "Done for today" skipped a medication day; double-tap Create duplicated reminders), toast id collision (`Date.now()`→counter), **shared-device cross-user cache exposure** (root-layout QueryClient + staleTime 60s survived SPA logout → `queryClient.clear()` on logout + login), memories-feed error state (failed fetch no longer renders as "No memories yet"), + 1 deferred (deep-link return param through the auth middleware — post-launch, needs open-redirect-safe design). tsc/lint/build green. **Engineering verdict: no remaining launch-blocking defects.** Before it: the **Production Polish Pass (2026-07-13, COMPLETE; presentation-only "feels premium" refinement):** fixed the app-wide toast (dead `animate-slide-in` class → real `animate-toastIn`, brand sage-deep/rounded-2xl/shadow-soft [8:1 contrast], safe-area placement), gave the "More" drawer a real entrance (scrim fade + iOS-curve slide-in, CSS-only, entrance-only; 44px lucide-X close + focus ring), platform CSS polish (`text-size-adjust:100%`, `touch-action:manipulation` on interactives, sage `::selection`, reduced-motion guards for ALL utility animations), inset sage focus rings on the bottom-nav tabs, and a hidden Nest wake-choreography avatar preload (first wake never pops). Deferred: toast exit anim, route transitions, MediaThumb blur-up, theme-color (deliberate sage — don't re-litigate). tsc/lint/build green. Before it: the **Remy AVATAR asset tier — "Remy IS the button" (2026-07-13, COMPLETE; asset-architecture + presentation only, post-certification, no logic/routing/behaviour change):** every expression asset now has a second render tier `remy_avatar_<expr>.png` (256×256 square, transparent, character ~86% fill, crest headroom for the float bob), derived **mechanically (crop+scale only — never redrawn)** from the same approved masters; registered in the ONE registry (`avatarSrc` + `resolveRemyAssetSrc` w/ scene fallback); `<Remy>` gained `assetVariant` (default "scene" — existing consumers unchanged); adopted by the 4 compact surfaces (Nest 48px / NestMenu greeting 44px / RemyMomentChip 40px / FloatingCompanionLayer 64px). Root cause fixed: 1536×1024 landscape scene art letterboxed the bird to ~15px in the 48px Nest FAB. Avatars 50–160KB vs 2.3–3.2MB scenes. tsc/lint/build green; 17/17 avatars visually verified. Authoritative: CLAUDE.md "Remy asset pipeline — TWO RENDER TIERS". Also reconciled stale docs: the PNGs are TRANSPARENT (not opaque), and `main` IS pushed/in sync. Before it: **RC FINAL RELEASE CANDIDATE CERTIFICATION is COMPLETE** (independent verification; NO code change — certification docs only; committed locally, unpushed): validation GREEN (tsc/lint/build); the critical production invariants were verified intact with no regression (protect-by-default auth [B1], private `memory-media` [B2], Apple-3.1.1 purchase gating, the LA5.1 Art-17 moderation-migration fix, Stripe 500-on-writeFailed, no committed secrets, `.env.example`↔code consistency); an independent **7-lens multi-agent certification (Senior Staff / Principal SW / Security / SRE / Mobile / Privacy / Platform — 8 agents, 0 errors) returned ALL GO/96, board decision GO, overall 96/100, ZERO verified engineering blockers.** **DECISION: RemyNest Release Candidate is CERTIFIED for production deployment.** The RC2→LA7 programme is complete and LA7's "no remaining engineering blockers" is CONFIRMED. Residual = **operator** (push the 14 commits, apply migrations, set prod env, Storage backup + test restore, uptime/Sentry alerts), **legal** (`/terms` jurisdiction, controller entity/DPO, DPA), **product** (App-Store submission package, Health/Sensitive data declaration) — cleanly separated, none engineering. See `docs/RC-FINAL-CERTIFICATION.md`.)*

Superseded (LA7) Current Task: *(**LA7 Final Production Launch Readiness Audit is COMPLETE** (verification pass; NO runtime/product/architecture change; committed locally, unpushed): the final full-system audit across every subsystem + the ~24 critical end-to-end flows. **DETERMINATION: RemyNest has NO remaining ENGINEERING blockers for production deployment.** Scores: Overall ~90 · Engineering ~96 · Infrastructure ~88 · Deployment ~90 (the gap is operator/legal/product activation, not code). The 9-lens audit + 7-lens review both hit the subagent session limit → completed **inline** (repo precedent RC3/LA4/LA6). **One config-drift finding FIXED:** `.env.example` documented the WRONG Stripe price-id names (`STRIPE_PRICE_PREMIUM` vs the real `STRIPE_{PREMIUM,FAMILY,ENTERPRISE}_{MONTHLY,YEARLY}_PRICE_ID` ×6) + omitted `NEXT_PUBLIC_{APP,PLAY}_STORE_URL` → corrected `.env.example` + reconciled `check-production-env.mjs`. Checkout already validates + returns a clean 400 when a price is unset (config, not a crash). **Deferred (LOW, not removed):** 3 caller-less API routes (send-notification/build-relationships/timeline; auth-gated + harmless; unseen-caller risk). Residual = **operator** (push commits, apply migrations, set prod env, backups, monitoring), **legal** (`/terms` jurisdiction, controller entity/DPO), **product** (submission package, Health-data declaration) — cleanly separated, none engineering. tsc/lint/build green. Before it: LA6 DR/BC; LA5.1 UGC moderation; LA5 store-compliance; LA4/LA3/LA2/LA1; RC5 CERTIFIED. See `docs/LA7-LAUNCH-READINESS-REPORT.md`.)*

Superseded (LA6) Current Task: *(**LA6 Disaster Recovery & Business Continuity is COMPLETE** (documentation-first; NO runtime/product/architecture change; committed locally, unpushed): produced the operational DR/BC layer that complements the existing backup docs + the GDPR breach policy — **`docs/DISASTER_RECOVERY_PLAN.md`** (dependency map, RTO/RPO objectives, SPOF analysis, consolidated backup strategy, recovery ownership, incident-severity model, automation roadmap) + **`docs/runbooks/`** (index + 5 scenario runbooks: database-and-storage-recovery, deployment-and-migration-rollback, dependency-outages, secrets-and-credential-rotation, operational-incident-response) + **`scripts/check-production-env.mjs`** (operator env-integrity check — recovery automation; never prints secret values; fails non-zero on a missing REQUIRED var) + reconciled 2 stale snapshots (OPERATIONAL_READINESS, HEALTHCHECK). **DR ~82/100 · BC ~80/100.** The 6-lens review (SRE/Cloud/DR/Supabase/DevOps/Security) hit the subagent session limit → completed inline (repo precedent) and **FIXED a false claim** (only 2 of 13 migrations have rollback blocks — corrected across both migration runbooks + scoped the probe-gating claim to additive migrations). tsc/lint/build green. **No new engineering launch blocker.** Residual is **OPERATOR/infra** (unchanged from RC4/RC5, now with runbooks): the **`memory-media` Storage backup + test restore** (HIGH), uptime-monitor + Sentry-alert activation, a restore drill, an encrypted offline secrets copy. **DR posture (LOCKED/accepted):** Postgres RPO ≤24h (PITR deferred, cost); single region. Before it: LA5.1 UGC moderation; LA5 store-compliance; LA4 observability; LA3/LA2/LA1; RC5 CERTIFIED. See `docs/LA6-DISASTER-RECOVERY-REPORT.md`.)*

Superseded (LA5.1) Current Task: *(**LA5.1 Apple Guideline 1.2 UGC moderation is COMPLETE + multi-agent-VERIFIED** (CLOSES the CRITICAL LA5 iOS blocker; maintains existing architecture; NOT a social platform; committed locally, unpushed): implemented the minimum production-ready **report / block / leave** framework — `moderation_reports` + `user_blocks` (operator-applied, additive/reversible/probe-gated migration; RLS insert-own + **select-own only**, no update/delete → status service-role only; reporter identity never exposed; a blocked user can't discover who blocked them); server actions `reportUser`/`reportContent`/`blockUser`/`unblockUser`/`leaveWorkspace`/`listSafetyOverview` (session-derived actor, structured/never-throw, service-role scoped, degrade-if-absent, rate-limited + dedup, authorized by `getSharedCarePeopleIds`); block enforcement on **both** `inviteCaregiver` (create) + `acceptInvite` (accept) in either direction (never removes existing care access — that's the explicit revoke/leave path); **Safety Center** `/settings/safety` + Settings link + shared portaled/focus-trapped `ReportDialog` + a **Report button on care-context search results**. GDPR export enrolled (v1.2) + co-caregiver emails masked. **6-lens multi-agent review — all `satisfiesApple12 = true`; the 1 BLOCKING defect (FK SET-NULL × a must-have-target CHECK that would abort a reported user's account delete = Art 17 regression) + all high-value nits FIXED.** tsc/lint/build green. **NO remaining ENGINEERING blocker for iOS submission on Apple 1.2.** **OPERATOR (required to activate):** apply `20260712120000_moderation_foundation.sql` before iOS submission + run the deletion regression. Remaining iOS items are NON-engineering: `/terms` jurisdiction (legal); submission package (product). Before it: LA5 store-compliance; LA4 observability; LA3 perf; LA2 a11y; LA1 clinical; RC5 CERTIFIED. See `docs/LA5.1-APPLE-1.2-MODERATION-REPORT.md` + `docs/features/moderation.md`.)*

Superseded (LA5) Current Task: *(**LA5 Apple App Store & Google Play Compliance is COMPLETE + multi-agent-VERIFIED** (store-review compliance audit + SAFE fixes only; behaviour-preserving; no architecture/logic/subscription/schema change; frozen reminder untouched; committed locally, unpushed): a 6-lens audit [Apple/Play/Mobile/Privacy/Healthcare/Security; 28→21 adversarially-verified findings] scored **Apple ~74→~80, Play 57**, then drove safe fixes (independently reviewed: behaviour preserved by all 6 lenses, **0 blocking regressions**, 1 must-fix + 3 nits all applied): **completed de-medicalization** (deleted the fabricated cognitive-decline scoring `lib/analytics/cognitiveDeclineSignals.ts` + the "Routine Changes" card; neutralized the "Memory Activity" `cognitiveActivity`→`loggingActivity` "Declining"/"Reduced"→"Quiet"/"Quieter"); **dropped health-app positioning** (JSON-LD `HealthApplication`→`LifestyleApplication`; removed "cognitive-care" from seo/terms/contact); **Apple-1.2 EULA zero-tolerance abuse clause** in `/terms`; **de-drugged reminder placeholder** (Donepezil→"Morning medication"); **Sentry processor disclosure** + **cancel-before-delete caveat** + **data-rights contact → admin@ across ALL legal pages** (`lib/contact.ts` privacy/dpo→admin@); **Android `allowBackup=false`**. **iOS = DO NOT SUBMIT yet:** the CRITICAL **UGC report/block MECHANISM** (Apple 1.2 — a feature) + the `/terms` **governing-law jurisdiction** (legal/counsel) must land first. **Google Play = deferred/not submittable** (no FCM/signing, versionCode 1). Before it: LA4 observability; LA3 perf; LA2 a11y; LA1 clinical; RC5 CERTIFIED. See `docs/LA5-STORE-COMPLIANCE-REPORT.md`.)*

Previous Task: *(**LA4 Production Observability & Failure Recovery is COMPLETE + multi-agent-VERIFIED** (behaviour-preserving; no UI/logic/billing/AI/schema change; committed locally, unpushed): added an env-gated, PII-scrubbed, never-throws `captureError()` helper (`lib/observability/capture.ts`) applied to 15 key API catch sites (memories list/create/[id]/search, timeline, gdpr export/delete, send-notification, profile, build-relationships, active-profile, memory-chat, cron top-level, story-narration) so **handled 500s + previously-silent AI failures now become Sentry events, correlated by route/requestId**; fixed 2 residual raw-`PostgrestError` PII logs (memories/[id], gdpr/delete-account). The formal **6-lens multi-agent review** then VERIFIED it correct/PII-safe/regression-free (**SOUND-WITH-FIXES, reliability 86/100, 0 defect**) and drove **6 same-class follow-up fixes** (memories/search raw-PII, Ask Remy chat capture, Stripe checkout/portal/cancel captures, upload-url + search/global silent-failure captures, cron raw-PII). **CORRECTION:** the `onRequestError` hook is **INERT on Next 14.2.5** (Next-15 API, kept forward-compatible) — the 15 explicit `captureError` catch-sites are the actual server-error coverage today. Reliability observability 78→~86. **OPERATOR activation: set the Sentry DSN + alert rules + an uptime monitor on /api/health.** Before it: LA3 perf; LA2 a11y; LA1 clinical; RC5 CERTIFIED FOR APP STORE + PRODUCTION. Remaining launch gates are OPERATOR/infra only: Sentry DSN/alerts; wire PrivacyInfo.xcprivacy into Xcode + rebuild; confirm backups + Storage-bucket backup + test restore; apply the operator DB indexes; prod env/migrations. Google Play deferred.)*

Earlier Task: *(**LA3 Performance & Scalability Hardening — COMPLETE** (behaviour-preserving optimization; no contract/logic/security/billing/AI change; committed locally, unpushed): stripped the dead pgvector `embedding` (~15-29KB/row) from the 4 hot memory reads (feed/timeline API/timeline page/search) via a runtime `stripEmbedding()`; memoized the memories feed (useMemo sort+grouping + useCallback handlers + React.memo MemorySection — no per-keystroke re-sort/reconcile); parallelized the `(app)` layout data waterfall (Promise.all, fallbacks preserved); removed 2 dead memory-chat round-trips; memoized the Toast context value; dev-gated middleware request logs. Perf 72→~82. **DB indexes (memories composite, pgvector ANN, pg_trgm, profile_relationships) are OPERATOR recommendations** — schema is dashboard-managed; SQL in `docs/LA3-PERFORMANCE-REPORT.md`. Before it: LA2 a11y; LA1 clinical; RC5 CERTIFIED FOR APP STORE + PRODUCTION. The remaining launch gates are OPERATOR/infra only: wire PrivacyInfo.xcprivacy into the Xcode target + native rebuild; confirm daily backups + Storage-bucket backup + test restore; apply the operator DB indexes; set prod env + probe-gated migrations. Google Play is a decided post-iOS deferral.)*

Superseded Task: *(**LA2 Accessibility & Inclusive Design Readiness — COMPLETE** (WCAG 2.2 AA pass; presentation/aria/semantic only, no logic/data/architecture change; committed locally, unpushed): cleared the 4 Level-A blockers (keyboard-reachable media upload, announced toasts, labeled primary forms, memory-delete confirmation) + skip-to-content link + CreateMemoryForm focus rings/contrast + single `<main>` landmark (13 pages) + navbar/PhotoViewer aria + heading semantics + reduced-motion skeletons; a11y score 68→~84. Larger items recommended (charcoal-muted contrast sweep, modal focus traps, MemoryCard restructure, Undo/soft-delete). Before it: LA1 clinical readiness; RC5 CERTIFIED FOR APP STORE + PRODUCTION. The remaining launch gates are OPERATOR/infra only: wire PrivacyInfo.xcprivacy into the Xcode target + native rebuild; confirm daily backups + establish a Storage-bucket backup + run a test restore; set prod env + apply probe-gated migrations. Google Play is a decided post-iOS deferral.)*

Prior Task: *(**LA1 Memory-Care Clinical Readiness — COMPLETE** (presentation-only clinical-workflow polish; committed locally, unpushed): removed the fabricated biometric/Alzheimer-risk Insights charts (non-diagnostic integrity — resolves the RC3-flagged disclose-or-remove item) + reality-orientation date on Home + honest My-Nest reminder copy + caregiver access-level clarity + legible medication-reminder hint. Before it, RC5 CERTIFIED FOR APP STORE + PRODUCTION (94/100). The remaining launch gates are OPERATOR/infra only: wire PrivacyInfo.xcprivacy into the Xcode target + native rebuild; confirm daily backups + establish a Storage-bucket backup + run a test restore; set prod env + apply probe-gated migrations. Google Play is a decided post-iOS deferral.)*

Next Task: *(no engineering launch blocker remains)* — the remaining iOS-launch items are **operator/product/legal**: apply the LA5.1 moderation migration (operator), resolve the `/terms` governing-law jurisdiction (legal), and assemble the App-Store submission package (product). The **UGC report/block mechanism (Apple 1.2)** — formerly the single highest-priority engineering blocker — is **DONE (LA5.1)**.

Estimated Tasks Remaining (to launch): **~8** — **0 engineering blockers** + ~8 operator/product/legal (see CURRENT LAUNCH BLOCKERS). Post-launch roadmap is separate.

==================================================
## VERIFIED COMPLETE
==================================================

*Verified against actual source. **Do NOT suggest, rebuild, or re-flag these unless explicitly asked.***

**Remy companion platform**
- ✔ Public API (`@/lib/remy` — the only import path) · Event Bus · Brain · Emotion Engine · Policy Engine — one of each, `lib/remy/core/*`
- ✔ Single Renderer (`components/remy/Remy.tsx`, `<Remy state>`) · Provider (`RemyProvider.tsx`, mounted once)
- ✔ Presentation layer + two render surfaces (`RemyStage` in-place, `FloatingCompanionLayer` floating)
- ✔ Expression vocabulary (17) · Behaviour vocabulary (17, `behavior.ts`) · Animation cues (7)
- ✔ Autonomous floating companion (reacts to memory/search/offline events, session greeting, rest→sleep)
- ✔ Asset registry (`asset-registry.ts`) — 23 real approved scene/support PNGs + **17 derived avatar-tier PNGs** (2026-07-13: `avatarSrc`/`RemyAssetVariant`/`resolveRemyAssetSrc`, scene fallback; `<Remy assetVariant>`; avatar adopted by Nest/NestMenu/RemyMomentChip/FloatingCompanionLayer), 0 placeholders; `remy_master_v1.png` immutable/unregistered

**The Nest** (bottom-nav center)
- ✔ Persistent living nest · Wake choreography (resting→waking→peeking→emerging→greeting→returningHome, behaviour-driven, **no menuOpen state**) · Behaviour player (`use-nest-interaction.ts`) · Portaled menu · a11y · reduced-motion
- ✔ Nest choreography + evolution model in the platform (`lib/remy/core/nest.ts`)
- ✔ **Living ambient presence** — glow + drifting motes + breathing; **time-of-day lighting** (moonlight at night, `lib/remy/core/time-of-day.ts`) + night→sleeping resting look + time-appropriate greeting
- ✔ **Nest evolution wired to REAL memory counts** — 6 stages Tiny→Cozy→Family→Golden→Memory Tree→Sanctuary; app-shell count → `AppNavbar` → `MobileBottomNav` → `Nest` (no placeholder)
- ✔ **framer-motion motion primitives** (`components/remy/motion/primitives.tsx`) — the NestMenu "Remy offers actions" staggered reveal (de-menu-ified); ambient loops stay CSS

**Companion presence (app-wide)**
- ✔ **Screen awareness** — Remy reacts as you move between screens (`RemyScreenAwareness` + `lib/remy/core/screen-behavior.ts`; timeline/people/library/reminders/settings/dates/dashboard → brief fitting reaction; memories/search/remy/home omitted so Remy never double-reacts)
- ✔ **Milestone celebrations from REAL memory counts** (`RemyMilestones` + `lib/remy/core/achievements.ts` + `lib/remy/companion/persistence.ts`; first/10/50/100/500/1000 + Nest stage-ups; baselines on first load → no retroactive spam)
- ✔ **Celebration surface** (`RemyCelebration`) — centre-stage feather-burst + sparkles + heart via the single `<Remy>` renderer + the **real `goldenFeather` asset**; portaled, `pointer-events-none`, aria-live, reduced-motion-safe
- ✔ **Reusable companion effects** (`components/remy/effects/RemyEffects.tsx`) — FeatherBurst / Sparkles / HeartPulse (framer-motion, centralized, reduced-motion-safe)
- ✔ Event-bus **`{ replay: false }` subscribe option** — a secondary listener never steals the Brain's initial-mount replay buffer (order-independent)

**Companion Intelligence (behavioural, proactive)**
- ✔ **Insights Engine** (`lib/remy/core/insights-engine.ts`, PURE) — a `CompanionSnapshot` → behavioural `Observation[]` (morning/evening greeting, first-visit-today, returning-after-days / long-inactivity, reminders-due-today, all-reminders-completed, reminders-completed-today, memories-this-week, no-memories-today, nest-evolved; birthday-tomorrow rule present, data-wired when a source exists). No React/DOM/DB/timers/clock.
- ✔ **Priority Engine** (`lib/remy/core/priority-engine.ts`, PURE) — dedupe + cooldown-drop + rank (urgency→importance) → **at most ONE** moment; no spam.
- ✔ **RemyMoments** (`components/remy/companion/RemyMoments.tsx`, mounted once) — **once per app-open** (not polled) it fetches a real snapshot, runs the engines, and briefly shows one moment via the single `<Remy>` renderer; portaled, tap-to-dismiss, aria-live, reduced-motion-safe.
- ✔ **Read-only snapshot loader** (`app/api/remy/companion-snapshot`) — auth-gated, workspace-scoped memory/reminder head-counts; degrades to zeros (never errors the companion).
- ✔ **Behavioural memory** (`lib/remy/companion/persistence.ts` `CompanionMemory`) — last-visit day (greeting once/day + inactivity), acknowledged Nest stage, per-kind cooldowns.

**Living Relationship System (long-term, behavioural)**
- ✔ **Six PURE engines** (`lib/remy/core/`): `relationship-engine` (`RelationshipSnapshot` → warm long-term `RelationshipObservation[]`: welcome-back, memory + first-memory anniversaries, favourite person, new family member, "preserved N together", sanctuary, revisit-chapter), `story-engine` (`buildChapters` — decade + dominant-category, no hardcoded names), `anniversary-engine` (`findAnniversaries` — real day-precision dates only), `favourite-engine` (`rankFavouritePeople` — real `mention_count` + optional view/search), `legacy-engine` (`buildLifeSummary` — timeline/chapters/key-people/major-events, NOT AI), `legacy-export` (`buildLegacyExport` — structured object for a future PDF/book). Shared types in `family-types.ts`.
- ✔ **`RemyRelationship`** (`components/remy/companion/`, mounted once) — once per app-open (not polled) it reads a real snapshot, runs the engines, and shows AT MOST ONE relationship moment via the shared chip; yields to `RemyMoments` via a longer delay + the moment gate.
- ✔ **Read-only snapshot loader** (`app/api/remy/relationship-snapshot`) — auth-gated, workspace-scoped memories + people (`people`/`mention_count`) + dated memories (effective `memory_date ?? created_at`); degrades to empty.
- ✔ **Shared UI + coordination:** `RemyMomentChip` (one chip impl reused by both moment surfaces), `moment-gate` (process-wide "one moment at a time" mutex), `day.ts` (shared date helpers), generic `selectMoment<T extends RankableMoment>` (one selector for both).
- ✔ **Relationship memory** (`persistence.ts` `RelationshipMemory`) — cooldowns, acknowledged people-total / favourites / anniversaries / chapters, visited memories, favourite cache.

**Emotional Intelligence (significance over quantity)**
- ✔ **significance-engine** (`lib/remy/core/`, PURE) — ranks memories by SIGNIFICANCE not recency (people/favourite/anniversary/chapter-size/attachments/ai-importance/revisits + future pinned/conversation).
- ✔ **emotional-engine** (PURE) — `EmotionalProfile`: most significant person/memory, strongest chapter, most active relationship, most revisited memory, most emotional category + 4 INTERNAL 0–100 scores (family-strength / life-continuity / relationship-health / memory-preservation). Scores never rendered raw.
- ✔ **personality-engine** (PURE) — behavioural TRAITS (family-historian / memory-guardian / story-teller / legacy-builder / care-champion / photo-collector / daily-rememberer / occasional-visitor) from real thresholds; behaviours only, no scores.
- ✔ **relationship-engine consumes the EmotionalProfile** — 5 new observations ("…has become part of your family's story", "X seems especially important", "…shaped much of your family's history", "your family returns to this story often", "you've carefully protected these memories").
- ✔ **Pipeline wired in RemyRelationship**: snapshot → **memory-understanding → memory-graph → journey → life-story → reasoning → biography → conversation-foundation → question-understanding → answer-planning → answer-assembly → conversation-rendering → conversation-composer → conversation-verbalizer** → story → favourite → anniversary → significance → emotional → personality → relationship → priority → one `<Remy>` renderer. (Everything up to answer-assembly is deterministic INTELLIGENCE feeding significance; **conversation-rendering** [1st presentation layer] + **conversation-composer** [1st NL-planning layer] + **conversation-verbalizer** [1st provider-boundary layer — real LLM DEFERRED] are PRESENTATION/PROVIDER-side — they consume only the render/composition/assembly and feed NOTHING.) Snapshot enriched (real `attachments`/`ai_importance`/`memory_person_links`/historical flag).

**Memory Understanding (internal semantic layer)**
- ✔ **memory-understanding-engine** (`lib/remy/core/`, PURE) — `buildMemoryUnderstanding(DatedMemory[]) → MemoryUnderstanding[]`: per-memory themes, life stage, importance (ordinary/important/major/legacy), attachment/emotional/relationship richness, event type, time span, structured relationship (primary/secondary people, participants, family vs individual), and a confidence (0–100). **Deterministic, real-data-only, structured data only (no prose/GPT/fabrication).** Sits at the FRONT of the pipeline (right after the snapshot); INTERNAL (not shown) — feeds the downstream richness ratios today and is the input for FUTURE engines. Types in `family-types.ts` (`MemoryUnderstanding`/`MemoryTheme`/`LifeStage`/`MemoryImportance`/`MemoryRelationship`); exported from `@/lib/remy`.
- ✔ **memory-graph-engine** (`lib/remy/core/`, PURE) — `buildMemoryGraph(MemoryUnderstanding[]) → MemoryGraph` (nodes / edges / clusters). Deterministic semantic LINKS between memories: an edge joins two memories that share REAL attributes (same-person / same-family / same-theme / same-chapter / same-year / same-category / same-event / same-life-stage), weighted by fixed shared-attribute weights → `weak|moderate|strong`; theme-based clusters (Family Memories / Travel / School Years / Work Life / Medical Journey / Pet Memories / …). Pruned (`MIN_EDGE_WEIGHT`) + deterministically capped (`MAX_EDGES`). **No GPT, no fabricated links.** INTERNAL (not shown); sits after memory-understanding; its per-memory edge-degree feeds the significance engine (more connected = more significant — a clean optional `SignificanceContext` extension). Types (`MemoryGraph`/`MemoryNode`/`MemoryEdge`/`MemoryEdgeType`/`ConnectionStrength`/`MemoryCluster`); exported from `@/lib/remy`. Foundation for future related-memories / journeys / semantic-search / reasoning.
- ✔ **journey-engine** (`lib/remy/core/`, PURE) — `buildJourneys({ understandings, graph, … }) → JourneyAnalysis` (`journeys` / `connections` / `summary`). Understands COMPLETE LIFE JOURNEYS: a Journey is a structured collection of connected memories representing ONE continuous part of a life (School Years / University / Career / Family Holidays / Medical Journey / Care Journey / …), that EMERGES from real signals only — dominant theme + life stage + shared people + chronological continuity + graph connectivity. Memories are grouped by dominant theme then split into continuous segments by real year gaps (`> MAX_GAP_YEARS`, or a chapter change with a smaller gap when chapters are supplied); a segment below `MIN_JOURNEY_SIZE` (or otherwise unconfident) is **left separate** (never force-merged). Each journey carries deterministic `significance`/`continuity`/`importance` + a real `JourneyTimeline`; journeys are linked by real shared people/theme (`JourneyConnection`). Undated memories never fabricate a year (`startYear/endYear = 0`). **No GPT, no fabricated journeys/years/transitions/links.** REQUIRED inputs are understanding + graph (the outputs available at its pipeline position); chapters/significant/emotional/favourites are OPTIONAL refinements (no-op today). INTERNAL (not shown); sits after memory-graph; its per-memory journey significance feeds the significance engine (a clean optional `SignificanceContext` extension). Types (`Journey`/`JourneyStage`/`JourneyTimeline`/`JourneyImportance`/`JourneyConnection`/`JourneySummary`/`JourneyAnalysis`); exported from `@/lib/remy`. Foundation for future timeline / journeys / life-story / reasoning surfaces.
- ✔ **life-story-engine** (`lib/remy/core/`, PURE) — `buildLifeStory({ journeyAnalysis, graph?, understandings?, chapters?, significantMemories? }) → LifeStoryAnalysis` (`story` / `chapters` / `timeline` / `milestones` / `summary`). The canonical, structured CHRONOLOGICAL life story assembled ONLY from real journeys — the source for future AI conversation / biography / timeline UI / story-book export / reasoning. Journeys are chronologically ordered (undated last) then chained into **chapters** by single-linkage: two adjacent journeys join only when their years are continuous (dated gap `≤ MAX_CHAPTER_GAP`; a dated gap `> MAX_HARD_GAP` **always splits** — disconnected life periods are never merged), their life stages are compatible, AND a REAL relational signal supports it (shared people / journey-or-graph connection / same theme / shared life chapter). Chapters carry deterministic `continuity`/`centrality`; the timeline/milestones/summary reference only EXISTING journeys/years/memories (no prose/narration; titles reuse real journey titles). **Undated memories never fabricate a year** (`startYear/endYear = 0`); **NO GPT, no invented chapters/years/events, no merged disconnected journeys.** REQUIRED input = `JourneyAnalysis`; graph/understandings/chapters/significant are OPTIONAL refinements (graph + understandings passed live). INTERNAL (not shown); sits after journey; its per-memory life-story centrality feeds the significance engine (clean optional `SignificanceContext` extension). Types (`LifeStory`/`LifeStoryChapter`/`LifeStoryTimeline`/`LifeStoryTimelineEntry`/`LifeStoryMilestone`/`LifeStorySummary`/`LifeStoryAnalysis`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (4 lenses / 12 points, 0 findings).
- ✔ **reasoning-engine** (`lib/remy/core/`, PURE) — `buildReasoning({ journeyAnalysis, lifeStory, graph, understandings, …optional })  → ReasoningAnalysis` (`anchors` / `themes` / `influences` / `relationshipStrengths` / `gaps` / `summary`). Remy's STRUCTURAL understanding OF a life, reasoned deterministically from the real journey/life-story/graph/understanding layers: **Life Anchors** (dominant structural pillars — a theme with `≥ MIN_ANCHOR_MEMORIES` real journey-memories; `"other"` is never an anchor; carries `strength` + real-signal `confidence`), **Life Themes** (lifetime theme distribution + share), **Life Influences** (people with greatest real influence — memory/journey counts + graph degree + optional favourite bonus), **Relationship Strengths** (structured counts only — memory/journey/co-appearance, NO emotional reading), and **Memory Gaps** (FACTUAL only — large year gaps, sparse/missing-between-present/weakly-documented life stages; **never a guess at WHY** — no reason field). Its per-memory anchor strength feeds the significance engine (clean optional `SignificanceContext` extension). All scores structured numbers (never prose); **no GPT, no fabricated anchors/people/dates/chronology, no inference beyond the real data**. REQUIRED inputs = journey + life-story + graph + understanding; emotional/personality/relationship/favourite/significant are OPTIONAL refinements. INTERNAL (not shown); sits after life-story. Types (`ReasoningAnalysis`/`LifeAnchor`/`LifeTheme`/`LifeInfluence`/`RelationshipStrength`/`MemoryGap`/`MemoryGapKind`/`ReasoningSummary`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings).
- ✔ **biography-engine** (`lib/remy/core/`, PURE) — `buildBiography({ journeyAnalysis, lifeStory, reasoning, graph, understandings, …optional }) → BiographyAnalysis` (`sections` / `periods` / `references` / `coverage` / `summary`). A STRUCTURED (non-prose) representation of a life — the foundation for a future biography / story-book renderer. **Sections** mirror the real life-story chapters 1:1 (id/title[reused chapter title]/journeyIds/chapterIds/memoryIds/theme/lifeStage + `coverage` [breadth] + `confidence` [dated/media/peopled backing]); **Periods** group sections chronologically by life stage using ONLY real memory years (0 when undated — never invents a date); **References** point ONLY at real journey/chapter/anchor/theme/person/memory ids (bounded — `MAX_PEOPLE_REFS`/`MAX_MEMORY_REFS`); **Coverage** = structured metrics (memory/journey/chapter/life-stage/timeline coverage + confidence); **Summary** = dominantTheme/dominantAnchor/coveredYears/coverage/confidence. **No paragraphs, no narration, no fabricated memories/people/dates/chronology.** Deterministic (chapters-order + fixed stage order; structural ids; no clock/randomness). INTERNAL (not shown); sits after reasoning; its per-memory section coverage feeds the significance engine (clean optional `SignificanceContext` extension). REQUIRED inputs = journey + life-story + reasoning + graph + understanding; favourite/emotional/relationship/significant are OPTIONAL refinements. Types (`BiographyAnalysis`/`BiographySection`/`BiographyPeriod`/`BiographyReference`/`BiographyReferenceKind`/`BiographyCoverage`/`BiographySummary`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings).
- ✔ **conversation-foundation-engine** (`lib/remy/core/`, PURE) — `buildConversationFoundation({ journeyAnalysis, lifeStory, reasoning, biography, graph, understandings, …optional }) → ConversationFoundation` (`topics` / `threads` / `references` / `context` / `summary`). The deterministic groundwork a FUTURE conversational layer will consume — **NOT chat, NOT GPT, NOT an LLM, NOT prompts, NOT generated text.** **Topics** = REAL recurring subjects (kind anchor / theme / person / life-stage; each `≥ MIN_TOPIC_MEMORIES` real memories — `"other"` excluded, a theme already an anchor is not duplicated); **Threads** = a topic's memories grouped by the real biography chapter they live in (`≥ MIN_THREAD_MEMORIES`); **References** = pointers ONLY to real memory/journey/chapter/anchor/theme/person ids (bounded); **Context**/**Summary** = structured metrics only. **No invented topics/threads/memories/people/dates, no narration/prompts/generated sentences** — every output is a structured number or a real id/enum. Deterministic (sorted topics/threads + fixed stage order + structural ids; no clock/randomness). INTERNAL (not shown); sits after biography; its per-memory topic weight feeds the significance engine (clean optional `SignificanceContext` extension). REQUIRED inputs = journey + life-story + reasoning + biography + graph + understanding; favourite/relationship/significant/emotional are OPTIONAL refinements. Types (`ConversationFoundation`/`ConversationTopic`/`ConversationTopicKind`/`ConversationThread`/`ConversationReference`/`ConversationReferenceKind`/`ConversationContext`/`ConversationSummary`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings).
- ✔ **question-understanding-engine** (`lib/remy/core/`, PURE) — `buildQuestionUnderstanding({ conversationFoundation, biography, reasoning, lifeStory, journeyAnalysis, graph, understandings, …optional }) → QuestionUnderstanding` (`intents` / `focus` / `constraints` / `references` / `context` / `summary`). The deterministic layer a FUTURE conversational layer will use to convert a PARSED question into structured RETRIEVAL INTENT over the real intelligence stack — **NOT chat, NOT GPT, NOT an LLM, and takes NO free-text question.** **Intents** = the answerable retrieval intents (kinds memory/person/journey/theme/life-stage/relationship/timeline/event/date/**place**/comparison/summary/reference), each derived from a REAL upstream entity with its own `focus` (structured ids) + `weight` + `confidence`; **the `place` kind is NEVER produced** (the schema stores no locations — a supported kind with no real backing yields ZERO intents rather than a fabricated one). **Focus** = real structured ids only (memory/journey/chapter/anchor/theme/person/life-stage); **Constraints** = structured only (date-range / life-stage / person / theme / journey); **References** = pointers ONLY to real memory/journey/chapter/theme/anchor/person; **Context**/**Summary** = structured metrics only. **No natural language, no generated text, no invented ids.** Deterministic (all foci sorted via `makeFocus`; intents sorted + capped per-kind + globally; structural ids `intent-<kind>-<slug>`; no clock/randomness). INTERNAL (not shown); sits after conversation-foundation; its per-memory intent weight feeds the significance engine (clean optional `SignificanceContext` extension). REQUIRED inputs = conversation-foundation + biography + reasoning + life-story + journey + graph + understanding; favourite/relationship/significant/emotional are OPTIONAL refinements. Types (`QuestionUnderstanding`/`QuestionIntent`/`QuestionIntentKind`/`QuestionFocus`/`QuestionConstraint`/`QuestionConstraintKind`/`QuestionReference`/`QuestionReferenceKind`/`QuestionContext`/`QuestionSummary`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings).
- ✔ **answer-planning-engine** (`lib/remy/core/`, PURE) — `buildAnswerPlan({ questionUnderstanding, conversationFoundation, biography, reasoning, lifeStory, journeyAnalysis, graph, understandings, …optional }) → AnswerPlan` (`steps` / `sources` / `context` / `coverage` / `summary`). The deterministic EXECUTION PLAN a FUTURE conversational layer will run AFTER Question Understanding — **NOT chat, NOT GPT, NOT an LLM, and produces NO answers.** **Steps** = an ordered list of structured retrieval steps (kinds memory/journey/chapter/theme/anchor/person/timeline/relationship/event/summary/comparison/**reference**), each EXECUTES a real question intent (`STEP_KIND_OF_INTENT` map; `place` intent → no step; `reference` step kind reserved/never produced) and points only at real ids copied from the intent focus; sorted by weight and `order`-numbered. **Sources** = the real entity pool the steps draw from — journey/chapter/anchor/theme/person from the steps, each memory's real biography chapter, **memory sources ranked by real graph connectivity** (capped), plus lifeStory milestones and optional significant/favourite entities. **Coverage**/**Context**/**Summary** = structured metrics only. **No prose, no generated answers, no invented ids.** Deterministic (steps from ordered intents + fixed map + sort/slice; sources from ordered steps + graph-ranked memories; structural ids `step-<kind>-<slug>`; no clock/randomness). ALL 8 required inputs genuinely consumed (verified). INTERNAL (not shown); sits after question-understanding; its per-memory step weight feeds the significance engine (clean optional `SignificanceContext` extension). Types (`AnswerPlan`/`AnswerPlanStep`/`AnswerPlanStepKind`/`AnswerPlanSource`/`AnswerPlanSourceKind`/`AnswerPlanContext`/`AnswerPlanCoverage`/`AnswerPlanSummary`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings).
- ✔ **answer-assembly-engine** (`lib/remy/core/`, PURE — the FINAL deterministic intelligence layer) — `buildAnswerAssembly({ answerPlan, questionUnderstanding, conversationFoundation, biography, reasoning, lifeStory, journeyAnalysis, graph, understandings, …optional }) → AnswerAssembly` (`sections` / `chronology` / `evidence` / `memories` / `people` / `journeys` / `chapters` / `anchors` / `themes` / `references` / `coverage` / `context` / `summary`). Assembles ONLY the structured, FACTUAL answer PACKAGE a FUTURE conversational layer will VERBALIZE — **NOT chat, NOT GPT, NOT an LLM, and generates NO answers.** **Sections** = the answer-plan steps rendered as structured sections (fixed `SECTION_KIND_OF_STEP` map; kinds memory/journey/chapter/timeline/relationship/theme/anchor/comparison/summary), real ids + weight/confidence/order only. **Chronology** = deterministic order from the real life-story chapters (ids/order/confidence only — no generated timeline text). **Evidence**/**references** = real supporting entities (memory/journey/chapter/anchor/theme/person) aggregated + deduped + ranked + bounded (each memory's real biography chapter added; memories additionally ranked by real graph connectivity). **Coverage** = memory/journey/chapter/theme/anchor/person coverage + timeline/answer completeness + confidence + context depth (all 0–100). **No prose, no narration, no generated answers, no invented ids** — every field is a structured id, enum, or number; if data doesn't exist it produces ZERO output. Deterministic (sections from ordered steps + fixed map + sort/slice; evidence Map drained by sort; graph-ranked memories; structural ids `assembly-<slug>`; no clock/randomness). ALL 9 required inputs genuinely consumed (verified). INTERNAL (not shown); sits after answer-planning; its per-memory section weight feeds the significance engine (final clean optional `SignificanceContext` extension, `Math.min(2, x/50)`). Types (`AnswerAssembly`/`AnswerSection`/`AnswerSectionKind`/`AnswerEvidence`/`AnswerEvidenceKind`/`AnswerChronology`/`AnswerAssemblyContext`/`AnswerAssemblyCoverage`/`AnswerAssemblySummary`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings). **This completes the deterministic intelligence stack.**
- ✔ **conversation-rendering-engine** (`lib/remy/core/`, PURE — the **FIRST PRESENTATION-LAYER** engine) — `buildConversationRender({ answerAssembly, tone?, verbosity?, perspective? }) → ConversationRender` (`sections` / `metadata` / `summary` / `context`). Adds NO intelligence: it consumes ONLY the already-complete `AnswerAssembly` (+ optional deterministic tone/verbosity/perspective controls) and prepares deterministic RENDER INSTRUCTIONS for a FUTURE conversational/LLM layer. It does NOT retrieve / search / rank / reason / infer / compare / evaluate evidence / build-or-change chronology / generate facts, and it generates NO natural language. **Sections** = the top-`maxSections` (per verbosity) assembly sections as render instructions (structural `render-<sectionId>` id + `style` hint [highlight/standard/supporting/summary from section kind+weight] + importance + the section's own real `evidenceIds`, no text). **Metadata** = `preferredOpening`/`preferredClosing` (structural render-section-id POINTERS, never text) + `emphasis` (ids) + continuity + confidence. **Context** = the resolved tone/verbosity/perspective + section budget + includeChronology flag + depth. **Summary** = section/evidence counts + confidence + renderComplexity. **No prose, no narration, no generated answers, no prompts, no invented ids** — every field is a structured id, enum, or number; empty assembly → empty render. Deterministic (assembly sections already sorted; slice+map; Set drained by sort; structural ids; no clock/randomness). **This engine deliberately does NOT feed the significance pipeline** (presentation prep, not a memory signal — no `significance-engine` change this phase); in `RemyRelationship` it is computed then `void`-ed (its consumer is the FUTURE conversational layer). INTERNAL (not shown); sits after answer-assembly. Types (`ConversationRender`/`ConversationRenderSection`/`ConversationRenderMetadata`/`ConversationRenderContext`/`ConversationRenderSummary`/`ConversationTone`/`ConversationVerbosity`/`ConversationPerspective`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings).
- ✔ **conversation-composer-engine** (`lib/remy/core/`, PURE — the **FIRST NATURAL-LANGUAGE-PLANNING** layer) — `buildConversationComposition({ conversationRender, answerAssembly, style?, audience?, intent? }) → ConversationComposition` (`sections` / `paragraphs` / `sentencePlans` / `referencePlans` / `flow` / `metadata` / `context` / `summary`). Adds NO intelligence: it consumes ONLY the already-approved `ConversationRender` + the `AnswerAssembly` it renders (+ optional deterministic style/audience/intent controls) and prepares a deterministic COMPOSITION PLAN of how a FUTURE LLM/API provider would compose the answer. **It generates NO language** (no sentences/paragraphs/prose/prompts/LLM calls) and performs NO retrieval / search / ranking / reasoning / chronology-construction / significance / evidence-scoring / factual decisions. **Composition sections** map each render section → a role hint (lead/body/aside/summary) + paragraph plans; **paragraph plans** group **sentence plans** (structural roles: opening/topic/evidence/transition/closing — never text); **reference plans** point at real entity ids (kind looked up from the real answer section via `kindMapOf` — a join, not retrieval); **flow** = opening/closing section-id pointers + transition ids + section order; metadata/context/summary carry the render's tone/verbosity + counts. **No prose, no invented ids** — every field is a structured id, enum, or number; empty render → empty composition. Deterministic (iterates render sections in renderOrder; structural ids `compose-<renderId>`/`<sectionId>-p<i>`/`-s<i>`/`-r<i>`; Maps/Sets query-only; no clock/randomness). **This presentation-side engine deliberately feeds NOTHING** (no `significance-engine` change; NO prior deterministic engine changed) — in `RemyRelationship` it is computed after the render and `void`-ed (consumer = the future LLM/API layer). INTERNAL (not shown). Types (`ConversationComposition`/`ConversationCompositionSection`/`ConversationCompositionMetadata`/`ConversationCompositionContext`/`ConversationCompositionSummary`/`ConversationStyle`/`ConversationAudience`/`ConversationIntent`/`ConversationFlow`/`ConversationParagraph`/`ConversationSentencePlan`/`ConversationReferencePlan`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings).
- ✔ **conversation-verbalizer-engine** (`lib/remy/core/`, PURE — the **FIRST provider-boundary layer**) — `buildConversationOutput({ conversationComposition, conversationRender, answerAssembly, provider?, model?, temperature? }) → ConversationOutput` (`text` / `citations` / `metadata` / `tokens` / `generation`). Architecturally the ONLY place natural language would be produced, **but the actual LLM verbalization is DEFERRED** (consistent with the launch-only priority + AI-connection deferral): the engine performs **NO network / LLM call** and is a PURE, deterministic, SYNCHRONOUS function. It consumes ONLY the already-complete `ConversationComposition` (+ the `ConversationRender`/`AnswerAssembly` it composes) read-only and assembles the deterministic **PROVIDER REQUEST** a FUTURE provider adapter (OpenAI/Anthropic/…) would send: the strict **prompt** (with the mandatory 7-clause **PROMPT CONTRACT** — "you are not allowed to retrieve/rank/infer/change chronology; verbalize ONLY the supplied composition; every statement must trace to references; if info is missing, do not invent it") + the structured composition serialization; **citations** (paragraph → sentence ids → reference ids → real entity ids, for traceability); **provider metadata** (provider/model/temperature); **token** estimate; and **generation** metadata. It does **NO intelligence** (no retrieve/search/rank/infer/compare/chronology/significance/plan-building/reference-invention — every refId is resolved from an EXISTING `referencePlan`). **Deterministic vs non-deterministic boundary:** all INPUTS + this output are deterministic (`text=""`, `metadata.verbalized=false`, `generation.status="deferred"`); the ONLY non-determinism (a future LLM's WORDING) is not in this pure output — a future provider adapter (the ONLY place a `fetch` may live) fills `text` and may choose wording but NOT content. **This presentation/provider-side engine feeds NOTHING** — NO `significance-engine` change, NO prior deterministic engine changed (among `lib/remy/core/*.ts` only `family-types.ts` [additive] changed); in `RemyRelationship` it is computed after the composer and `void`-ed. INTERNAL (not shown). Types (`ConversationOutput`/`ConversationOutputMetadata`/`ConversationCitation`/`ConversationGeneration`/`ConversationTokenUsage`/`ConversationProvider`); exported from `@/lib/remy`. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings). **The next Remy layer would be the actual provider ADAPTER (real OpenAI/Anthropic call, behind the provider seam) that verbalizes this request into `text` — a separate approved phase; only there does a real LLM/network call live.**
- ✔ **conversation provider interface** (`lib/remy/providers/`, PURE abstraction — NO implementation) — the provider ABSTRACTION layer the future provider adapters implement. **It does NOT connect to OpenAI/Anthropic/Gemini/Azure/Ollama or any network** — NO fetch, NO SDK, NO async, NO network; it is 4 pure files: **`conversation-provider.ts`** (the sole `ConversationProviderAdapter` interface — `generateConversation(request: ConversationOutput): Promise<ConversationOutput>` + `configuration()` + `health()`; a provider may ONLY accept a `ConversationOutput` and return one — it MUST NOT retrieve/rank/reason/score/plan/render/compose), **`provider-types.ts`** (`ProviderName`/`ProviderCapability`/`ProviderConfiguration`/`ProviderHealth`/`ProviderResult`/`ProviderLimits`/`ProviderVersion` + `ProviderHealthStatus`), **`provider-errors.ts`** (`ProviderErrorCode` + the throwable `ProviderError extends Error` + `notImplementedError`), **`provider-registry.ts`** (a deterministic registry mapping EVERY `ProviderName` to a `DeferredProvider` stub whose `generateConversation` simply **throws "Provider not implemented."** — no async, synchronous throw; `configuration().implemented=false`, `health().status="unimplemented"`; + `getConversationProvider`/`listProviders`/`isProviderImplemented`). **Future adapters documented, NONE implemented:** OpenAI, Anthropic, Gemini, Azure OpenAI, Ollama, LM Studio, Custom Enterprise — each future adapter is the ONLY place a `fetch`/SDK/real LLM call may EVER live. **NO wiring** (RemyRelationship/UI/JSX/significance untouched), **NO existing file changed** (only the new `lib/remy/providers/` directory was added; `git diff HEAD` = pbxproj only), and it is intentionally NOT exported from `@/lib/remy` (internal infra, not a feature API — a future adapter imports the siblings directly). Deterministic + pure. Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings).
- ✔ **conversation-request-engine** (`lib/remy/core/`, PURE — provider REQUEST/RESPONSE refactor) — `buildConversationRequest({ conversationComposition, conversationRender, answerAssembly }) → ConversationRequest`. A PURE ARCHITECTURAL REFACTOR that separates the overloaded Phase-18 `ConversationOutput` (a "provider request" object carrying an empty `text`) into a dedicated **`ConversationRequest`** (provider INPUT — `prompt`/`contract`/`citations`/`metadata`/`summary`; **NO `text`**) + **`ConversationResponse`** (provider OUTPUT foundation — `text`/`provider`/`model`/`usage`/`status`/`citations`/`metadata`; **NO `prompt`**; intentionally minimal, filled by a FUTURE provider). The engine builds a `ConversationRequest` ONLY (never a `ConversationResponse`), transforming the SAME deterministic request info the verbalizer produces (prompt with the 7-clause contract embedded in `prompt.system` + exposed in `contract.clauses`; citations back to the same real ids; the same controls) into the new model. It performs **NO intelligence** (no retrieval/ranking/chronology/reasoning/language-generation — pure serialization of the ordered plans; every refId resolves from an EXISTING referencePlan; `answerSectionById.has(...)` is a membership annotation, not a search/rank). **`ConversationOutput` is NOT removed** (kept for backwards compatibility until a future migration). Deterministic (ordered iteration, query-only Maps, `ceil` token estimate; no clock/randomness). **NO provider/network/SDK/fetch/async/Promise, NO wiring, NO UI, NO index.ts export, NO significance/intelligence change, NO existing engine modified** — only `family-types.ts` (additive) + this new engine changed. Types (`ConversationRequest`/`ConversationRequestMetadata`/`ConversationPrompt`/`ConversationContract`/`ConversationRequestSummary`/`ConversationResponse`/`ConversationResponseMetadata`/`ConversationResponseUsage`/`ConversationResponseStatus`). Independent multi-agent adversarial review CLEAN (7 lenses, 0 findings). **Future migration:** a later phase migrates the provider interface (`generateConversation`) from `ConversationOutput` to `ConversationRequest`→`ConversationResponse`.
- ✔ **conversation provider migration** (`lib/remy/providers/conversation-provider.ts` + `provider-registry.ts` + `family-types.ts`) — migrated the provider ABSTRACTION from the legacy `ConversationOutput` request model to the canonical `ConversationRequest` → `ConversationResponse` architecture. **PURE, types-only, behaviour-preserving.** The `ConversationProviderAdapter` interface is now `generateConversation(request: ConversationRequest): Promise<ConversationResponse>` (was `ConversationOutput → Promise<ConversationOutput>`); `DeferredProvider`'s signature migrated identically and its **BODY is byte-unchanged** — still the synchronous `void request; throw notImplementedError(this.name)` (no async introduced; a `never`-typed throw satisfies the `Promise<ConversationResponse>` return). `configuration()`/`health()`/`getConversationProvider`/`listProviders`/`isProviderImplemented` unchanged. **`ConversationOutput` is RETAINED and marked `@deprecated`** (points to `ConversationRequest`; a future phase removes it) — the verbalizer (`buildConversationOutput`), `RemyRelationship`, `index.ts` re-export, and `provider-types.ts` `ProviderResult.output` still reference it and compile (the `@deprecated` JSDoc adds no lint warnings — `no-deprecated` isn't enabled). **NO real provider implemented** (deferred stays deferred/throws), NO network/SDK/fetch/async, NO wiring/UI/index.ts change, NO significance/intelligence change. Only the 3 listed files changed (`git diff HEAD` = those + pbxproj); the request/verbalizer/composer/rendering/significance engines, RemyRelationship, index.ts, provider-types.ts, provider-errors.ts are byte-unchanged. Independent multi-agent adversarial review CLEAN (7 lenses — provider-abstraction-correctness / migration-correctness / backwards-compatibility / platform-integrity / purity / regressions / future-provider-readiness — 0 confirmed blocking). **Known follow-up (non-blocking, pre-existing from Phase 20 — NOT introduced here):** `ConversationResponse.provider`/`ConversationResponseMetadata.provider` are typed by `ConversationProvider` (family-types, 5 values: deferred/openai/anthropic/azure/local) which DIVERGES from the registry's `ProviderName` (providers, 8 values: +gemini/azure-openai/ollama/lm-studio/custom-enterprise). A future real adapter for those 5 extra providers cannot set `provider` from its own `name` without reconciling the two enums. It is LATENT (the deferred stub only throws, never builds a `ConversationResponse`; baseline tsc clean). **The future real-adapter phase MUST reconcile `ConversationProvider` ↔ `ProviderName`** (out of scope here — would require editing `provider-types.ts`, and this phase was restricted to the 3 files above).
- ✔ **OpenAI provider adapter** (`lib/remy/providers/openai-provider.ts` + `provider-registry.ts`) — the **FIRST real production provider** behind the provider abstraction. `OpenAIProvider implements ConversationProviderAdapter` (`generateConversation(request: ConversationRequest): Promise<ConversationResponse>` + `configuration()` + `health()`). It is a **PURE EXECUTION layer**: it sends the immutable `request.prompt.full` to OpenAI **exactly as supplied** (a single `user` message; **no rewriting / injecting intelligence / reordering / enriching**), passes `request.citations` through **unchanged**, and returns a `ConversationResponse` (`text` from the completion, `provider` `"openai"`, `model`, `usage` [prompt/completion/total tokens], `status` `"generated"`, `citations`, `metadata`). It does **NO intelligence** (no retrieval/rank/reason/score/plan/render/compose/chronology). The **official OpenAI SDK + network + env + async are ISOLATED** inside `openai-provider.ts` (the ONLY `"openai"` import in the Remy provider layer; the OpenAI SDK was **already a project dependency** — no `package.json` change). **Construction is side-effect-free** (constructor reads no env / creates no client; `resolveApiKey`/`resolveModel` read `process.env.OPENAI_MODEL`/`OPENAI_API_KEY` lazily; `new OpenAI({ apiKey })` is created inside `generateConversation`) — so the registry stays deterministic and makes no network call at load. **Error handling:** missing `OPENAI_API_KEY` → `ProviderError("invalid-request")`; SDK/network failures are caught and converted via `toProviderError` (429→`rate-limited`, ≥500→`provider-failure`, else `invalid-request`; unknown→`provider-failure`) — **the raw SDK exception is NEVER leaked** (a new generic-message `ProviderError` is thrown). **No retries/timeouts/fallbacks yet** (later phases). **Registry:** `getConversationProvider("openai")` now returns the real `OpenAIProvider`; every OTHER name still maps to the `DeferredProvider` stub (preserved — still throws); `isProviderImplemented("openai")` is now `true`. **DORMANT** — nothing in `app/`/`lib/`/`components/` outside the provider layer invokes it (server-side, env-gated); the deterministic pipeline is **entirely unchanged**. **Enum fix (no mapping layer):** the response's `provider`/`metadata.provider` use the `ConversationProvider`-typed literal `PROVIDER_TAG = "openai"` (`"openai"` ∈ BOTH `ConversationProvider` and `ProviderName`); `this.name` stays `ProviderName`. **Pre-work:** the Phase-21 `ConversationProvider` (5) ↔ `ProviderName` (8) unification was investigated and **preserved** (unification needs either a core→providers dependency inversion or a breaking change to `ConversationProvider`'s members — neither "without changing public architecture"; OpenAI needs no unification since `"openai"` is in both, and NO temporary mapping layer was introduced). Only `provider-registry.ts` + the new `openai-provider.ts` changed; `family-types.ts`, the request/verbalizer/composer/rendering/significance engines, `RemyRelationship`, `index.ts`, `conversation-provider.ts`, `provider-types.ts`, `provider-errors.ts`, and `package.json` are byte-unchanged. Independent multi-agent adversarial review CLEAN (7 lenses — provider-correctness / sdk-correctness / error-handling / architecture-purity / regressions / platform-integrity / future-multi-provider-readiness — 0 findings). **Known follow-up STILL OPEN:** reconcile `ConversationProvider` ↔ `ProviderName` before a real adapter for gemini/azure-openai/ollama/lm-studio/custom-enterprise is built.
- ✔ **Provider registry activation** (`lib/remy/providers/provider-registry.ts` + `conversation-provider.ts` + `provider-types.ts`) — the registry is now the **SINGLE authoritative provider resolver** (verified: nothing outside `lib/remy/providers/` imports/constructs/resolves a provider; the layer is fully **DORMANT** — `getConversationProvider` is not even called yet). Added a **resolution-only production seam**: **`PRODUCTION_PROVIDER: ProviderName = "openai"`** (a **fixed deterministic literal**, never env-derived) + **`getProductionProvider()`** which **delegates to `getConversationProvider(PRODUCTION_PROVIDER)`** (single canonical resolver — **no second path**). Both **RESOLVE ONLY** — they return an adapter; they **never call `generateConversation`, open a network connection, read env, or execute** (Phase 24 will invoke the seam). `"openai"` → real `OpenAIProvider`; every other name → `DeferredProvider` (still **THROWS** `notImplementedError`, behaviour byte-unchanged). **Registry construction stays deterministic** (no network/SDK/env/clock at load or construction). Now-false post-Phase-22 docstrings ("NONE implemented" / "always false" / "ONLY registered adapter") were corrected — **interface signatures + type definitions byte-identical** (comment-only); the legacy **`ProviderResult`** (typed on the deprecated `ConversationOutput`) left **byte-unchanged** (belongs to the enum-reconciliation follow-up). **NO runtime activation, NO UI/routes/API/significance/RemyRelationship change, NO `package.json`/`ConversationRequest`/`ConversationResponse`/`family-types` change.** Independent MULTI-AGENT adversarial review CLEAN (7 lenses — registry-correctness / provider-abstraction / platform-integrity / regression / deterministic-construction / future-multi-provider / architectural-purity — every raw finding adversarially refuted; 0 confirmed blocking). tsc/lint/build green. **Phase 24 will ACTIVATE** (wire `getProductionProvider()` into an execution path).
- ✔ **Production provider activation** (`lib/remy/providers/conversation-execution.ts`, new — the ONLY change) — the **FIRST end-to-end conversation EXECUTION path**. Exports one async **`executeConversation(input): Promise<ConversationResponse>`** (input = the request engine's `ConversationRequestInput` = `{ conversationComposition, conversationRender, answerAssembly }`) that composes, in order: **`buildConversationRequest(input)`** (pure Phase-20 request engine → immutable `ConversationRequest`, incl. `prompt.full`) → **`getProductionProvider()`** (Phase-23 registry seam → `OpenAIProvider`) → **`provider.generateConversation(request)`** → `ConversationResponse`. The provider receives **`request.prompt.full` EXACTLY as built** (no rewrite/inject/enrich/manipulation). **NO intelligence** (no retrieval/ranking/chronology/reasoning/significance/planning/language-gen); **uses `getProductionProvider()`** (never instantiates a provider, **never imports OpenAI**, **never bypasses the registry**); `ConversationRequest`/`ConversationResponse` + every engine preserved **exactly**. Lives in the **provider layer** (network-capable boundary); imports only the pure `buildConversationRequest` from core (safe one-way providers→core; core stays provider-free; no cycle). **Side-effect-free import** (no clock/`Math.random`/env read/provider construction at module load — the LLM call is the ONLY non-determinism, exactly at `generateConversation`). The deprecated Phase-18 verbalizer (`buildConversationOutput` → `@deprecated ConversationOutput`) is **NOT** in the live path (superseded by the request engine). **DORMANT** — **nothing invokes it yet** (no UI/route; NOT wired into `RemyRelationship`); Phase 25 wires it into a user-facing conversational surface. **Exactly ONE execution path, no duplicate resolution.** Independent MULTI-AGENT adversarial review CLEAN (7 lenses — execution-correctness / provider-correctness / architecture-purity / runtime-regression / platform-integrity / deterministic-execution / future-multi-provider-readiness — **0 findings**). tsc/lint/build green.
- ✔ **Live conversation integration** (the FIRST user-facing AI execution of the conversation platform) — the opt-in **`/remy/story` "Remy narrates your story"** surface invokes `executeConversation` on an explicit user tap. **4 new files + 1 tiny edit:** `lib/remy/story-pipeline.ts` (PURE orchestrator `buildStoryConversationInputs(snapshot)` — sequences the EXISTING 12 engines in the EXACT order/inputs `RemyRelationship` uses → `{ conversationComposition, conversationRender, answerAssembly }`; no new engine/pipeline; deterministic), `app/(app)/remy/story-action.ts` (`"use server"` `narrateStoryConversation()` — auth-gated + workspace-scoped snapshot **mirroring `/api/remy/relationship-snapshot`** → orchestrator → **`executeConversation`** → **structured `{ text, status, memoryCount }` that NEVER throws**; **server-authoritative prompt** — client supplies nothing; 0 memories → `"empty"` without a provider call; failure/unconfigured `OPENAI_API_KEY` → `"unavailable"`), `components/remy/RemyStoryConversation.tsx` (client button → renders `.text` + `AIDisclaimer`), `app/(app)/remy/story/page.tsx` (the page); **edit** `app/(app)/remy/page.tsx` (one discovery `<Link>`). **`executeConversation` now has EXACTLY ONE caller — no dormant seam remains.** **Reuse-not-redesign:** the live Ask Remy chat (`ask-action.ts`), `RemyRelationship`/app-open path, every deterministic engine, and the whole provider layer are **byte-unchanged**; `executeConversation`/`getProductionProvider()`/`OpenAIProvider` used exactly as built; no direct OpenAI import outside the provider layer; `package.json` unchanged. Requires a server `OPENAI_API_KEY` (operator env); iOS purchase-compliance unaffected. Independent MULTI-AGENT adversarial review CLEAN (7 lenses — execution / provider / architecture-purity / runtime-regression / security-scoping / determinism / platform-integrity — 0 confirmed blocking; 3 refuted-but-worthwhile robustness fixes applied + re-verified SOUND). tsc/lint/build green. **Follow-ups (not blockers):** no per-question quota/entitlement gate or rate-limit on the story surface's provider call yet.
- ✔ **AI usage, billing & observability** (production hardening — instrumentation only; no redesign) — every real AI execution now records **user / workspace / provider / model / operation / real tokens / latency / estimated cost / status / error_code** to a new **`ai_usage`** table. Instrumentation lives in **`lib/remy/execute-conversation-with-usage.ts`** — the wrapper that is now `executeConversation`'s **SINGLE caller** (the Phase-24 LOCKED "no persistence inside `executeConversation`" is honoured; `executeConversation` + the whole provider layer are **byte-unchanged**). **ONE execution path** (UI → story-action → wrapper → `executeConversation` → `getProductionProvider()` → `OpenAIProvider`); the wrapper is a decorator, not a second path. **Cost:** `lib/ai/usage/cost.ts` — the SINGLE, isolated, provider-independent, model-aware price table + `estimateCostUsd` (unknown model → 0; dated-snapshot prefix fallback). **Recorder:** `lib/ai/usage/ai-usage.ts` — `classifyAiError` (missing_api_key/timeout/rate_limited/provider_unavailable/invalid_request/invalid_response/unknown) + `recordAiUsage` (service-role, scoped by explicit user_id; **NEVER throws AND never hangs** — swallows errors, 3 s timeout bound). **Quota (architecture only, NOT enforced):** `lib/ai/usage/quota.ts` — `getUsageToday/getUsageThisMonth/getEstimatedMonthlyCost/canExecuteConversation` (`AI_USAGE_LIMITS` default null → always allow, no DB read; degrade to zeros). **Migration** `supabase/migrations/20260711120000_ai_usage_foundation.sql` — table + indexes + **RLS (select-own; inserts service-role only)** + a **service-role-only `SECURITY DEFINER` `ai_usage_summary` aggregate** (execute revoked from public/authenticated → no IDOR). Uses REAL provider values; **INERT until the operator applies the migration** (writes/reads degrade silently → deploy is a no-op until then). Independent MULTI-AGENT adversarial review CLEAN (7 lenses — execution-integrity / observability-correctness / never-breaks-execution / cost-correctness / security-scoping / quota-architecture / scope-and-regression — 0 confirmed blocking; 2 worthwhile fixes applied [bounded log-write timeout; per-user gate]). tsc/lint/build green. **Operator step:** apply the migration (+ `SUPABASE_SERVICE_ROLE_KEY`, already set) to activate logging. Follow-ups (not blockers): no usage UI; quotas not enforced; no `ai_usage` retention/rollup.
- ✔ **AI subscriptions, quotas & usage dashboard** (production hardening — REAL subscription-aware enforcement + dashboard/API + admin analytics; no provider/prompt/architecture redesign; still ONE execution path). **Entitlements (SINGLE config):** `lib/ai/usage/entitlements.ts` — `AI_PLAN_LIMITS` by `BillingPlan` (FREE 5/day + 50/month; PREMIUM/FAMILY/ENTERPRISE unlimited) + `resolveAiEntitlement` (on `resolveSubscription`) + neutral `AI_UPGRADE_MESSAGE`; no scattered limits, no Stripe/schema change. **Quota (REAL, not dormant):** `lib/ai/usage/quota.ts` `canExecuteConversation(userId, entitlement)` → STRUCTURED (allowed/tier/reason/remaining/upgrade), **premium bypass (no DB read)**, Free enforced on SUCCESSFUL counts, degrades to zeros, **never throws** (+ pure `evaluateConversationGate`). **Enforcement is in the STORY ACTION as a PRE-check** before the pipeline build → on deny a structured `status:"quota_exceeded"` with NO provider call (action still never throws); the wrapper's old dormant gate was **removed** (now pure exec+observability). **iOS anti-steering:** `RemyStoryConversation` shows upgrade copy **web-only** (`useIsNativePlatform`) — no purchase link on native. **Dashboard/API/settings:** `lib/ai/usage/overview.ts` `getAiUsageOverview` (today/month/cost/remaining + **provider/model via the registry seam — provider-independent**), presentational `components/remy/AiUsageDashboard.tsx`, display-only **`/settings/ai`** (+ a `/settings` link), **`GET /api/remy/usage`** (auth-gated, per-user, mobile-ready JSON; degrades). **Admin analytics (SERVER-ONLY, no public route):** `lib/ai/usage/admin-analytics.ts` (global conversations/rates/latency/tokens/spend/most-used-model). **Migration** `20260711130000_ai_usage_analytics.sql` — service-role-only `SECURITY DEFINER` aggregates (`ai_usage_overview` per-user + `ai_usage_admin_*` global; execute revoked from public/authenticated → no IDOR). **Provider-independent:** the missing-key classifier no longer probes `OPENAI_API_KEY` (message-based) → no OpenAI-specific logic in the AI-usage layer. `executeConversation` + provider layer + engines + story-pipeline + Ask Remy + `RemyRelationship` + `cost.ts` + `package.json` byte-unchanged; ONE `executeConversation` caller (wrapper), ONE wrapper caller (action). Independent MULTI-AGENT adversarial review CLEAN (7 lenses — execution-integrity / quota-enforcement / entitlement-billing / security-scoping / provider-independence / data-correctness / scope-regression — 0 confirmed blocking; provider-independence fix applied). tsc/lint/build green. **Operator step:** apply BOTH `ai_usage` migrations (+ `SUPABASE_SERVICE_ROLE_KEY`, already set). Known limitation (not a blocker): the quota gate is read-then-act (soft cost cap, not a security boundary) — negligible with the serialized single-client UI.
- ✔ **Memory Intelligence Engine V2** (ADDITIVE capability + data layer; the "advanced AI memory intelligence" post-launch item; NOT wired into any live path). New subsystem **`lib/remy/memory-intelligence/`**: adaptive **importance scoring**, **relationship weighting** (`people.role`→tier; spouse/partner/parent/child High · sibling/friend/caregiver Medium · acquaintance Low), deterministic **decay** (pinned + medical/emergency/health never decay; milestone/childhood/family slow; recall rewinds age; floored), **reinforcement** (confidence up on recall / down on down-rank; pure; future feedback), cached **classification** (free-form `ai_category`→controlled taxonomy; medical/emergency bias only on a real keyword hit → a keyword-less memory falls through to `miscellaneous`), event **clustering** (wedding/hospital-stay/…), **forgotten** detection (internal), and a configurable **combined ranking** (Semantic+Importance+Relationship+Recency+Reinforcement+Confidence). **ONE central config `config.ts`** (all weights/thresholds/taxonomy). Pure deterministic engines (no clock/DB/`Math.random`; caller passes `now`) + a service-role **`store.ts`** (batch reads = no N+1; user-scoped writes; lazy defaults; never throws) + migration **`20260711140000_memory_intelligence.sql`** — a NEW `memory_intelligence` side table (**never alters `memories`**; RLS select-own + service-role writes; `reinforce_memory`/`backfill` SECURITY DEFINER, execute revoked from public/authenticated → no IDOR; reversible). Revives the dead `ai_importance` string as a real signal. Reuses (not rebuilds) `match_memories`/enrichment/existing clusters/`retrieval.ts`. **DORMANT:** nothing imports it — `executeConversation` + wrapper still ONE caller each, ONE execution path; provider layer / Ask Remy / story pipeline / billing / quota / dashboard byte-unchanged; no OpenAI import. Verified tsc/lint/build green + 28-assertion runtime formula check + independent MULTI-AGENT adversarial review (7 lenses): 1 blocking (classification bias) + 1 non-blocking (write scoping) found, both fixed + independently re-verified CLEAN. **Operator step:** apply the migration to activate persistence (degrades silently until then). Activation into a retrieval path is a FUTURE phase.
- ✔ **RC2 Security Hardening** (production security posture; no feature/UI/architecture change). **(1) HTTP security headers** (`next.config.js` on every response): CSP, HSTS (2y preload), `X-Frame-Options DENY`, nosniff, Referrer-Policy, Permissions-Policy, COOP `same-origin-allow-popups` — CSP is Capacitor/Supabase-realtime/Stripe/OneSignal/Sentry-compatible (permissive script/connect; strict `frame-ancestors none`/`object-src none`/`base-uri self`); nonce tightening is FUTURE. **(2) API rate limiting** (`lib/security/rate-limit.ts`, dependency-free in-memory sliding window, ONE `RATE_LIMITS` config, fails OPEN) on upload-url / memory-chat / memories-create / enrich / stripe-checkout / stripe-portal / gdpr-export / gdpr-delete / story-action, keyed by session user id (distributed store = operator upgrade). **(3) Logging hardening** (`lib/logger.ts` — debug/info DEV-ONLY, warn/error always): the RAG **PHI leak** (`retrieve-memory-context` logging memory content/context) removed; checkout `USER EMAIL` log removed; webhook/checkout/create/insights narration dev-gated. **(4) Dead code:** deleted 3 orphan routes (`/api/search`, `/api/create-reminder`, `/api/send-reminders`) + cleaned residual middleware/e2e refs (`/api/search/global` kept; live cron `/api/cron/send-due-reminders`). **(5) OWASP fixes** (MULTI-AGENT ASVS/API-Top-10 review — authorization otherwise CLEAN: no IDOR / no unscoped service-role query / no missing gate): `POST /api/active-profile` now auth+`userCanAccessProfile`-gates before writing the CARE cookie; `inviteCaregiver` uses a scoped escaped `.ilike` (no full-table fetch, no LIKE-wildcard); **middleware catch FAILS CLOSED**; `send-notification` no longer leaks the raw DB error; `auth/logout` uses request-origin (was hardcoded localhost); `/api/health` drops the commit SHA. Sessions/cookies already solid. Verified tsc/lint/build green + independent MULTI-AGENT adversarial review CLEAN (7 lenses, 0 confirmed blocking; all 6 non-blocking findings fixed + re-validated). **Remaining (scheduled):** ~~HIGH — memory-EDIT kept-attachment storage-quota bypass~~ **RESOLVED** (see the RC2-follow-up entry below); product decision — Ask Remy / `/api/memory-chat` AI **quota gating**.
- ✔ **RC — FINAL RELEASE CANDIDATE CERTIFICATION** (independent verification; NO code change — certification docs only). Validation GREEN; critical invariants verified intact (no regression); **independent 7-lens multi-agent certification (8 agents, 0 errors) — ALL GO/96, board GO, overall 96/100, ZERO verified engineering blockers.** **RemyNest Release Candidate is CERTIFIED for production deployment.** Residual = operator/legal/product go-live activation only. See `docs/RC-FINAL-CERTIFICATION.md`.
- ✔ **LA7 — Final Production Launch Readiness Audit** (verification pass; NO runtime/product/architecture change; audit + review completed inline [subagent limit], repo precedent). Full-system audit across every subsystem + the ~24 critical flows → **NO remaining ENGINEERING blockers for production deployment** (Overall ~90 · Engineering ~96 · Infra ~88 · Deploy ~90). **Fixed** one config-drift finding: `.env.example` wrong Stripe price-id names + missing store URLs → corrected + reconciled `check-production-env.mjs` (checkout validates + 400s on unset price — config, not a crash). **Deferred (LOW):** 3 caller-less API routes (send-notification/build-relationships/timeline) — post-launch cleanup, not removed (unseen-caller risk). Residual = operator/legal/product only (cleanly separated). tsc/lint/build green. See `docs/LA7-LAUNCH-READINESS-REPORT.md`.
- ✔ **LA6 — Disaster Recovery & Business Continuity** (documentation-first; NO runtime/product/architecture change; inline-reviewed [6 lenses; subagent limit hit → main-loop review, repo precedent], 1 false claim fixed). Produced the operational DR/BC layer complementing the existing backup docs + GDPR breach policy: **`docs/DISASTER_RECOVERY_PLAN.md`** (dependency map · RTO/RPO objectives · SPOF analysis · consolidated backup strategy · recovery ownership · severity model · automation roadmap) + **`docs/runbooks/`** (index + 5 scenario runbooks: db-and-storage-recovery · deployment-and-migration-rollback · dependency-outages · secrets-and-credential-rotation · operational-incident-response) + **`scripts/check-production-env.mjs`** (operator env-integrity check; never prints values; fails non-zero on a missing REQUIRED var) + reconciled 2 stale snapshots (OPERATIONAL_READINESS, HEALTHCHECK). **DR ~82 · BC ~80.** Verified-accurate DR facts: cron self-heals (lease/reclaim + fails-closed 401); Stripe webhook idempotent + 500-retry (backlog self-drains); **iOS reminders fire device-local** (availability fallback); `/api/health` liveness-only; rate limiter fails open. **Corrected:** only 2 of 13 migrations have `-- ROLLBACK` blocks (memory_intelligence + moderation); older ones need manual DDL reversal / restore; only additive migrations are probe-gated. **No new engineering blocker.** OPERATOR residual (now with runbooks): `memory-media` Storage backup + test restore (HIGH); uptime/Sentry-alert activation; restore drill; offline secrets copy. LOCKED/accepted: 24h Postgres RPO (PITR deferred); single region. tsc/lint/build green. See `docs/LA6-DISASTER-RECOVERY-REPORT.md`.
- ✔ **LA5.1 — Apple Guideline 1.2 UGC moderation** (CLOSES the CRITICAL LA5 iOS blocker; maintains existing architecture; NOT a social platform; multi-agent-VERIFIED — 6 lenses all `satisfiesApple12=true`, 1 blocking defect + nits ALL fixed). Minimum production-ready **report / block / leave**: **migration** `moderation_reports` + `user_blocks` (additive/reversible/probe-gated; RLS insert-own + **select-own only**, no update/delete → status service-role only; reporter identity never exposed; a blocked user can't discover who blocked them; FKs reporter/blocker→CASCADE, reported/memory→SET NULL; **deliberately NO must-have-target CHECK** — it would abort a reported user's account delete = Art 17 regression the review caught). **Server actions** (`app/(app)/settings/safety/actions.ts`; session-derived, structured/never-throw, service-role scoped, degrade-if-absent, rate-limited + dedup, authorized by `getSharedCarePeopleIds`): reportUser/reportContent/blockUser/unblockUser/leaveWorkspace/listSafetyOverview. **Block enforcement** on `inviteCaregiver` + `acceptInvite` (either direction; never removes existing care access). **UI:** `/settings/safety` Safety Center + Settings link + portaled/focus-trapped `ReportDialog` + a **Report button on care-context search results** (SearchResultRow sibling, non-navigational for other-authored rows). **GDPR:** own reports + blocks enrolled in the export (v1.2); co-caregiver emails masked. **NO remaining engineering blocker for iOS on Apple 1.2.** **OPERATOR:** apply `20260712120000_moderation_foundation.sql` + run the deletion regression before submission. **Accepted limitation:** the share-care boundary is app-layer (not RLS-encoded) — matches the repo pattern. **Follow-up:** a care-Timeline Report affordance (2nd cross-user surface; 1.2 already satisfied). tsc/lint/build green. See `docs/features/moderation.md` + `docs/LA5.1-APPLE-1.2-MODERATION-REPORT.md`.
- ✔ **LA5 — Apple App Store & Google Play Compliance** (store-review compliance audit + SAFE fixes only; behaviour-preserving; no architecture/logic/subscription/schema change; frozen reminder untouched; multi-agent-VERIFIED — behaviour preserved by all 6 lenses, **0 blocking regressions**). 6-lens audit (Apple/Play/Mobile/Privacy/Healthcare/Security), 28→21 adversarially-verified findings → **Apple ~74→~80, Play 57**. **Fixes:** (1) **completed de-medicalization** — deleted the fabricated cognitive-decline scoring (`lib/analytics/cognitiveDeclineSignals.ts` + the "Routine Changes" card + the `declineSignals` useMemo/import/prop) AND neutralized the "Memory Activity" card (`cognitiveActivity`→`loggingActivity`, "Declining"/"Reduced"→"Quiet"/"Quieter"); (2) **dropped health-app positioning** — JSON-LD `HealthApplication`→`LifestyleApplication` + removed "cognitive-care" from `lib/seo.ts`/`/terms`/`/contact`; (3) **Apple-1.2 EULA zero-tolerance abuse clause** in `/terms`; (4) **de-drugged the reminder placeholder** (Donepezil→"Morning medication"; frozen form byte-unchanged); (5) **Sentry processor disclosure** (`/privacy`+`/cookies`) + **cancel-before-delete caveat** (`/privacy`+`/account-deletion`) + **data-rights contact → `admin@` across ALL live legal pages** (`lib/contact.ts` privacy/dpo→admin@); (6) **Android `allowBackup=false`**. Independently reviewed (7 agents): 0 blocking, 1 must-fix (contact reconciliation) + 3 nits ALL applied. tsc/lint/build green. **STILL OPEN (tracked, not new):** the CRITICAL **UGC report/block MECHANISM** (Apple 1.2, feature — #1 iOS blocker); the `/terms` **jurisdiction** placeholder (legal/counsel); real contact mailboxes + controller identity (operator/legal); Health/Sensitive ASC-label + Play Data-Safety decision (product). **iOS = DO NOT SUBMIT until the mechanism + jurisdiction land; Google Play = deferred/not submittable.** See `docs/LA5-STORE-COMPLIANCE-REPORT.md`.
- ✔ **LA4 — Production Observability & Failure Recovery** (behaviour-preserving; no UI/logic/billing/AI/schema change; multi-agent-VERIFIED **SOUND-WITH-FIXES**, reliability observability 78→~86, 0 defect/regression). **The gap:** API-route catch blocks logged via `logger.error(errorMessage(e))` which only writes a Sentry console BREADCRUMB, not an error EVENT — so handled 500s were invisible/unalertable in Sentry. **Fixes (observability-only):** **(1)** added `export const onRequestError = Sentry.captureRequestError` in `instrumentation.ts` — **CORRECTED by review: INERT on Next 14.2.5** (it's a Next-15 hook; kept forward-compatible/harmless, auto-activates on a future Next 15 upgrade). **(2)** new `lib/observability/capture.ts` `captureError(error, { route, requestId })` — ENV-GATED (no-op without a DSN), PII-SCRUBBED (existing `beforeSend`; tags are ids/route only), NEVER-THROWS/NEVER-BLOCKS (try/catch + fire-and-forget) — **this is the ACTUAL server-error coverage today.** **(3)** applied at 15 key catch sites alongside the existing log (responses byte-unchanged): memories list/create/[id]/search, timeline, gdpr export/delete, send-notification, profile, build-relationships, active-profile×2, memory-chat, cron top-level crash (frozen scheduling untouched), and the story-narration action (a `catch {}` that swallowed AI failures silently). The Stripe webhook billing logic was NOT touched (its `writeFailed→500` retry is already logged). **(4)** fixed 2 residual raw-`PostgrestError` PII logs (memories/[id], gdpr/delete-account → `errorMessage`). **REVIEW + FOLLOW-UP:** the formal 6-lens multi-agent review (SRE/backend/Next.js/Supabase/prod-ops/security) verified the above + drove **6 same-class follow-up fixes**: memories/search `logSearchError` raw-PII→`errorMessage` (HIGH — file LA4 edited), Ask Remy chat `captureError` (primary live-chat swallow), Stripe checkout/portal/cancel `captureError`, upload-url + search/global silent-failure `captureError`, cron `logReminderError` raw-PII→`errorMessage`. Verified tsc/lint/build green. **OPERATOR ACTIVATION (required to realise LA4):** set the Sentry DSN (+ SENTRY_ORG/PROJECT/AUTH_TOKEN for source maps), configure alert rules, add an uptime monitor on `/api/health`. **RECOMMEND (roadmap, need table/cron):** Stripe per-event idempotency ledger; unattended `auth_pending` retry cron; orphan-object storage sweep; `reminder_local_confirmations` deletion enrolment; `ai_usage` TTL. See `docs/LA4-RELIABILITY-REPORT.md`.
- ✔ **LA3 — Performance & Scalability Hardening** (behaviour-preserving optimization; NO contract/logic/security/billing/AI change; DB schema is dashboard-managed so index tasks are OPERATOR recs; 6-specialist multi-agent audit → perf 72→~82). **9 safe fixes:** **(1)** stripped the dead pgvector `embedding` (~15-29KB/row) from the 4 hot memory reads (`/api/memories`, `/api/timeline`, timeline RSC page, `/api/memories/search`) via a runtime `stripEmbedding()` helper in `lib/memory-media-signing.ts` (clone+delete — NOT a select-list change; verified only the memory-DETAIL page reads `.embedding`, which doesn't use these paths). **(2)** memoized the memories feed (`memories/page.tsx`: `useMemo` sort+today/thisWeek/earlier grouping + `useCallback` handleDelete/handleEdit + `React.memo(MemorySection)` with a generic-preserving cast) — a search keystroke no longer re-sorts/reconciles the whole list. **(3)** removed 2 DEAD DB round-trips per memory-chat turn (`retrieve-memory-context.ts` — the memory_relationships + memory_cluster_items SELECTs only logged error, rows discarded). **(4)** parallelized the `(app)/layout.tsx` data waterfall (`Promise.all` of the 4 independent reads after the cheap active-profile cookie read; every degrade-to-default fallback preserved). **(5)** memoized the ToastProvider context value. **(6)** dev-gated the edge middleware request narration. Verified tsc/lint/build green + 6-lens self-review. **OPERATOR DB (recommended — verify then apply):** composite `memories(memory_profile_id,created_at,id)` + `(user_id,…)`; pgvector HNSW ANN on `embedding`; `pg_trgm` GIN on the search text columns; `profile_relationships(caregiver_account_id,memory_profile_id,invite_status)`. **LARGER (recommend):** a shared `cache()` `getSessionUser()` to dedupe the CARE-read duplicate `auth.getUser()`; a SECURITY-DEFINER RPC for the people-enrichment per-person aggregate; explicit-column selects excluding embedding (operator, real schema). See `docs/LA3-PERFORMANCE-REPORT.md`.
- ✔ **LA2 — Accessibility & Inclusive Design Readiness** (WCAG 2.2 AA pass; presentation/aria/semantic/contrast/client-confirm only — no logic/data/architecture change; dark mode still deferred; frozen reminder engine logic untouched [aria/copy only]; 7-dimension multi-agent audit → a11y score 68→~84). **Cleared 4 Level-A blockers:** keyboard-reachable media upload (`AttachmentManager` `hidden`→`sr-only`, 2.1.1); announced toasts (`ToastProvider` `role=status`/`aria-live`, 4.1.3); programmatic labels on the primary forms (`CreateProfileForm`/`InviteCaregiverForm`/`MemoryDateField`/reminders title+frequency, 3.3.2/4.1.2); memory-delete **confirmation** before the irreversible action (`memories/page.tsx` `handleDelete` → `window.confirm`, 3.3.4). **Plus AA:** skip-to-content link + `<main id>` (2.4.1); `CreateMemoryForm` focus rings on all 5 controls (2.4.7) + `gray-400`→`charcoal-soft`/`red-500`→`red-600` contrast (1.4.3); login/signup error `role=alert`+contrast; single `<main>` landmark (13 page-level `<main>`→`<div>`, 1.3.1); reduced-motion skeletons (4 files, 2.2.2) + `scroll-padding-top` (2.4.11); AppNavbar menu-trigger `aria-haspopup`/`aria-expanded`/focus-ring (4.1.2); PhotoViewer `role=dialog`+counter live region (4.1.2/4.1.3, no false `aria-modal`); BehavioralAnalyticsCard value `<h3>`→`<p>` heading semantics (1.3.1). Verified tsc/lint/build green + 6-lens self-review. **RECOMMENDED (larger, deferred — NOT blockers):** `text-charcoal-muted` sub-18px contrast sweep (~142 sites) + lint rule; modal focus-traps (PhotoViewer/ModalShell/DeleteAccountModal — the last is freeze-deferred); MemoryCard button-in-`<Link>` restructure; Undo/soft-delete; reminder-delete confirm; RemyAsk live-region consolidation; VoiceOver/NVDA smoke test. See `docs/LA2-ACCESSIBILITY-REPORT.md`.
- ✔ **LA1 — Memory-Care Clinical Readiness** (clinical/caregiving workflow polish; presentation-only, no schema/API/architecture change, reminder engine untouched; 9-persona clinical review → clinical 73/caregiver 76/patient 70). **Implemented (5 safe presentation fixes):** **(1)** DE-MEDICALIZED Insights — REMOVED the fabricated-sensor + diagnostic-named charts (Cognitive Score/Drift, Memory Continuity/Recall Drift, Attention, Sleep Recovery, Wearable Biometrics, Alzheimer Risk Signals) that synthesized pseudo-clinical trends from journal mood, and **deleted the 12 orphaned files** (7 charts + `lib/cognition/{continuityEngine,attentionEngine,sleepRecoveryEngine,wearableEngine,riskAnalysis}`); kept only honest real-data views (emotional tone, categories, reminder consistency) + the non-clinical narrative; de-alarmed the card's red "Routine Changes". **This RESOLVES the RC3-flagged "cognitive/Alzheimer insights disclose-or-remove decision" on the REMOVE side** — do NOT re-add. **(2)** reality-orientation date line on Home (`components/OrientationLine.tsx`, client-after-mount SSR-safe, aria-live). **(3)** honest My-Nest reminder copy (`DashboardFocus` no longer falsely says reminders are care-only). **(4)** caregiver access-level labels + least-privilege helper (`InviteCaregiverForm`; copy only). **(5)** legible medication-reminder placeholder + non-clinical hint (`reminders/page.tsx`; frozen form byte-unchanged). Verified tsc/lint/build green + main-loop 5-lens self-review. **RECOMMENDED (post-launch features, non-clinical):** ICE/emergency-contact card; "This is Me" care-continuity block; reminder completion attribution; who's-who family roster; care-team activity view; voice capture (deferred); memory-form reminiscence guidance. **REJECTED (bloat/liability):** medication-interaction/dosing checker, clinical shift-notes/vitals log, wandering-GPS/EHR/wearable ingestion, promoting Reminders to primary nav. See `docs/LA1-CLINICAL-READINESS-REPORT.md`.
- ✔ **RC5 — App Store & Production Release Certification** (final certification; no feature/behaviour change; reminder engine untouched). 8 independent lenses (Apple/Google/QA/Staff/SRE/Security/A11y/Privacy) + synthesis: **overall 94/100, 0 NEW code-level release blockers**; every RC2–RC4 hardening claim re-verified in code. **Verdict: ✅ CERTIFIED FOR APP STORE SUBMISSION + ✅ CERTIFIED FOR PRODUCTION RELEASE (iOS + web); Google Play deferred (decided — versionCode 1, no signing/FCM).** Two NEW low-risk findings were **fixed in the RC5 commit**: (1) the `(auth)` login/signup/forgot-password/reset-password inputs were placeholder-only → additive `aria-label`s (WCAG 1.3.1/3.3.2/4.1.2); (2) `lib/ai-memory.ts` `logAIStage`/`logAIError` routed through the dev-gated `logger` — the JSON-parse error logs its **name only** (its message can embed model output derived from health content) and the AI-request error uses `errorMessage()`. Verified tsc/lint/build green. Remaining = OPERATOR/infra only (Xcode manifest wiring + rebuild; backups + Storage-bucket backup + test restore; prod env/migrations) + the documented roadmap — NO code blockers. See `docs/RC5-RELEASE-CERTIFICATION.md`.
- ✔ **RC4 — Production Launch Readiness hardening** (production-hardening only; no feature/behaviour change; 8-dimension multi-agent audit scored 74/100 → ~85 after this pass; reminder engine untouched). **Security/observability:** closed a hot-path **PHI/PII log leak** (`lib/profile-access.ts` logged caregiver email + full care-recipient rows on every navigation; `build-people.ts` logged extracted person names) → dev-gated `logger`, IDs/counts only, `errorMessage()`; extended the Sentry `beforeSend` scrubber to redact **exception values + console breadcrumbs** (closes the PHI-to-Sentry breadcrumb path); message-only'd residual raw-`PostgrestError` logs (memory-enrichment/create/gdpr-export/register-device/send-notification/memories). **Reliability:** added `maxDuration` to 8 long/AI routes (memory-chat 60, remy/story 60, memories search/create 60, upload-url 30, gdpr export/delete 60, build-relationships 60) so a slow OpenAI call hits the graceful abort/degrade instead of a raw 504; 30s `AbortSignal.timeout` on the story provider call; memory-create removes orphaned storage on insert-failure. **Deployment/store:** `poweredByHeader:false`; committed `.env.example`; iOS `ITSAppUsesNonExemptEncryption=false`; **new `ios/App/App/PrivacyInfo.xcprivacy`** app privacy manifest (tracking=false; data types = nutrition labels; UserDefaults CA92.1). Verified tsc/lint/build green + main-loop 7-lens self-review (behaviour preserved, 0 blocking). **Recommendation: READY FOR APP STORE with operator steps** (wire the manifest into the Xcode target + rebuild; confirm backups + test restore); **Google Play deferred** (no FCM/google-services.json; allowBackup=true). **OPERATOR/ROADMAP (do NOT re-flag as new):** Storage-bucket (`memory-media`) backup strategy + a proven test restore (HIGH, operator/infra); `select('*')` embedding-column trim; Stripe per-event idempotency ledger; `auth_pending` retry cron; `pg_trgm` search index; central env validation + `requestId`→Sentry; Android Play-readiness. See `docs/RC4-PRODUCTION-READINESS-REPORT.md`.
- ✔ **RC3 — GDPR & Privacy Compliance hardening** (compliance-only; no feature/behaviour change; 10-dimension multi-agent source audit scored 70/100 → ~80 after this pass). **Code:** GDPR export widened (`collect-user-data.ts` now also exports `people`/`ai_usage`/`memory_intelligence`/`storage_ledger`, owner-scoped, additive, schemaVersion 1.1; operator-gated tables degrade to `[]`); Sentry PII scrubbing (`lib/observability/sentry-privacy.ts` `sendDefaultPii:false` + non-dropping `beforeSend`, wired to all 3 configs); PII log minimisation (`logger.errorMessage()`; raw error objects → message-only across dashboard/actions + active-profile/profile/timeline/build-relationships/memory-chat/onboarding; removed a client plan-log; dev-gated AI-chat narration); transparency copy (privacy page deletion-is-live + Art 22/no-training; cookies per-cookie table; CreateProfileForm non-blocking authority note). **Docs:** privacy-policy §13 subprocessor table corrected (GA/PostHog/Resend removed — none in code; OneSignal content added); deletion policy discloses processor-side erasure gaps; export report refreshed; **6 new `docs/compliance/13–18`** (ROPA, Subprocessor List, Retention, Incident Response, Responsible Disclosure, DPIA — DRAFT, `[CONFIRM]`-marked) + `RC3-GDPR-AUDIT-REPORT.md`; data-classification/retention/erasure comments across deletion/storage/media/AI files + 2 migrations. Verified tsc/lint/build green + main-loop 5-lens self-review (behaviour preserved, 0 blocking; the multi-agent review workflow hit the subagent session limit and was completed inline). **ROADMAP (NOT done — behaviour/feature/legal, do NOT re-flag as new):** Stripe/OneSignal processor-side erasure on delete; per-memory/edit storage orphan removal + orphan-sweep cron; auth_pending retry cron; reminder_local_confirmations deletion enrolment; ai_usage TTL; care-profile/DOB rectification endpoint; AI opt-out toggle; export media binaries; live-legal-page↔docs reconciliation + legal entity/DPO/jurisdiction + DPA + OpenAI ZDR; security.txt; cognitive-insights disclose-or-remove decision.
- ✔ **RC2 follow-up — memory-EDIT kept-attachment storage-quota bypass FIXED** (surgical security fix, closes the RC2 deferred HIGH). The ledger trigger projects each attachment's `->>'size'` from `memories.attachments`, so `PUT /api/memories/[id]` trusting the CLIENT-reported `size` of KEPT attachments let a client under-count the ledger + bypass the storage quota. Fix: BOTH edit branches (active JSON direct-to-storage + dormant multipart rollback) now re-derive every kept attachment's `size` from AUTHORITATIVE storage metadata via `getStorageObjectInfo(supabase, a.storagePath ?? a.url)` (the SAME helper the new-attachment path uses) BEFORE persist / `buildMemoryMediaPayload`; only `size` is corrected (`{...a, size}` when `exists && size!=null`, else unchanged — no data loss), order preserved (`Promise.all`). The multipart fix is in the ROUTE (the shared `normalizeAttachments`/`handleMemoryMediaUpload` pipeline is byte-unchanged); url/name/type/mimeType/path/schema/cover/API-shape/ownership-guard unchanged; the pre-existing final `isOwnedStoragePath` guard 400-rejects foreign kept paths. Verified tsc/lint/build green + focused adversarial review (quota-bypass / client-trust / spoofing / TOCTOU / mixed-edits → HIGH FULLY ELIMINATED). Residuals are PRE-EXISTING + orthogonal (orphan-object gap [needs orphan-sweep cron], non-forceable `size=null` fallback, coarse create-shared TOCTOU, ledger `DISTINCT ON` duplicate-`id` dedup) — do NOT re-flag as new.

**AI (all live on OpenAI — NOT deferred)**
- ✔ Ask Remy (conversational, gpt-4o-mini, retrieval-grounded) · Semantic search (premium-gated) · Embeddings (text-embedding-3-small) · Vector search (`match_memories`, 6 consumers) · AI summaries + tagging (gpt-4.1-mini) · Hybrid AI memory retrieval · Multi-turn conversational memory
- ✔ Deterministic AI-intelligence layer (insights/understanding/family/collections/connections/story/biography — no LLM)

**Memory engine**
- ✔ Memories CRUD · attachments jsonb + cover · **multi-photo** · **video (direct-to-storage)** · thumbnail size ladder (env-gated) · historical memory dates (precision) · people · relationships · clusters · deferred AI enrichment (`/api/memories/[id]/enrich`)

**Reminders (FROZEN / production-stable)**
- ✔ Create · complete (per-occurrence recurring advance) · delete · recurrence engine · native iOS local notifications · cron push + processing lease · OneSignal (SDK 5.5.2 + APNs + foreground banner + identity bridge) · caregiver + My-Nest reminders

**Caregiver system**
- ✔ Role/ownership core (`userCanAccessProfile`/`userCanWriteProfile`/`userOwnsProfile`) · **access_level ENFORCED on writes** (commit f53694b, 5 sites) · profile create · invite/accept/decline · owner-only revoke + list · **downgrade auto-reconciliation** · workspace switching (cookie re-validated per request)

**Subscription (web-checkout model)**
- ✔ Stripe checkout · portal · at-period-end cancel · signature-verified webhook (500-retry-on-write-fail, idempotent) · downgrade entitlement reconciliation · read-time premium gating · **Apple 3.1.1 native purchase-gating on every CTA** · storage-limit upgrade modal · single-source `BILLING_PLANS`

**Storage**
- ✔ Storage ledger (trigger-maintained) · byte-based quota enforcement (`enforceUploadQuota` → HTTP 413) · direct-to-storage upload (owner-scoped server paths, real-size re-verify) · **memory-EDIT kept attachments re-derive authoritative size (`getStorageObjectInfo`) — no client-size quota bypass** · subscription→storage-tier chain wired (NOT a stub) · per-file cap removed (total-per-user model)

**Security**
- ✔ Protect-by-default auth (`middleware.ts`, no PROTECTED_ROUTES allowlist) + `(app)` layout gate · object-level ownership validation · GDPR export · GDPR delete (re-auth gate, resumable) · per-route auth consistency · cron/webhook protection (fail-closed) · input sanitization · service-role isolation

**Web funnel & deployment**
- ✔ `/pricing` · `/download` (store buttons wired, "Coming soon" fallback) · `/support` · `/account/subscription` · Vercel linked + prod cron · Sentry wired · buildable iOS Capacitor+CocoaPods project · PWA manifest + brand icons

**UX**
- ✔ Project Polaris — all 8 passes shipped (Dashboard/shell, Home+Memories, People+Insights, Search, Timeline, Library, Settings, Reminders). Presentation-only over frozen logic. **No passes remain.**

==================================================
## DO NOT REBUILD
==================================================

*Architecturally complete. **Never redesign or reimplement.***

- One Remy Platform only (public API `@/lib/remy`; one Event Bus, Brain, Emotion Engine, Policy Engine, Provider, Renderer, Asset Registry)
- Single `<Remy>` renderer · Behaviour layer (above Emotion) · Nest behaviour choreography
- Emotion Engine · Policy Engine · Event Bus
- Memory engine (insert-first + deferred enrichment) · Storage system (ledger + byte quota + direct upload) · Thumbnail signing ladder
- Reminder engine (native local + cron fallback + OneSignal) — FROZEN, bug-fix only, investigation-first
- Subscription/billing engine (web-checkout + webhook + reconciliation) · Caregiver architecture (roles + access_level)
- Authentication (protect-by-default) · Vector search (`match_memories` + app-layer ownership backstop)
- Brand system (Product sage/sand/gold vs Companion purple/gold) · Remy asset pipeline (single flat folder + registry)

==================================================
## CURRENT LAUNCH BLOCKERS
==================================================

*Real blockers only. No invented work.*

**ENGINEERING**
- ~~UGC report/block MECHANISM (Apple 1.2)~~ — **DONE in LA5.1** (report user + report content + block + leave, multi-agent-verified `satisfiesApple12=true`). **NO remaining engineering blocker for iOS on Apple 1.2.** Requires the operator to apply the moderation migration to activate (see OPERATOR below).

**PRODUCT**
- App Store submission package: reviewer demo account + sample data + review notes; ASC privacy labels; metadata; screenshots.
- Brand raster exports + App Store / Play screenshots (generator script exists — needs an operator run).
- Decide the Health/Sensitive data declaration for the ASC privacy label + (future) Play Data Safety, and align all surfaces. *(Landing JSON-LD `applicationCategory` was moved off `HealthApplication` → `LifestyleApplication` in LA5.)*

**LEGAL**
- `/terms` governing-law **jurisdiction** placeholder + company particulars (`app/terms/page.tsx:111`).

**OPERATOR**
- **Apply the moderation migration `20260712120000_moderation_foundation.sql` (LA5.1) BEFORE iOS submission** — the report/block mechanism degrades to "unavailable" until applied, and Apple 1.2 requires it live for App Review. Then run the deletion regression (report a user → delete that user → confirm auth deletion succeeds).
- Apply prod Supabase migrations (storage_ledger `20260623120000`; reminder lease `20260707120000` + confirmations `20260707130000`).
- Set Vercel env: Sentry DSN; `NEXT_PUBLIC_APP_STORE_URL` / `_PLAY_STORE_URL`; verify 6 Stripe LIVE price IDs + webhook secret.
- **Push the unpushed commits** (as of LA7: **14** — RC2-follow-up `6029af8` through the LA7 commit; `main` auto-deploys to production) + smoke-test registration→login→upload→quota→checkout→webhook→reminder-delivery. Verify prod env first with `node scripts/check-production-env.mjs`.
- Stand up real contact mailboxes (support@ / privacy@ / dpo@ / security@ — `lib/contact.ts` placeholders).
- **DR activation (LA6 — now with runbooks in `docs/runbooks/`):** confirm Supabase Pro daily backups + retention + run a **restore drill**; enact the **`memory-media` Storage backup + test restore** (HIGH residual); configure the **uptime monitor** (`/api/health`) + **Sentry alert rules**; keep an **encrypted offline secrets copy** + run `scripts/check-production-env.mjs` pre-deploy.
- *(Only if Android ships now)* FCM `google-services.json` + CAMERA/media perms + release keystore/signing.

==================================================
## POST-LAUNCH ROADMAP
==================================================

*Intentionally deferred — do NOT start pre-launch.*

- Voice engine (TTS) · Animated Remy (Rive/Lottie backend behind the AnimationController seam) · live emotional reactions
- Voice-recording memories · Audio & Document/PDF upload **UI** (backend already allowlists them — only the picker is missing) · Speech-to-text transcription
- Semantic Search V2 · advanced AI memory intelligence *(the Memory Intelligence Engine V2 capability + data layer is now BUILT — see VERIFIED COMPLETE; what remains is ACTIVATING it into a live retrieval/search path, a future approved phase)*
- Golden Feather · dedicated per-stage nest ARTWORK (evolution is wired to real counts; only the per-stage art remains) · animated Remy character (wings / blink / emerge via Rive/Lottie) · celebration character effects (feather/heart) · achievements celebration art · seasonal themes · accessory system · emotion-specific artwork
- Apple Watch · Widgets · CarPlay · physical companion
- Dark-theme UI rollout (mechanism-only today) · PITR (daily backups baseline) · staging env · schema-as-migrations · DST-aware cron recurrence · iOS Notification Service Extension (exactly-once delivery) · Android as a shipping product · npm audit remediation · orphan-object storage sweeper

==================================================
## LOCKED ARCHITECTURE DECISIONS
==================================================

- **Decision:** The Nest is NOT a Floating Action Button. **Status:** LOCKED. **Reason:** the interaction with Remy is the feature; the menu is only a consequence of the `greeting` behaviour (`presentsActions`).
- **Decision:** Behaviour sits ABOVE Emotion (behaviour → existing expression/emotion/cue). **Status:** LOCKED.
- **Decision:** One Remy Platform only. **Status:** LOCKED.
- **Decision:** Single Renderer only (`components/remy/Remy.tsx`); render Remy only via `<Remy state>`/`RemyStage` — never a hardcoded `<img>`. **Status:** LOCKED.
- **Decision:** Exactly one of each — public API, provider, asset registry, event bus, brain, policy. **Status:** LOCKED.
- **Decision:** Features publish semantic events; the platform decides feeling/expression/visibility. **Status:** LOCKED.
- **Decision:** Auth is protect-by-default (no PROTECTED_ROUTES allowlist). **Status:** LOCKED (B1 launch blocker).
- **Decision:** `memory-media` bucket is PRIVATE; PHI via signed URLs only. **Status:** LOCKED (B2).
- **Decision:** Every client-cookie workspace WRITE is app-layer-authorized (`userCanWriteProfile`) before the write; RLS alone is not sufficient. **Status:** LOCKED.
- **Decision:** Caregiver write permission is determined ONLY by `access_level` (only `read` restricts; owner always writes). **Status:** LOCKED.
- **Decision:** Stripe webhook returns non-2xx (500) on required DB-write failure; 200 for no-matching-row. **Status:** LOCKED.
- **Decision:** Single source of truth `subscription_plan → BILLING_PLANS.storageGB → quota`; storage bundled with tiers (FREE 1 / PREMIUM 25 / FAMILY 100 GB / ENTERPRISE ∞); enforcement is total-per-user, not per-file. **Status:** LOCKED.
- **Decision:** Media upload is direct-to-storage; API is JSON metadata-only. **Status:** LOCKED.
- **Decision:** Memory create is insert-first; AI enrichment is deferred (fire-and-forget). **Status:** LOCKED.
- **Decision:** iOS purchase UI is web-only (Apple 3.1.1); no native IAP/StoreKit; Restore Purchases not required in this model. **Status:** LOCKED.
- **Decision:** Reminder system is FROZEN (bug-fix only, must begin with a proven defect); native = CocoaPods (not SPM); do not regenerate the iOS project. **Status:** LOCKED.
- **Decision:** "My Nest" is a workspace STATE, not a page/route (lives in profile dropdown; care-switching in WorkspaceSelector drawer). **Status:** LOCKED.
- **Decision:** Any fixed/full-screen overlay under a `backdrop-filter`/`transform`/`filter` ancestor MUST `createPortal(document.body)`. **Status:** LOCKED.
- **Decision:** Responsive nav swap is gated at `lg` (1024px), not `md`. **Status:** LOCKED.
- **Decision:** All AI is OpenAI (gpt-4o-mini / gpt-4.1-mini / text-embedding-3-small); the AI-intelligence layer is SHIPPED (only the companion's animation/voice content is deferred). **Status:** LOCKED.
- **Decision:** Time-of-day is a platform layer (`lib/remy/core/time-of-day.ts`); the Nest's ambient lighting, resting look (night→sleeping), and greeting derive from it. **Status:** LOCKED.
- **Decision:** Nest interaction MOTION uses framer-motion, centralized in `components/remy/motion/primitives.tsx` (no duplicated animation logic); cheap infinite AMBIENT loops (glow / motes / breathing) stay in CSS. **Status:** LOCKED.
- **Decision:** Nest evolution = 6 stages (Tiny→Cozy→Family→Golden→Memory Tree→Sanctuary) driven by REAL memory counts threaded from the app shell; dedicated per-stage nest ARTWORK is a future registry-only drop. **Status:** LOCKED.
- **Decision:** App-wide companion presence = three surfaces mounted ONCE in the shell (RemyScreenAwareness, RemyCelebration, RemyMilestones), driven by the ONE event bus + single `<Remy>` renderer; each renders null until Remy reacts. **Status:** LOCKED.
- **Decision:** Milestone celebrations derive from REAL memory counts (persisted last-count; no retroactive celebration on first load). Companion effects (feather/sparkle/heart) are centralized in `components/remy/effects/RemyEffects.tsx`; the feather uses the real `goldenFeather` asset. **Status:** LOCKED.
- **Decision:** The event bus's initial-mount replay buffer belongs to the Brain (the first `{replay:true}` subscriber); any SECONDARY bus listener MUST subscribe with `{ replay: false }`. **Status:** LOCKED.
- **Decision:** Companion Intelligence = two PURE core engines (`insights-engine` → observations, `priority-engine` → at most one), a once-per-app-open surface (`RemyMoments`), a read-only snapshot loader, and behavioural memory in the persistence layer. It is deterministic + rule-based — **NOT AI chat, NOT notifications, NOT a poll/background job**; max one proactive moment at a time, cooldown-gated. **Status:** LOCKED. **Do NOT** put clock/DB access in the engines, poll the snapshot, or fabricate observations for absent data.
- **Decision:** Living Relationship System = six PURE engines (relationship / story / anniversary / favourite / legacy / legacy-export, `lib/remy/core/*`) + a once-per-app-open surface (`RemyRelationship`) + a read-only snapshot loader + relationship memory in persistence. Deterministic + rule-based over REAL data (memories/people/dates) — **NOT AI/GPT, NOT chat, NOT notifications, NOT a poll**. Chapters are INFERRED (decade + dominant category), never hardcoded; anniversaries use only real day-precision dates. **Status:** LOCKED. **Do NOT** put clock/DB in the engines, hardcode chapter names, fabricate dates/people, or exceed one moment.
- **Decision:** Only ONE proactive moment chip shows at a time GLOBALLY — the shared `moment-gate` mutex + the single `RemyMomentChip`; `selectMoment` is the single generic selector (`<T extends RankableMoment>`) for both moment surfaces. **Status:** LOCKED (no duplicate chip/selector).
- **Decision:** Emotional Intelligence = three PURE engines (`significance-engine` ranks by SIGNIFICANCE not recency, `emotional-engine` → `EmotionalProfile`, `personality-engine` → behavioural traits). The 0–100 scores are INTERNAL — **Remy never exposes raw scores, only behaviours/observations/traits**. **Status:** LOCKED. **Do NOT** render a raw score, rank memories by recency/creation-order, put clock/DB in the engines, or fabricate significance from absent data.
- **Decision:** Memory Understanding = a PURE engine (`memory-understanding-engine`) that turns each real memory into a structured `MemoryUnderstanding` (deterministic, real-data-only, no prose/GPT/fabrication). It is INTERNAL — never shown in the UI; it feeds current ratios + future engines. Life stage / themes are inferred from real `ai_category` keywords ("unknown"/"other" fallback — NOT age-mapped from a nonexistent birth date). **Status:** LOCKED. **Do NOT** surface the understanding in the UI, generate prose, put clock/DB in it, or fabricate a life stage/theme from absent data.
- **Decision:** Memory Graph = a PURE engine (`memory-graph-engine`) that builds a deterministic semantic graph (nodes/edges/clusters) of how memories connect, from REAL shared attributes only (no GPT, no fabricated links); edges are pruned + deterministically capped; clusters are theme-based. INTERNAL (not shown); its edge-degree connectivity feeds the significance engine (clean optional context extension). **Status:** LOCKED. **Do NOT** surface the graph in the UI, fabricate/guess links, put clock/DB/randomness in it, or make edges non-deterministic.
- **Decision:** Journey Engine = a PURE engine (`journey-engine`) that turns the understanding + graph layers into complete LIFE JOURNEYS (`JourneyAnalysis`: journeys/connections/summary) — connected memories representing one continuous life period. Journeys EMERGE from real signals (theme + life stage + shared people + chronological continuity + graph connectivity); unrelated memories are never merged, missing years/transitions/links are never fabricated, and an unconfident group (below `MIN_JOURNEY_SIZE`, or split by a large real gap) is left separate. Deterministic (fixed weights, stable sorts, no clock/randomness). INTERNAL (not shown); its per-memory journey significance feeds the significance engine (clean optional context extension). REQUIRED inputs = understanding + graph (its pipeline-position outputs); chapters/significant/emotional/favourites are OPTIONAL refinements. **Status:** LOCKED. **Do NOT** surface journeys in the UI, fabricate/guess journeys/years/transitions/links, put clock/DB/randomness in it, reorder the downstream pipeline, or make output non-deterministic.
- **Decision:** Life Story Engine = a PURE engine (`life-story-engine`) that assembles the canonical CHRONOLOGICAL life story from real journeys only (`LifeStoryAnalysis`: story/chapters/timeline/milestones/summary) — the source for future AI conversation / biography / timeline UI / story-book export / reasoning. Chapters = runs of chronologically-continuous, CONNECTED journeys (join only when years are continuous [dated gap `> MAX_HARD_GAP` always splits], life stages compatible, AND a real relational signal supports it); disconnected journeys are never merged, chapters/years/events are never invented, and the timeline/milestones/summary/titles reference only EXISTING journeys/years/memories (no prose). Deterministic (stable sorts, no clock/randomness). INTERNAL (not shown); its per-memory life-story centrality feeds the significance engine (clean optional context extension). REQUIRED input = `JourneyAnalysis`; graph/understandings/chapters/significant are OPTIONAL refinements. **Status:** LOCKED. **Do NOT** surface the life story in the UI, fabricate/guess chapters/years/events, merge disconnected journeys, generate prose/narration, put clock/DB/randomness in it, reorder the downstream pipeline, or make output non-deterministic.
- **Decision:** Reasoning Engine = a PURE engine (`reasoning-engine`) that REASONS over the real journey/life-story/graph/understanding layers to derive Remy's structural understanding OF a life (`ReasoningAnalysis`: anchors/themes/influences/relationshipStrengths/gaps/summary) — the foundation for future reasoning/conversation/biography surfaces. **Life Anchors** = dominant structural pillars (a theme with `≥ MIN_ANCHOR_MEMORIES`; `"other"` never anchors); **Life Themes** = real theme distribution; **Life Influences** = people ranked by real memory/journey/graph signal; **Relationship Strengths** = structured counts only (no emotional reading); **Memory Gaps** = FACTUAL only (large year gaps, sparse/missing/weakly-documented life stages) and NEVER a guess at WHY. All scores structured numbers (no prose); no GPT, no fabricated anchors/people/dates/chronology, no inference beyond the real data. Deterministic (stable sorts, no clock/randomness). INTERNAL (not shown); its per-memory anchor strength feeds the significance engine (clean optional context extension). REQUIRED inputs = journey + life-story + graph + understanding; emotional/personality/relationship/favourite/significant are OPTIONAL refinements. **Status:** LOCKED. **Do NOT** surface reasoning in the UI, invent anchors/people/dates/chronology, add a "why" to a Memory Gap, generate prose/narration, put clock/DB/randomness in it, reorder the downstream pipeline, or make output non-deterministic.
- **Decision:** Biography Engine = a PURE engine (`biography-engine`) that assembles a STRUCTURED (non-prose) representation of a life from the real journey/life-story/reasoning/graph/understanding layers (`BiographyAnalysis`: sections/periods/references/coverage/summary) — the foundation for a future biography/story-book renderer. Sections mirror real life-story chapters 1:1 (title reuses the real chapter title; carries breadth `coverage` + real-signal `confidence`); periods group sections chronologically by life stage using only real memory years (0 when undated); references point ONLY at real journey/chapter/anchor/theme/person/memory ids (bounded); coverage/summary are structured metrics. **No paragraphs, no narration, no fabricated memories/people/dates/chronology.** Deterministic (chapters-order + fixed stage order; structural ids; no clock/randomness). INTERNAL (not shown); its per-memory section coverage feeds the significance engine (clean optional context extension). REQUIRED inputs = journey + life-story + reasoning + graph + understanding; favourite/emotional/relationship/significant are OPTIONAL refinements. **Status:** LOCKED. **Do NOT** surface the biography in the UI, generate prose/paragraphs/narration, fabricate memories/people/dates/chronology, reference a non-real entity, put clock/DB/randomness in it, reorder the downstream pipeline, or make output non-deterministic.
- **Decision:** Conversation Foundation Engine = a PURE engine (`conversation-foundation-engine`) that builds the deterministic groundwork a FUTURE conversational layer will consume (`ConversationFoundation`: topics/threads/references/context/summary) — **explicitly NOT chat, NOT GPT, NOT an LLM, NOT prompts, NOT generated text.** Topics = REAL recurring subjects (anchor/theme/person/life-stage, each `≥ MIN_TOPIC_MEMORIES`; `"other"` excluded, anchor-themes not duplicated); threads = a topic's memories grouped by the real biography chapter (`≥ MIN_THREAD_MEMORIES`); references point ONLY at real memory/journey/chapter/anchor/theme/person ids (bounded); context/summary are structured metrics. **No invented topics/threads/memories/people/dates, no narration/prompts/generated sentences.** Deterministic (sorted topics/threads + fixed stage order + structural ids; no clock/randomness). INTERNAL (not shown); its per-memory topic weight feeds the significance engine (clean optional context extension). REQUIRED inputs = journey + life-story + reasoning + biography + graph + understanding; favourite/relationship/significant/emotional are OPTIONAL refinements. **Status:** LOCKED. **Do NOT** build chat/GPT/LLM/prompts/generated text on this, surface it in the UI, invent topics/threads/memories/people/dates, reference a non-real entity, put clock/DB/randomness in it, reorder the downstream pipeline, or make output non-deterministic.
- **Decision:** Question Understanding Engine = a PURE engine (`question-understanding-engine`) that builds the deterministic RETRIEVAL-INTENT layer a FUTURE conversational layer will use to convert a PARSED question into structured intent (`QuestionUnderstanding`: intents/focus/constraints/references/context/summary) — **explicitly NOT chat, NOT GPT, NOT an LLM, and it takes NO free-text question / does NO natural-language parsing.** Intents = the answerable retrieval intents (13 kinds), each from a REAL upstream entity with structured focus/weight/confidence; **the `place` kind is NEVER produced (no location data — a supported kind with no real backing yields ZERO intents, never a fabricated one).** Focus/constraints/references are structured real ids only; context/summary are structured metrics. **No natural language, no generated text, no invented ids.** Deterministic (sorted foci + sorted+capped intents + structural ids; no clock/randomness). INTERNAL (not shown); its per-memory intent weight feeds the significance engine (clean optional context extension). REQUIRED inputs = conversation-foundation + biography + reasoning + life-story + journey + graph + understanding; favourite/relationship/significant/emotional are OPTIONAL refinements. **Status:** LOCKED. **Do NOT** build chat/GPT/LLM/free-text parsing/generated responses on this, produce a fabricated `place` (or any no-backing) intent, surface it in the UI, invent ids, reference a non-real entity, put clock/DB/randomness in it, reorder the downstream pipeline, or make output non-deterministic.
- **Decision:** Answer Planning Engine = a PURE engine (`answer-planning-engine`) that builds the deterministic EXECUTION PLAN a FUTURE conversational layer will run AFTER Question Understanding (`AnswerPlan`: steps/sources/context/coverage/summary) — **explicitly NOT chat, NOT GPT, NOT an LLM, and it produces NO answers.** Steps = an ordered list of structured retrieval steps, each EXECUTES a real question intent (fixed `STEP_KIND_OF_INTENT` map; `place` intent → no step; `reference` step kind reserved/never produced) pointing only at real ids; sources = the real entity pool the steps draw from (memory sources ranked by real graph connectivity, capped; + each memory's real biography chapter, milestones, optional significant/favourite); coverage/context/summary are structured metrics. **No prose, no generated answers, no invented ids.** Deterministic (steps from ordered intents + fixed map + sort/slice; graph-ranked sources; structural ids; no clock/randomness). ALL required inputs genuinely consumed. INTERNAL (not shown); its per-memory step weight feeds the significance engine (clean optional context extension). REQUIRED inputs = question-understanding + conversation-foundation + biography + reasoning + life-story + journey + graph + understanding; favourite/relationship/significant/emotional are OPTIONAL refinements. **Status:** LOCKED. **Do NOT** build chat/GPT/LLM/generated answers on this, surface it in the UI, invent ids/steps, reference a non-real entity, put clock/DB/randomness in it, reorder the downstream pipeline, or make output non-deterministic.
- **Decision:** Answer Assembly Engine = a PURE engine (`answer-assembly-engine`, **the FINAL deterministic intelligence layer**) that assembles ONLY the structured, FACTUAL answer package a FUTURE conversational layer will VERBALIZE (`AnswerAssembly`: sections/chronology/evidence/real-entity-lists/coverage/context/summary) — **explicitly NOT chat, NOT GPT, NOT an LLM, and it generates NO answers.** Sections = the answer-plan steps as structured sections (fixed `SECTION_KIND_OF_STEP` map); chronology = deterministic order from real life-story chapters (ids/order/confidence only); evidence/references = real supporting entities aggregated/deduped/ranked/bounded (+ each memory's real biography chapter, memories graph-ranked); coverage/context/summary are structured metrics. **No prose, no narration, no generated answers, no invented ids; zero output when data is absent.** Deterministic (ordered steps + fixed map + sort/slice; evidence Map drained by sort; structural ids; no clock/randomness). ALL required inputs genuinely consumed. INTERNAL (not shown); its per-memory section weight feeds the significance engine (clean optional context extension, `Math.min(2, x/50)`). REQUIRED inputs = answer-plan + question-understanding + conversation-foundation + biography + reasoning + life-story + journey + graph + understanding; favourite/significant/relationship/emotional are OPTIONAL refinements. **FIXED pipeline order:** snapshot → memory-understanding → memory-graph → journey → life-story → reasoning → biography → conversation-foundation → question-understanding → answer-planning → answer-assembly → story → favourite → anniversary → significance → emotional → personality → relationship → priority → one renderer. **Status:** LOCKED. This is the LAST deterministic intelligence layer — the next Remy layer would be the conversational/LLM RENDERING layer (a separate approved phase that CONSUMES this package). **Do NOT** build chat/GPT/LLM/generated answers on this, surface it in the UI, invent ids/sections, reference a non-real entity, put clock/DB/randomness in it, reorder the downstream pipeline, or make output non-deterministic.
- **Decision:** Conversation Rendering Engine = a PURE engine (`conversation-rendering-engine`, **the FIRST PRESENTATION-LAYER engine**) that consumes ONLY the `AnswerAssembly` (+ optional deterministic tone/verbosity/perspective controls) and prepares deterministic RENDER INSTRUCTIONS (`ConversationRender`: sections/metadata/summary/context) for a FUTURE conversational/LLM layer. It adds NO intelligence: NO retrieval / search / ranking / reasoning / inference / comparison / evidence-evaluation / chronology-building / fact-generation, and it generates NO natural language. Sections = top-`maxSections` assembly sections as render instructions (structural `render-<sectionId>` id + style hint + importance + the section's own real evidence ids); metadata `preferredOpening`/`preferredClosing` are structural section-id POINTERS (never text); everything is a structured id/enum/number. **This engine deliberately does NOT feed the significance pipeline** (presentation prep, not a memory signal) — there is NO `significance-engine` change; in `RemyRelationship` it is computed then `void`-ed (consumer = the future conversational layer). Deterministic (assembly already-sorted → slice/map; Set drained by sort; structural ids; no clock/randomness). No prior deterministic engine changed. INTERNAL (not shown). REQUIRED input = `AnswerAssembly` ONLY; tone/verbosity/perspective are OPTIONAL controls. **FIXED pipeline order:** … → answer-assembly → conversation-rendering → story → favourite → … → priority → one renderer. **Status:** LOCKED. This is the FIRST presentation-layer engine (prepares rendering instructions only). **Do NOT** make it retrieve/rank/reason/build-chronology/generate language, feed it into significance/ranking, surface it in the UI, invent ids/sections, reference a non-real entity, put clock/DB/randomness in it, or make output non-deterministic. The next Remy layer would be the actual conversational/LLM layer that consumes this render — a separate approved phase.
- **Decision:** Conversation Composer Engine = a PURE engine (`conversation-composer-engine`, **the FIRST natural-language-PLANNING layer**) that consumes ONLY the `ConversationRender` + the `AnswerAssembly` it renders (+ optional deterministic style/audience/intent controls) and prepares a deterministic COMPOSITION PLAN (`ConversationComposition`: sections/paragraphs/sentencePlans/referencePlans/flow/metadata/context/summary) for a FUTURE LLM/API provider. It generates NO language (no sentences/paragraphs/prose/prompts/LLM calls) and performs NO retrieval / search / ranking / reasoning / chronology / significance / evidence-scoring / factual decisions. Sentence plans are structural ROLES (opening/topic/evidence/transition/closing) — never text; section roles + flow + metadata are structural pointers/enums; reference plans point at real ids (kind from a `kindMapOf` join over the real answer section — not retrieval). Deterministic (iterates render sections in renderOrder; structural ids; Maps/Sets query-only; no clock/randomness). **This presentation-side engine feeds NOTHING** — NO `significance-engine` change, NO prior deterministic engine changed; in `RemyRelationship` it is computed then `void`-ed (consumer = the future LLM/API layer). INTERNAL (not shown). REQUIRED inputs = `ConversationRender` + `AnswerAssembly` ONLY; style/audience/intent are OPTIONAL controls. **FIXED pipeline order:** … → conversation-rendering → conversation-composer → story → favourite → … → priority → one renderer. **Status:** LOCKED. **Do NOT** make it retrieve/rank/reason/build-chronology/generate language, feed it into significance/ranking, import any other engine, surface it in the UI, invent ids, reference a non-real entity, put clock/DB/randomness in it, or make output non-deterministic. The next Remy layer would be the actual conversational/LLM PROVIDER that verbalizes this composition — a separate approved phase.
- **Decision:** Conversation Verbalizer Engine = a PURE, deterministic, SYNCHRONOUS engine (`conversation-verbalizer-engine`, **the FIRST provider-boundary layer**) that consumes ONLY the `ConversationComposition` (+ the `ConversationRender`/`AnswerAssembly` it composes) and assembles the deterministic PROVIDER REQUEST (`ConversationOutput`: text/citations/metadata/tokens/generation) a FUTURE provider adapter (OpenAI/Anthropic/…) would send — the strict prompt with the mandatory 7-clause PROMPT CONTRACT, citations back to real ids, and provider/generation/token metadata. **The actual LLM verbalization is DEFERRED** (consistent with the launch-only priority + AI-connection deferral): the engine makes **NO network/LLM call**, `text=""`, `metadata.verbalized=false`, `generation.status="deferred"`. **Provider boundary:** a real provider ADAPTER (the ONLY place a `fetch`/network call may live) is NOT built — it is a future phase that fills `text`. **Deterministic vs non-deterministic boundary:** all inputs + this output are deterministic; the ONLY non-determinism (a future LLM's WORDING) is not in this pure output; the LLM may choose wording but NOT content (prompt-contract-enforced). It does NO intelligence (no retrieve/rank/infer/compare/chronology/significance/plan-building/reference-invention — every refId resolves from an EXISTING referencePlan). **This provider-side engine feeds NOTHING** — NO `significance-engine` change, NO prior deterministic engine changed; in `RemyRelationship` it is computed then `void`-ed. INTERNAL (not shown). REQUIRED inputs = `ConversationComposition` + `ConversationRender` + `AnswerAssembly`; provider/model/temperature are OPTIONAL controls. **FIXED pipeline order:** … → conversation-composer → conversation-verbalizer → story → favourite → … → priority → one renderer. **Status:** LOCKED. **Do NOT** make the pure engine call a real LLM/network/fetch (that belongs ONLY in a future provider adapter), do intelligence (retrieve/rank/reason/chronology/significance), weaken the prompt contract, feed it into significance/ranking, import any other engine, surface it in the UI, invent ids, reference a non-real entity, put clock/DB/randomness in it, or make the output non-deterministic. The next Remy layer would be the actual provider ADAPTER that verbalizes this request — a separate approved phase (the only place a real LLM/network call lives).
- **Decision:** Conversation Provider Interface = the provider ABSTRACTION layer (`lib/remy/providers/`) the future provider adapters implement — **PURE, NO implementation, NO network/SDK/async/fetch/wiring.** Four files: `conversation-provider.ts` (the sole `ConversationProviderAdapter` interface — `generateConversation(request: ConversationOutput): Promise<ConversationOutput>` + `configuration()`/`health()`; a provider may ONLY accept a `ConversationOutput` and return one; it MUST NOT retrieve/rank/reason/score/plan/render/compose); `provider-types.ts` (`ProviderName`/`ProviderCapability`/`ProviderConfiguration`/`ProviderHealth`/`ProviderResult`/`ProviderLimits`/`ProviderVersion`); `provider-errors.ts` (`ProviderErrorCode` + `ProviderError extends Error` + `notImplementedError`); `provider-registry.ts` (a deterministic registry — every `ProviderName` maps to a `DeferredProvider` stub whose `generateConversation` simply THROWS "Provider not implemented.", `implemented=false`, `health="unimplemented"`; + `getConversationProvider`/`listProviders`/`isProviderImplemented`). **Future adapters documented, NONE implemented:** OpenAI/Anthropic/Gemini/Azure OpenAI/Ollama/LM Studio/Custom Enterprise — each future adapter is the ONLY place a `fetch`/SDK/real LLM call may live. NO existing file changed; NO wiring/UI/significance change; intentionally NOT exported from `@/lib/remy` (internal infra, not a feature API). **Status:** LOCKED. **Do NOT** implement a real provider (network/SDK/fetch/async) in this abstraction, do intelligence in the provider layer, wire it into RemyRelationship/UI/significance, or make the registry non-deterministic. A real provider ADAPTER is a separate approved phase — and the ONLY place a real LLM/network call may EVER live.
- **Decision:** Conversation Request/Response model (`conversation-request-engine` + `family-types.ts`) = the dedicated, separated provider models. `ConversationRequest` = the canonical IMMUTABLE provider INPUT (`prompt`/`contract`/`citations`/`metadata`/`summary`; **NO generated `text`**). `ConversationResponse` = the provider OUTPUT foundation (`text`/`provider`/`model`/`usage`/`status`/`citations`/`metadata`; **NO `prompt`**; minimal — filled by a FUTURE provider). `buildConversationRequest({ conversationComposition, conversationRender, answerAssembly })` is a PURE, deterministic transform that produces a `ConversationRequest` ONLY (never a `ConversationResponse`); it does NO intelligence/retrieval/ranking/chronology/language-generation. **`ConversationOutput` (Phase 18) is RETAINED** for backwards compatibility until a future migration phase. This was an ADDITIVE refactor — NO provider/network/SDK/fetch/async, NO wiring/UI/index.ts export/significance change, NO existing engine modified (only `family-types.ts` additive + the new engine). **Status:** LOCKED. **Do NOT** remove `ConversationOutput` before the migration phase, add `text` to `ConversationRequest` or `prompt` to `ConversationResponse`, make the request engine call a provider / do intelligence / be non-deterministic, wire it into RemyRelationship/UI/significance, or export it from `@/lib/remy`. **Future migration:** a later approved phase points the provider interface (`generateConversation`) at `ConversationRequest` → `ConversationResponse` and then retires `ConversationOutput`.
- **Decision:** The conversation provider ABSTRACTION consumes `ConversationRequest` and returns `Promise<ConversationResponse>` (migrated Phase 21 from the legacy `ConversationOutput`). `ConversationProviderAdapter.generateConversation(request: ConversationRequest): Promise<ConversationResponse>`; the `DeferredProvider` stub still **synchronously throws** "Provider not implemented." (behaviour byte-preserved — NO real provider, NO network/SDK/async). `ConversationOutput` is **`@deprecated` but RETAINED** for backwards compatibility (verbalizer / `RemyRelationship` / `index.ts` / `ProviderResult.output` still use it) until a future removal phase. This was a PURE types-only, behaviour-preserving migration of exactly 3 files (`conversation-provider.ts`, `provider-registry.ts`, `family-types.ts`) — no other engine touched, no wiring/UI/significance change. **Status:** LOCKED. **Do NOT** make the deferred provider return a value instead of throwing (behaviour must stay preserved) or become async, implement a real provider here, re-add `ConversationOutput` as the provider input, or remove `ConversationOutput` before the removal phase. **Known follow-up (pre-existing, non-blocking):** reconcile `ConversationProvider` (family-types, 5 values) ↔ `ProviderName` (providers, 8 values) BEFORE a real adapter for gemini/azure-openai/ollama/lm-studio/custom-enterprise is built (else its `ConversationResponse.provider` can't be set from its `name`).
- **Decision:** OpenAI is the FIRST real production provider (`lib/remy/providers/openai-provider.ts`, registered for `"openai"`). `OpenAIProvider` implements `ConversationProviderAdapter` as a **PURE EXECUTION layer** — it sends `request.prompt.full` to OpenAI **exactly as supplied** (no rewrite/inject/reorder/enrich), passes `request.citations` through **unchanged**, and returns a `ConversationResponse` (`text`/`provider:"openai"`/`model`/`usage`/`status:"generated"`/`citations`/`metadata`); it does **NO intelligence**. The **official OpenAI SDK + network + env + async are ISOLATED** in that one file (the ONLY place a real LLM/network call lives; the SDK was already a dependency — no `package.json` change). **Construction is side-effect-free** (env + client are lazy inside `generateConversation`), so the registry stays deterministic. SDK/network failures are wrapped in `ProviderError` (429→rate-limited / ≥500→provider-failure / else invalid-request; missing key→invalid-request) and the raw exception is **never leaked**; **no retries/timeouts/fallbacks yet**. The adapter is **DORMANT** (nothing invokes it — server-side, env-gated on `OPENAI_API_KEY`), so the deterministic pipeline and app runtime are unchanged. The response's `provider` uses the `ConversationProvider` literal `"openai"` (∈ both enums) — no mapping layer. The `DeferredProvider` stays for every other name. **Status:** LOCKED. **Do NOT** invoke this provider from RemyRelationship/UI/significance (activation is a future phase), add intelligence/retrieval/ranking to the provider, rewrite/modify the prompt or citations, un-isolate the SDK (network/SDK stays ONLY in `openai-provider.ts`), read env / create the client at construction, add a new dependency, or leak SDK exceptions. **A second real adapter** (Anthropic/Gemini/…) implements the SAME interface and **must first reconcile `ConversationProvider` ↔ `ProviderName`** (still open).
- **Decision:** The Provider Registry (`lib/remy/providers/provider-registry.ts`) is the SINGLE AUTHORITATIVE conversation-provider resolver — `getConversationProvider(name)` is the one canonical by-name resolver, and `getProductionProvider()` (→ `getConversationProvider(PRODUCTION_PROVIDER)`, `PRODUCTION_PROVIDER = "openai"`, a fixed deterministic literal) is the one production seam. Both RESOLVE ONLY (return an adapter; never call `generateConversation` / open a network connection / read env / execute); the provider layer stays DORMANT (nothing invokes it — Phase 24 will). Registry construction is deterministic (no network/SDK/env/clock at load or construction). `"openai"` → real `OpenAIProvider`; every other name → `DeferredProvider` (throws `notImplementedError`). **Status:** LOCKED. **Do NOT** add a second/competing resolver or duplicate the registry, invoke a provider / call `generateConversation` from the registry / RemyRelationship / UI / routes / significance (activation is Phase 24), make `PRODUCTION_PROVIDER` env-derived or construction non-deterministic, or migrate `ProviderResult` / reconcile the provider-identifier enums here.
- **Decision:** The FIRST end-to-end conversation execution path is `executeConversation` (`lib/remy/providers/conversation-execution.ts`) = `buildConversationRequest(input)` → `getProductionProvider()` → `provider.generateConversation(request)` → `ConversationResponse`. It is provider-agnostic (resolves via the registry, never instantiates a provider, never imports OpenAI, never bypasses the registry), passes `request.prompt.full` to the provider EXACTLY as built (no rewrite/inject/enrich), does NO intelligence, and lives in the provider layer (network boundary; imports only the pure `buildConversationRequest` from core — providers→core one-way, no cycle; side-effect-free import). As of Phase 25 it is invoked by EXACTLY ONE caller — the opt-in `/remy/story` surface's server action `narrateStoryConversation` (`app/(app)/remy/story-action.ts`); it is NOT invoked from `RemyRelationship`/the app-open path or the live Ask Remy chat. **As of Phase 26 that single caller is the observability WRAPPER `executeConversationWithUsage` (`lib/remy/execute-conversation-with-usage.ts`), which the story action calls.** **Status:** LOCKED. **Do NOT** invoke it from `RemyRelationship`/the app-open path or the live chat, add a SECOND `executeConversation` caller or execution path, duplicate provider resolution, instantiate a provider directly / bypass the registry / import OpenAI outside the provider layer, move intelligence/retrieval/ranking/planning into it, rewrite/enrich `prompt.full`, let the client supply the prompt/inputs (keep it server-authoritative), or add retries/streaming/timeout/fallback/persistence here.
- **Decision:** AI usage/billing/observability is INSTRUMENTATION ONLY around the single execution path — `executeConversationWithUsage` (`lib/remy/execute-conversation-with-usage.ts`) wraps (never replaces/bypasses) `executeConversation`, records real usage/cost to `ai_usage`, and re-throws on failure. Cost lives in ONE isolated model-aware layer (`lib/ai/usage/cost.ts`); logging (`recordAiUsage`) + quota reads (`canExecuteConversation` etc., `lib/ai/usage/quota.ts`) NEVER throw or block execution and quotas are architecture-only (default unlimited → always allow). `ai_usage` writes are service-role, scoped by session-derived user_id; the `ai_usage_summary` aggregate is service-role-only. **Status:** LOCKED. **Do NOT** move persistence into `executeConversation` (Phase-24 LOCKED), add a second wrapper/execution path, make `recordAiUsage`/quota throw or block, enforce quotas without setting `AI_USAGE_LIMITS`, scatter cost rates outside `cost.ts`, or expose `ai_usage_summary` to non-service-role.
- **Decision:** AI subscriptions/quotas are subscription-aware but instrumentation-shaped — AI limits live in ONE config (`lib/ai/usage/entitlements.ts` `AI_PLAN_LIMITS`, resolved via `resolveSubscription`; Premium/Family/Enterprise unlimited, Free capped); the quota gate (`canExecuteConversation`) is structured + never-throws + premium-bypass; ENFORCEMENT lives in the story action as a PRE-check (before the pipeline build) returning a structured `quota_exceeded` — the wrapper no longer gates. Usage dashboard (`/settings/ai`) + `GET /api/remy/usage` are per-user + auth-gated; admin analytics (`lib/ai/usage/admin-analytics.ts`) is SERVER-ONLY with no public route; the `ai_usage_*` aggregates are service-role-only (no IDOR); provider/model are read via the registry seam (provider-independent); iOS shows no purchase link on native. **Status:** LOCKED. **Do NOT** scatter AI limits outside `entitlements.ts`, add a second `executeConversation`/wrapper caller or execution path, make the quota gate throw / block the UI / be client-controllable, expose admin analytics or the aggregates to non-service-role, put OpenAI-specific logic outside the provider layer, or show a purchase link on native.
- **Decision:** Memory Intelligence Engine V2 (`lib/remy/memory-intelligence/`) is an ADDITIVE capability + data layer — adaptive importance / relationship weighting / decay / reinforcement / classification / clustering / forgotten / combined ranking — with ONE central config (`config.ts`); pure deterministic engines (no clock/DB/random) + a service-role side table (`memory_intelligence`, never alters `memories`, RLS select-own + service-role writes, no IDOR, reversible). It is NOT wired into any live path (Ask Remy / story pipeline / execution path / semantic search all byte-unchanged; ONE execution path preserved). The existing `match_memories` + `retrieval.ts` blended ranker remain the retrieval foundation. **Status:** LOCKED. **Do NOT** wire V2 into a live retrieval/execution path without a separate approved phase, scatter weights outside `config.ts`, put a clock/DB/`Math.random` in a pure engine, alter the `memories` table (use the side table), make `store.ts` throw or do an unscoped/cross-user write, or classify a keyword-less memory into a protected (medical/emergency/health) class.
- **Decision:** UGC moderation (Apple 1.2) = a minimum report/block/leave framework, NOT a social platform. Two additive tables (`moderation_reports` + `user_blocks`); server actions are session-derived + structured/never-throw + service-role-scoped + authorized by `getSharedCarePeopleIds` (report/block only someone you share care with); RLS is **insert-own + select-own only** (no update/delete → status is service-role only; reporter identity never exposed; a blocked user can't discover who blocked them); block enforcement is at the invitation path (`inviteCaregiver` + `acceptInvite`, either direction) and NEVER removes existing care access (owner **revoke** / caregiver **leave** are the explicit removal paths). `moderation_reports` has **NO must-have-target CHECK** (FKs are reported/memory→SET NULL so records survive deletion without dangling PII; a target CHECK would abort account/memory deletion = Art 17 regression). **Status:** LOCKED. **Do NOT** add a must-have-target CHECK, expose reporter identity to the reported user, trust a client-supplied actor id, let a block auto-remove care access, or build social/feed/comment/messaging/admin-dashboard surfaces. The share-care boundary is app-layer (not RLS-encoded) — an accepted limitation matching the repo pattern.

==================================================
## KNOWN OPEN ITEMS
==================================================

**CRITICAL** *(blocks launch)*
- ~~UGC report/block + EULA abuse clause (Apple 1.2)~~ — **DONE** (EULA clause LA5; report/block/leave mechanism LA5.1, multi-agent-verified). Now an OPERATOR step only: apply the LA5.1 moderation migration before iOS submission.
- Operator go-live steps (migrations applied [incl. LA5.1 moderation], env set, commits pushed, mailboxes, legal jurisdiction, submission package, screenshots) — see LAUNCH BLOCKERS.

**IMPORTANT** *(soon after launch)*
- **HTTP security headers + API rate limiting — DONE in RC2** (no longer open). Follow-ups: nonce-based CSP (drop `unsafe-inline/eval`, needs Capacitor/realtime/Stripe testing); distributed rate-limit store (Upstash/Redis) for multi-instance guarantees (operator).
- **NEW (HIGH, RC2-identified, deferred):** memory-EDIT kept-attachment **storage-quota bypass** — `PUT /api/memories/[id]` trusts the client-reported `size` of KEPT attachments (the storage ledger under-counts). Fix: re-derive kept sizes from storage via `getStorageObjectInfo` (as create does). Deferred to a focused follow-up (touches the frozen media pipeline + attachment types).
- Ask Remy's semantic retrieval is NOT premium-gated, and the live Ask Remy chat + `/api/memory-chat` are **rate-limited (RC2) but NOT AI-quota-gated** (`canExecuteConversation` / `recordAiUsage`) — product decision: does Ask Remy count against the AI quota?
- Memory EDIT/DELETE authorization is `user_id`-scoped only (fails safe/restrictive) — does not apply the `userCanWriteProfile` access_level model like create. Decide whether caregiver-edit parity is intended.
- Fix `.env.local` local formatting bug (CRON_SECRET missing a trailing newline before TOMBSTONE_USER_ID) — local only.

**FUTURE** — see POST-LAUNCH ROADMAP.

**Documentation divergences** *(source code is authoritative)* — **reconciled in the 2026-07-09 sync pass:** HANDOFF was rebuilt as a lightweight continuation doc (the stale "memory-media public" + "11–14 unpushed commits" + obsolete-roadmap/blocker lines removed); CLAUDE.md's Pass-1 `RemyActionButton` reference now carries a SUPERSEDED marker pointing to the Nest note; and `docs/roadmap/launch-roadmap.md` carries a SUPERSEDED banner deferring to this file. Remaining, still true (not a contradiction):
- RLS applied-state (memories INSERT + relationship/cluster tables) is **dashboard-managed** → confirm in the Supabase SQL editor; not repo-verifiable.
- CLAUDE.md keeps older per-feature notes (Polaris Pass-1, "access_level open") in place as dated history, each explicitly **superseded** by a later authoritative note — layered supersession, not a live contradiction.

==================================================
## LAST COMPLETED TASK
==================================================

**2026-07-16 — Canonical icon + Nest/Ask-Remy V1 brand correction (launch-blocking visual fix):**
the App Store icon is now the **purple fingerprint-heart-bird** (composed at 1024² opaque from the
operator's exact standalone high-res mark `logo vector version.png` on the board-sampled deep-purple
gradient — never redrawn/upscaled; supersedes the WRONG 2026-07-15 sage nest-and-egg selection); the
Nest button idle state is the NEST itself (approved `nestEmpty` art, Remy pops out via the unchanged
wake choreography, same 6 real actions); the NestMenu sheet + Ask Remy chat carry the companion
violet/lavender palette with a large V1 composer (presentation-only; chat logic byte-unchanged).
Validated tsc/lint/build + iOS simulator build green. See `docs/handoffs/HANDOFF_CURRENT.md` and the
CLAUDE.md "Canonical iOS APP ICON" note. **2026-07-16 — Build 18 prepared for TestFlight:** `CURRENT_PROJECT_VERSION 12→18` (both configs;
incorporates the operator's working-tree 17 bump), marketing version 1.0; signed Release archive
verified at 1.0 (18) and staged at `~/Downloads/RemyNest-Build18.xcarchive`; upload = operator step.

*(The entry below is the older synchronized summary.)*

Summary: **RC2 — Security Hardening** (production security posture; no feature/UI/architecture change). Implemented the RC1 audit's code-level security items: **(1) HTTP security headers** (`next.config.js` on every response — CSP, HSTS 2y-preload, `X-Frame-Options DENY`, nosniff, Referrer-Policy, Permissions-Policy, COOP; CSP crafted Capacitor/Supabase-realtime/Stripe/OneSignal/Sentry-compatible since the iOS app loads the live site — permissive script/connect, strict `frame-ancestors none`/`object-src none`/`base-uri self`; nonce tightening is FUTURE). **(2) API rate limiting** (`lib/security/rate-limit.ts` — dependency-free in-memory sliding window, ONE `RATE_LIMITS` config, fails OPEN, `MAX_KEYS` evict) applied keyed by session user id to `upload-url`/`memory-chat`/`memories-create`/`enrich`/`stripe-checkout`/`stripe-portal`/`gdpr-export`/`gdpr-delete`/`story-action` (distributed store = operator upgrade). **(3) Logging hardening** (`lib/logger.ts` — debug/info DEV-ONLY, warn/error always → Sentry unaffected): removed the RAG **PHI leak** (`retrieve-memory-context` was logging memory content/context) + the checkout `USER EMAIL` log; gated webhook/checkout/create/insights narration. **(4) Dead code:** deleted 3 orphan routes (`/api/search`, `/api/create-reminder`, `/api/send-reminders`; kept `/api/search/global`; live cron `/api/cron/send-due-reminders`) + cleaned residual middleware/e2e refs. **(5) OWASP fixes** (MULTI-AGENT ASVS/API-Top-10 review — authorization otherwise CLEAN): `POST /api/active-profile` auth+`userCanAccessProfile`-gates before writing the CARE cookie; `inviteCaregiver` scoped escaped `.ilike` (no full-table fetch/no LIKE-wildcard); **middleware catch FAILS CLOSED**; `send-notification` stops leaking the raw DB error; `auth/logout` request-origin redirect (was hardcoded localhost); `/api/health` drops the commit SHA. Verified tsc/lint/build green + independent MULTI-AGENT adversarial review CLEAN (7 lenses, 0 confirmed blocking; all 6 non-blocking findings — enrich preset, ilike-escape, 3 stale e2e tests, residual middleware ref — fixed + re-validated). **Remaining:** HIGH — memory-EDIT kept-attachment storage-quota bypass (deferred, focused follow-up on the frozen media pipeline); product decision — Ask Remy/`memory-chat` AI quota gating. **Prior task (also on this branch): Memory Intelligence Engine V2** — an ADDITIVE "advanced AI memory intelligence" subsystem (`lib/remy/memory-intelligence/`) introducing adaptive importance scoring, relationship weighting, deterministic decay, reinforcement, cached classification, event clustering, forgotten detection, and a configurable combined ranking, while PRESERVING the current architecture (existing `match_memories` semantic search + `lib/remy/retrieval.ts` blended ranker remain the foundation). **ONE central config** (`config.ts`: `MEMORY_IMPORTANCE_WEIGHTS`, `RELATIONSHIP_WEIGHTS`, `DECAY_CONFIG`, `REINFORCEMENT_CONFIG`, `RANKING_WEIGHTS`, `FORGOTTEN_CONFIG`, taxonomy + cluster types — no scattered weights). Pure deterministic engines (importance/relationship/decay/reinforcement/classification/clustering/forgotten/ranking + `scoreMemoriesV2` orchestrator; no clock/DB/`Math.random` — caller passes `now`). Data layer: a service-role `store.ts` (batch reads = no N+1; user-scoped update-then-insert writes; lazy defaults; never throws) + migration `20260711140000_memory_intelligence.sql` — a NEW `memory_intelligence` side table (1:1 FK `memory_id`→memories on delete cascade; **never alters `memories`**; RLS select-own + service-role writes; `reinforce_memory`/`backfill_memory_intelligence` SECURITY DEFINER with execute revoked from public/authenticated → no IDOR; touch trigger; reversible). Decay protects pinned + medical/emergency/health (factor 1), slows milestone/childhood/family, and recall rewinds effective age; classification's medical/emergency bias applies ONLY on a real keyword hit (keyword-less → `miscellaneous`, never a protected class); the previously-dead `ai_importance` string is revived as a mapped signal. Reuses (not rebuilds) `match_memories`, enrichment, existing `memory_clusters`/`memory_relationships`, and `retrieval.ts`. **DORMANT/ADDITIVE:** nothing imports the subsystem yet — `executeConversation` + wrapper each still ONE caller (ONE execution path); the provider layer, Ask Remy (`ask-action.ts` + `retrieval.ts`), story pipeline, `relationship-intelligence.ts`, billing, quota, and usage dashboard are all byte-unchanged; no second execution path, no duplicate retrieval pipeline, no OpenAI import (provider-independent). Verified tsc/lint/build green + a 28-assertion runtime formula check + independent MULTI-AGENT adversarial review (7 lenses — algorithm-correctness / purity-determinism / single-config / additive-no-regression / security-scoping / performance / migration-datalayer): 1 blocking (classification bias promoted keyword-less memories to protected "medical") + 1 non-blocking (unscoped write could re-own a row) found → BOTH fixed and independently re-verified CLEAN. Operator step: apply `20260711140000_memory_intelligence.sql` (persistence degrades silently until then). Activation into a retrieval path is a FUTURE approved phase. **Prior task (also on this branch): AI Subscriptions, Quotas & Usage Dashboard** (production hardening — turns the Phase-26 dormant quota architecture into REAL, subscription-aware enforcement + a usage dashboard/API + server-only admin analytics; NO provider/prompt/architecture redesign; still ONE execution path). **Entitlements** = ONE config `lib/ai/usage/entitlements.ts` (`AI_PLAN_LIMITS` by `BillingPlan`: FREE 5/day + 50/month; PREMIUM/FAMILY/ENTERPRISE unlimited) + `resolveAiEntitlement` (on the existing `resolveSubscription`) + neutral `AI_UPGRADE_MESSAGE`. **Quota** `lib/ai/usage/quota.ts` (`canExecuteConversation(userId, entitlement)` → structured allowed/tier/reason/remaining/upgrade; premium bypass with NO DB read; Free enforced on SUCCESSFUL counts; never throws; pure `evaluateConversationGate`). **Enforcement lives in the story action as a PRE-check** (after the empty check, before the pipeline build) → structured `status:"quota_exceeded"` with NO provider call (action still never-throws, server-authoritative); the wrapper's old dormant gate was REMOVED (now pure exec+observability). **iOS anti-steering:** the story UI shows upgrade copy web-only (`useIsNativePlatform`) — no purchase link on native. **Dashboard/API/settings:** `lib/ai/usage/overview.ts` (`getAiUsageOverview`: today/month/cost/remaining + provider/model via the registry seam — provider-INDEPENDENT), `components/remy/AiUsageDashboard.tsx` (presentational), `/settings/ai` (display-only) + a `/settings` link, `GET /api/remy/usage` (auth-gated, per-user, mobile-ready). **Admin analytics** `lib/ai/usage/admin-analytics.ts` (SERVER-ONLY, no public route — global conversations/rates/latency/tokens/spend/most-used-model). **Migration** `20260711130000_ai_usage_analytics.sql` (service-role-only SECURITY DEFINER aggregates; execute revoked from public/authenticated → no IDOR). Provider-independence fix: the missing-key classifier no longer probes `OPENAI_API_KEY` (message-based) → no OpenAI-specific logic in the AI-usage layer. `executeConversation` + provider layer + engines + story-pipeline + Ask Remy + `RemyRelationship` + `cost.ts` + `package.json` byte-unchanged; ONE `executeConversation` caller (wrapper), ONE wrapper caller (action). Independent MULTI-AGENT adversarial review CLEAN (7 lenses — 0 confirmed blocking; provider-independence fix applied). INERT until the operator applies both `ai_usage` migrations. Known limitation (not a blocker): read-then-act soft quota (negligible with the serialized single-client UI). **Prior task (also on this branch): AI Usage, Billing & Observability** (production hardening — instrumentation ONLY; no redesign of Remy/providers/prompts/engines; no second execution path). Every real AI execution now records **user / workspace / provider / model / operation / REAL tokens / latency / estimated cost / status / error_code** to a new **`ai_usage`** table. Honouring the Phase-24 LOCKED "no persistence inside `executeConversation`", instrumentation lives in **`lib/remy/execute-conversation-with-usage.ts`** — the wrapper that is now `executeConversation`'s **SINGLE caller** (the story action calls the wrapper). **ONE execution path** (UI → story-action → wrapper → `executeConversation` → `getProductionProvider()` → `OpenAIProvider`); the wrapper is a decorator (reaches the provider ONLY via `executeConversation`), not a second path. New: `lib/ai/usage/cost.ts` (SINGLE isolated provider-independent model-aware price table + `estimateCostUsd`; unknown model → 0; dated-snapshot prefix fallback), `lib/ai/usage/ai-usage.ts` (`classifyAiError` taxonomy + `recordAiUsage` — service-role, scoped by explicit user_id, **NEVER throws AND never hangs** [swallow + 3 s timeout]), `lib/ai/usage/quota.ts` (`getUsageToday/getUsageThisMonth/getEstimatedMonthlyCost/canExecuteConversation` — ARCHITECTURE ONLY, `AI_USAGE_LIMITS` default null → always allow with no DB read; degrade to zeros), and the migration `supabase/migrations/20260711120000_ai_usage_foundation.sql` (table + indexes + RLS [select-own; inserts service-role only] + service-role-only `SECURITY DEFINER` `ai_usage_summary` aggregate — execute revoked from public/authenticated, no IDOR). Edit: `story-action.ts` threads `userId`+`workspaceId` and calls the wrapper (its never-throws / server-authoritative contract unchanged). Uses REAL provider values; cost is one isolated layer (no scattered costs). **INERT until the operator applies the migration** (writes/reads degrade silently → deploy is a no-op until then). `executeConversation` has EXACTLY ONE caller; the wrapper has exactly one caller; provider registry + deterministic engines + `RemyRelationship` + Ask Remy + `executeConversation` itself are byte-unchanged; no OpenAI import outside the provider layer; `package.json` unchanged. Independent MULTI-AGENT adversarial review CLEAN (7 lenses — execution-integrity / observability-correctness / never-breaks-execution / cost-correctness / security-scoping / quota-architecture / scope-and-regression — 0 confirmed blocking; 2 worthwhile fixes applied [bounded the awaited log write with a 3 s timeout; made the dormant gate per-user]). Operator step: apply the migration (+ `SUPABASE_SERVICE_ROLE_KEY`, already set) to activate logging. Known follow-ups (not blockers): no usage UI, quotas not enforced, no `ai_usage` retention/rollup. **Prior task (also on this branch): Live Conversation Integration** — the **FIRST user-facing AI execution** of the conversation platform. `executeConversation` (built dormant in Phase 24) is now invoked by a real user action via a NEW, isolated, **opt-in** surface: **`/remy/story` "Remy narrates your story."** **Investigation that shaped it (source-verified):** the deterministic companion pipeline is NOT a question flow — it is a life-story/significance analysis run in `RemyRelationship` on **app-open** (no question), and the real Ask Remy chat (`answerAskRemy`) is a **separate live AI layer** that never builds `ConversationComposition`/`Render`/`AnswerAssembly`; so wiring into `RemyRelationship` would fire a paid call every app-open (forbidden) and splicing into the live chat needs a forbidden redesign — hence the operator-chosen new opt-in surface. **4 new files + 1 tiny edit:** `lib/remy/story-pipeline.ts` (PURE orchestrator that SEQUENCES the EXISTING 12 engines in `RemyRelationship`'s exact order/inputs → `{ conversationComposition, conversationRender, answerAssembly }`; not a new engine/pipeline; deterministic; flat file to avoid a basename clash with `lib/remy/conversation.ts`), `app/(app)/remy/story-action.ts` (`"use server"` `narrateStoryConversation()` — auth-gated + workspace-scoped snapshot **mirroring `/api/remy/relationship-snapshot`** → orchestrator → **`executeConversation`** → **structured result that NEVER throws**; **server-authoritative** — client supplies nothing; 0 memories → `"empty"` with no provider call; failure/unconfigured `OPENAI_API_KEY` → `"unavailable"`), `components/remy/RemyStoryConversation.tsx` (client button renders `.text` + `AIDisclaimer` + states), `app/(app)/remy/story/page.tsx` (the page); **edit** `app/(app)/remy/page.tsx` (one discovery `<Link>` — no chat-logic change). **`executeConversation` now has EXACTLY ONE caller — no dormant seam remains.** Reuse-not-redesign: the live chat, `RemyRelationship`/app-open path, every deterministic engine, and the entire provider layer are **byte-unchanged**; `executeConversation`/`getProductionProvider()`/`OpenAIProvider` used exactly as built; no OpenAI import outside the provider layer; no prompt engineering / retrieval rewrite; `package.json` unchanged; citations/metadata preserved through the response (MVP UI renders `.text`). Requires a server `OPENAI_API_KEY` (operator env); iOS purchase-compliance unaffected. Independent MULTI-AGENT adversarial review CLEAN (7 lenses — 0 confirmed blocking; 3 refuted-but-worthwhile robustness fixes [flat-file rename to kill a basename collision; harden `narrateStoryConversation`/`loadStorySnapshot` to truly never-throw; degraded read → `"unavailable"` not `"empty"`] applied + independently re-verified SOUND). Known follow-ups (not blockers): no per-question quota/entitlement gate or rate-limit on the story surface's provider call yet. **Prior task (also on this branch): Production Provider Activation** — the **FIRST end-to-end conversation EXECUTION path** for the Remy Conversation Platform (activates Phases 18–23; NOT a redesign / prompt-engineering / retrieval). The ONLY change is **one new file** `lib/remy/providers/conversation-execution.ts` exporting the single async **`executeConversation(input): Promise<ConversationResponse>`** (input = the request engine's `ConversationRequestInput` = `{ conversationComposition, conversationRender, answerAssembly }`), composing in order: **`buildConversationRequest(input)`** (pure Phase-20 request engine → immutable `ConversationRequest`, incl. `prompt.full`) → **`getProductionProvider()`** (Phase-23 registry seam → `OpenAIProvider`) → **`provider.generateConversation(request)`** → `ConversationResponse`. The provider receives **`request.prompt.full` EXACTLY as built** (no rewrite/inject/enrich/manipulation — the OpenAIProvider sends it verbatim). **NO intelligence** (no retrieval/ranking/chronology/reasoning/significance/planning/language-gen); **uses `getProductionProvider()`** (never instantiates a provider, **never imports OpenAI**, **never bypasses the registry**); preserves `ConversationRequest`/`ConversationResponse` + every deterministic engine **exactly**. Lives in the **provider layer** (network-capable boundary — `async`, can trigger a real provider call); imports only the pure `buildConversationRequest` from core (safe one-way **providers→core**; core stays provider-free; no import cycle). **Side-effect-free import** — no clock/`Math.random`/env read/provider construction at module load; the LLM call is the ONLY non-determinism, exactly at `generateConversation`. The deprecated Phase-18 verbalizer (`buildConversationOutput` → `@deprecated ConversationOutput`, empty-text/deferred) is **NOT** in the live path (Phase 20's request engine superseded its request-building role; the OpenAIProvider is the runtime verbalizer). **DORMANT** — nothing invokes `executeConversation` yet (no UI/route; **NOT** wired into `RemyRelationship`, which would fire a real OpenAI call every app-open — a forbidden change). Callable activation seam exactly like `getProductionProvider()`; **Phase 25** wires it into a user-facing conversational surface. **Exactly ONE execution path, no duplicate provider resolution.** No forbidden file touched (`git diff HEAD` = only the new untracked file + the unrelated iOS pbxproj); `family-types.ts`/`ConversationRequest`/`ConversationResponse`, the request/verbalizer/composer/rendering/significance engines, `openai-provider.ts`/`provider-errors.ts`/`provider-types.ts`/`provider-registry.ts`/`conversation-provider.ts`, `lib/remy/index.ts`, `RemyRelationship.tsx`, all UI/routes/API, `package.json`/`package-lock.json` are byte-unchanged; still exactly one `RemyMomentChip`. Independent MULTI-AGENT adversarial review CLEAN (7 lenses — execution-correctness / provider-correctness / architecture-purity / runtime-regression / platform-integrity / deterministic-execution / future-multi-provider-readiness — 0 findings, 0 confirmed blocking). **Prior task (also on this branch): Provider Registry Activation** — an architectural activation phase making `lib/remy/providers/provider-registry.ts` the **SINGLE AUTHORITATIVE** conversation-provider resolver while preserving all runtime behaviour (it prepares Phase 24 and does **NOT** activate user-facing AI). Verified pre-work: **nothing outside `lib/remy/providers/` imports/constructs/resolves a provider** (the layer is fully **DORMANT** — `getConversationProvider` is not even called yet). Added a **resolution-only production seam** — **`PRODUCTION_PROVIDER: ProviderName = "openai"`** (a fixed deterministic literal, never env-derived) + **`getProductionProvider()`** which **delegates to `getConversationProvider(PRODUCTION_PROVIDER)`** (single canonical resolver — no second path); both **RESOLVE ONLY** (return an adapter; **never** call `generateConversation` / open a network connection / read env / execute — Phase 24 will invoke the seam). `"openai"` → real `OpenAIProvider`; every other name → `DeferredProvider` (still **THROWS** `notImplementedError`, behaviour byte-unchanged). Registry construction stays **deterministic** (no network/SDK/env/clock at load or construction). Corrected now-false post-Phase-22 docstrings ("NONE implemented" / "always false" / "ONLY registered adapter") across `provider-registry.ts` + `conversation-provider.ts` + `provider-types.ts` — **interface signatures + type definitions byte-identical** (comment-only); the legacy `ProviderResult` (typed on the deprecated `ConversationOutput`) left **byte-unchanged**. Only those **3 provider-layer files** changed (+ docs); **NO runtime activation, NO UI/routes/API/significance/RemyRelationship/`family-types`/`ConversationRequest`/`ConversationResponse`/`package.json` change**. Independent MULTI-AGENT adversarial review CLEAN (7 lenses — registry-correctness / provider-abstraction / platform-integrity / regression / deterministic-construction / future-multi-provider / architectural-purity — every raw finding adversarially refuted; **0 confirmed blocking**). **Phase 24 will ACTIVATE** (wire `getProductionProvider()` into an execution path). **Prior task (also on this branch): OpenAI Provider Adapter** — the **FIRST real production provider** implemented behind the existing provider abstraction. `lib/remy/providers/openai-provider.ts` (new) exports `OpenAIProvider implements ConversationProviderAdapter` and is registered in `provider-registry.ts` for `"openai"` (every other name still maps to the `DeferredProvider` stub, preserved). It is a **PURE EXECUTION layer** — it receives the immutable `ConversationRequest` (built by the deterministic request engine), sends `request.prompt.full` to OpenAI **exactly as supplied** (a single `user` message — **no rewriting / injecting intelligence / reordering / enriching**), passes `request.citations` through **unchanged**, and returns a `ConversationResponse` (`text` from the completion / `provider` `"openai"` / `model` / `usage` [prompt/completion/total tokens] / `status` `"generated"` / `citations` / `metadata`). It performs **NO intelligence** (no retrieval/rank/reason/score/plan/render/compose/chronology) — the deterministic Remy intelligence pipeline is entirely upstream and **UNCHANGED**. **The official OpenAI SDK + network + env + async are ISOLATED** inside `openai-provider.ts` (the ONLY `"openai"` import in the Remy provider layer; the OpenAI SDK was **already a project dependency** — used elsewhere by `lib/openai.ts`/`app/api/memory-chat/route.ts` — so **no `package.json` change** was made). **Construction is side-effect-free** (the constructor reads no env / creates no client; `resolveApiKey`/`resolveModel` read `process.env.OPENAI_MODEL`/`OPENAI_API_KEY` lazily; `new OpenAI({ apiKey })` is created inside `generateConversation`), so the registry stays deterministic and makes no network call at load. **Error handling:** missing `OPENAI_API_KEY` → `ProviderError("invalid-request")` (thrown before any network call); SDK/network failures are caught and converted via `toProviderError` (429→`rate-limited`, ≥500→`provider-failure`, other status→`invalid-request`, unknown→`provider-failure`) — **the raw SDK exception is NEVER leaked** (a new generic-message `ProviderError` is thrown). **No retries / timeouts / fallbacks yet** (later phases). **The adapter is DORMANT** — nothing in `app/`/`lib/`/`components/` outside the provider layer invokes `getConversationProvider`/`generateConversation` (server-side, env-gated), so it changes **no runtime behaviour** of the app; it is infrastructure ready for a future activation phase. `getConversationProvider("openai")` now returns the real adapter; `isProviderImplemented("openai")` is now `true`; the deferred provider is preserved for all other names. **Enum handling (no mapping layer):** the response's `provider`/`metadata.provider` use the `ConversationProvider`-typed literal `PROVIDER_TAG = "openai"` (`"openai"` ∈ BOTH the `ConversationProvider` [5-val] and `ProviderName` [8-val] unions); `this.name` stays `ProviderName`. **Pre-work:** the Phase-21 `ConversationProvider` ↔ `ProviderName` unification was investigated and **PRESERVED** (unification needs either a core→providers dependency inversion or a breaking change to `ConversationProvider`'s members — neither is "without changing public architecture"; OpenAI needs no unification, and NO temporary mapping layer was introduced) — the reconciliation remains a documented follow-up for when the divergent providers are implemented. Only `provider-registry.ts` + the new `openai-provider.ts` changed (`git diff HEAD` = `provider-registry.ts` + pbxproj; the adapter is untracked); `family-types.ts` [ConversationRequest/ConversationResponse **unchanged**], the request/verbalizer/composer/rendering/significance engines, `RemyRelationship`, `index.ts`, `conversation-provider.ts`, `provider-types.ts`, `provider-errors.ts`, and `package.json` are byte-unchanged; still exactly one `RemyMomentChip`. Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — provider-correctness, sdk-correctness [verified against the installed `openai@6.38.0` types], error-handling, architecture-purity, regressions, platform-integrity, future-multi-provider-readiness — with per-finding verification, 0 findings). Files: `lib/remy/providers/{openai-provider.ts (new), provider-registry.ts}`. Validation: tsc clean · lint 0 errors (2 pre-existing warnings) · build ✓. **Prior task (also on this branch): Conversation Provider Migration** — a PURE, types-only, behaviour-preserving migration of the provider ABSTRACTION from the legacy `ConversationOutput` request model to the canonical `ConversationRequest` → `ConversationResponse` architecture (introduced in Phase 20). Exactly 3 files changed: **`lib/remy/providers/conversation-provider.ts`** — the `ConversationProviderAdapter` interface method is now `generateConversation(request: ConversationRequest): Promise<ConversationResponse>` (was `ConversationOutput → Promise<ConversationOutput>`); imports + doc comments migrated. **`lib/remy/providers/provider-registry.ts`** — `DeferredProvider.generateConversation` migrated to the same signature; **its BODY is byte-unchanged** — still the synchronous `void request; throw notImplementedError(this.name)` (NO async introduced — a `never`-typed throw satisfies `Promise<ConversationResponse>`; execution model preserved; `configuration()`/`health()`/`getConversationProvider`/`listProviders`/`isProviderImplemented` unchanged). **`lib/remy/core/family-types.ts`** — `ConversationOutput` marked **`@deprecated`** (JSDoc pointing at `ConversationRequest`; **NOT removed** — its 5 fields byte-unchanged; retained for backwards compatibility until a future removal phase). **NO real provider implemented** (deferred stays deferred/throws — no OpenAI/Anthropic/Azure/Local), **NO network/SDK/fetch/async, NO wiring/UI/index.ts change, NO significance/intelligence change, NO other engine modified.** Backwards compat verified: the verbalizer (`buildConversationOutput`), `RemyRelationship`, the `index.ts` re-export, and `provider-types.ts` `ProviderResult.output` still reference the now-deprecated `ConversationOutput` and compile; the `@deprecated` JSDoc adds **no new lint warnings** (`no-deprecated` isn't enabled — full lint = only the 2 pre-existing unrelated warnings). `git diff HEAD` = only those 3 files + pbxproj; the request/verbalizer/composer/rendering/significance engines, RemyRelationship, `index.ts`, `provider-types.ts`, `provider-errors.ts` are byte-unchanged. Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — provider-abstraction-correctness, migration-correctness, backwards-compatibility, platform-integrity, purity, regressions, future-provider-readiness — with per-finding verification, **0 confirmed BLOCKING**; 1 CONFIRMED **non-blocking, pre-existing** finding: the `ConversationProvider` [family-types, 5 values] ↔ `ProviderName` [providers, 8 values] enum divergence means a future real adapter for gemini/azure-openai/ollama/lm-studio/custom-enterprise can't set `ConversationResponse.provider` from its `name` — LATENT [the deferred stub never builds a response], NOT introduced here, and a mandated follow-up for the future real-adapter phase [out of scope: would require editing `provider-types.ts`, not in this phase's allowed files]). Files: `lib/remy/providers/{conversation-provider.ts, provider-registry.ts}`, `lib/remy/core/family-types.ts`. Validation: tsc clean · lint 0 errors (2 pre-existing warnings) · build ✓. **Prior task (also on this branch): Conversation Request Engine** — a PURE ARCHITECTURAL REFACTOR (`lib/remy/core/conversation-request-engine.ts` + additive types in `family-types.ts`) that separates the overloaded Phase-18 `ConversationOutput` (a "provider request" object carrying an empty `text` field — architecturally a REQUEST, not an OUTPUT) into a dedicated **`ConversationRequest`** (provider INPUT) + **`ConversationResponse`** (provider OUTPUT foundation), BEFORE any real provider adapter is built. `buildConversationRequest({ conversationComposition, conversationRender, answerAssembly }) → ConversationRequest`. **`ConversationRequest`** = `prompt` (a structured `ConversationPrompt` system/body/full with the 7-clause contract in `prompt.system`) + `contract` (`ConversationContract` clauses + version) + `citations` (reused `ConversationCitation[]` — paragraph→sentence→reference→real ids) + `metadata` (provider-agnostic controls: style/audience/intent/tone/verbosity) + `summary` (counts + prompt-token estimate); **it MUST NOT contain generated text** (no `text` field). **`ConversationResponse`** = `text` + `provider` + `model` + `usage` (`ConversationResponseUsage`) + `status` (`ConversationResponseStatus`: deferred/generated/failed) + `citations` + `metadata` (`ConversationResponseMetadata`: provider/model/temperature/generated); **it MUST NOT contain the prompt** — intentionally minimal, filled by a FUTURE provider. The engine produces a `ConversationRequest` ONLY (never a `ConversationResponse`), transforming the SAME deterministic request info the verbalizer produces into the dedicated model (a migration-safe drop-in). It performs **NO intelligence** (no retrieval/ranking/chronology/reasoning/language-generation — pure serialization of the ordered plans; every refId resolves from an EXISTING referencePlan; `answerSectionById.has(...)` only annotates `source=assembly`, not a search/rank). Deterministic (ordered iteration, query-only Maps, `ceil` token estimate; no clock/randomness → byte-identical output). **`ConversationOutput` is NOT removed** (retained for backwards compatibility until a future migration phase). **NO provider/network/SDK/fetch/async/Promise, NO wiring, NO UI, NO index.ts export, NO significance/intelligence change, NO existing engine modified** — the verbalizer, composer, rendering engine, provider interface/registry, significance-engine, RemyRelationship, and index.ts are all byte-unchanged (`git diff HEAD` = `family-types.ts` [additive] + pbxproj only; the new engine is untracked; still exactly one `RemyMomentChip`). Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, determinism, no-intelligence, request/response-architecture-correctness, platform-integrity, regressions, future-migration-safety — with per-finding verification, 0 findings). Files: `lib/remy/core/{conversation-request-engine.ts (new), family-types.ts (additive)}`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Conversation Provider Interface** — the provider ABSTRACTION layer (`lib/remy/providers/`) that future provider adapters implement. **It does NOT connect to OpenAI/Anthropic/Gemini/Azure/Ollama or any network — NO fetch, NO SDK, NO async, NO network, NO wiring.** It creates ONLY the abstraction. Four pure files: **`conversation-provider.ts`** = the sole `ConversationProviderAdapter` interface (`generateConversation(request: ConversationOutput): Promise<ConversationOutput>` + `configuration(): ProviderConfiguration` + `health(): ProviderHealth`; the provider layer may ONLY accept a `ConversationOutput` and return one — it MUST NOT retrieve memories / rank / reason / score / plan / render / compose). **`provider-types.ts`** = `ProviderName` (deferred/openai/anthropic/gemini/azure-openai/ollama/lm-studio/custom-enterprise) / `ProviderCapability` / `ProviderConfiguration` / `ProviderHealth` (+ `ProviderHealthStatus`) / `ProviderResult` / `ProviderLimits` / `ProviderVersion`. **`provider-errors.ts`** = `ProviderErrorCode` + the throwable `ProviderError extends Error` (code + provider) + `notImplementedError`. **`provider-registry.ts`** = a **deterministic** registry mapping EVERY `ProviderName` to a `DeferredProvider` stub whose `generateConversation` **simply THROWS "Provider not implemented."** (NOT async — a synchronous throw satisfying the `Promise<ConversationOutput>` return type; `configuration().implemented=false`, `health().status="unimplemented"`), plus `getConversationProvider`/`listProviders`/`isProviderImplemented` (all deterministic; `isProviderImplemented` always false). **Future adapters documented, NONE implemented:** OpenAI, Anthropic, Gemini, Azure OpenAI, Ollama, LM Studio, Custom Enterprise — each future adapter is the ONLY place a `fetch`/SDK/real LLM call may EVER live. **NO wiring** into RemyRelationship, **NO UI/JSX**, **NO provider call**, and **NO existing file changed** (only the new `lib/remy/providers/` directory was added — `git diff HEAD` = pbxproj only; RemyRelationship/`lib/remy/index.ts`/significance-engine/family-types/all prior engines byte-unchanged; still exactly one `RemyMomentChip`). Intentionally NOT exported from `@/lib/remy` (internal platform infra, not a feature API — a future adapter imports the siblings directly). Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, provider-isolation, no-intelligence, abstraction-correctness, determinism, platform-integrity, regressions — with per-finding verification, 0 findings). Files: `lib/remy/providers/{provider-errors.ts, provider-types.ts, conversation-provider.ts, provider-registry.ts}` (all new). Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Conversation Verbalizer Engine** — a PURE, deterministic, SYNCHRONOUS engine (`lib/remy/core/conversation-verbalizer-engine.ts`, **the FIRST provider-boundary layer**). Architecturally the ONLY place natural language would be produced, but the **actual LLM verbalization is DEFERRED** (consistent with the launch-only priority + the AI-connection deferral): the engine makes **NO network/LLM call**. `buildConversationOutput({ conversationComposition, conversationRender, answerAssembly, provider?, model?, temperature? }) → ConversationOutput` (text / citations / metadata / tokens / generation). It consumes ONLY the already-complete `ConversationComposition` (+ the `ConversationRender`/`AnswerAssembly` it composes) read-only and assembles the deterministic **PROVIDER REQUEST** a FUTURE provider adapter (OpenAI/Anthropic/…) would send. **Prompt contract:** the prompt embeds the mandatory 7 clauses VERBATIM — "You are not allowed to retrieve information / rank memories / infer new facts / change chronology; You must verbalize ONLY the supplied ConversationComposition; Every factual statement must trace to supplied references; If information is missing, do not invent it" — plus the structured composition serialization (sections/paragraphs/sentence-plans in order + factual-source ids). **Citations** = paragraph → sentence ids → reference ids → real entity ids (traceability). **Provider metadata** (provider/model/temperature), **token** estimate (`ceil(prompt.length/4)`, completion 0), **generation** metadata (prompt + contract + status). **LLM responsibilities / boundary:** the (future) provider adapter is the ONLY place a `fetch`/real LLM call may live; the LLM may choose WORDING but NOT content — it may not change chronology/importance/ordering/references/facts/memory-ids. **Deterministic vs non-deterministic boundary:** all inputs + this output are deterministic (`text=""`, `verbalized=false`, `status="deferred"`); the ONLY non-determinism (a future LLM's wording) is not in this pure output. It does **NO intelligence** (no retrieve/search/rank/infer/compare/chronology/significance/plan-building/reference-invention — every refId resolves from an EXISTING referencePlan); every output field is a structured id, enum, number, or the prompt/citation strings. Deterministic (iterates ordered arrays; query-only Maps; no clock/randomness → byte-identical output). **CRITICAL: this provider-side engine feeds NOTHING** — **NO `significance-engine` change and NO prior deterministic engine changed** (among `lib/remy/core/*.ts` only `family-types.ts` [additive] changed); in `RemyRelationship` it is computed immediately after the composer and `void`-ed (consumer = the future provider adapter). INTERNAL (not shown); render path byte-unchanged (exactly one `RemyMomentChip`). Types (`ConversationOutput`/`ConversationOutputMetadata`/`ConversationCitation`/`ConversationGeneration`/`ConversationTokenUsage`/`ConversationProvider`); exported from `@/lib/remy` for the future provider adapter. Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, provider-isolation, no-intelligence, prompt-safety, determinism-boundaries, platform-integrity, regressions — with per-finding verification, 0 findings). Files: `lib/remy/core/{conversation-verbalizer-engine.ts (new), family-types.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts` (**significance-engine.ts and all prior engines byte-unchanged**). Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Conversation Composer Engine** — a PURE engine (`lib/remy/core/conversation-composer-engine.ts`, **the FIRST natural-language-PLANNING layer**). It consumes ONLY the already-approved `ConversationRender` + the `AnswerAssembly` it renders (+ optional deterministic style/audience/intent controls) and prepares a deterministic COMPOSITION PLAN of how a FUTURE LLM/API provider would compose the answer: `buildConversationComposition({ conversationRender, answerAssembly, style?, audience?, intent? }) → ConversationComposition` (sections / paragraphs / sentencePlans / referencePlans / flow / metadata / context / summary). **NOT chat, NOT GPT, NOT an LLM, and it generates NO language** (no sentences/paragraphs/prose/prompts/LLM calls); it performs NO retrieval / search / ranking / reasoning / chronology-construction / significance / evidence-scoring / factual decisions. **Composition sections** map each render section → a structural role hint (lead/body/aside/summary) + paragraph plans; **paragraph plans** group **sentence plans** whose `kind` is a structural ROLE (opening/topic/evidence/transition/closing — never text); **reference plans** point at real entity ids (kind looked up from the real answer section via a `kindMapOf` join — NOT retrieval/ranking); **flow** = opening/closing section-id POINTERS + transition ids + section order; metadata/context carry the render's tone/verbosity + counts. **No prose, no narration, no generated text, no prompts, no invented ids** — every field is a structured id, enum, or number; empty render → empty composition. Deterministic (iterates render sections in their existing renderOrder; structural ids `compose-<renderId>`/`<sectionId>-p<i>`/`-s<i>`/`-r<i>`; Maps/Sets are query-only; no clock/randomness → byte-identical output). **CRITICAL: this presentation-side engine deliberately feeds NOTHING** — there is **NO `significance-engine` change and NO prior deterministic engine changed** (among `lib/remy/core/*.ts` only `family-types.ts` [additive] changed); in `RemyRelationship` it is computed immediately after the render and `void`-ed (consumer = the future LLM/API layer). INTERNAL (not shown); render path byte-unchanged (exactly one `RemyMomentChip`). Types (`ConversationComposition`/`ConversationCompositionSection`/`ConversationCompositionMetadata`/`ConversationCompositionContext`/`ConversationCompositionSummary`/`ConversationStyle`/`ConversationAudience`/`ConversationIntent`/`ConversationFlow`/`ConversationParagraph`/`ConversationSentencePlan`/`ConversationReferencePlan`); exported from `@/lib/remy` for the future LLM/API provider. Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, determinism, consumes-only-render, no-language-generation, platform-integrity, pipeline-integrity, regressions — with per-finding verification, 0 findings). Files: `lib/remy/core/{conversation-composer-engine.ts (new), family-types.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts` (**significance-engine.ts and all prior engines byte-unchanged**). Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Conversation Rendering Engine** — a PURE engine (`lib/remy/core/conversation-rendering-engine.ts`, **the FIRST PRESENTATION-LAYER engine**; the deterministic intelligence stack is complete). It prepares deterministic RENDERING INSTRUCTIONS only — it does **not** perform retrieval, reasoning, or language generation: `buildConversationRender({ answerAssembly, tone?, verbosity?, perspective? }) → ConversationRender` (sections / metadata / summary / context). It consumes ONLY the already-complete `AnswerAssembly` (+ optional deterministic tone/verbosity/perspective controls) and produces render metadata for a FUTURE conversational/LLM layer. **NOT chat, NOT GPT, NOT an LLM, and generates NO language.** **Sections** = the top-`maxSections` (per verbosity: brief 4 / normal 8 / detailed 16) assembly sections rendered as instructions (structural `render-<sectionId>` id + `style` hint [highlight/standard/supporting/summary, from section kind + weight] + importance + the section's own real `evidenceIds`; no text). **Metadata** = `preferredOpening`/`preferredClosing` (structural render-section-id POINTERS — never opening/closing text) + `emphasis` (ids) + continuity + confidence (carried from the assembly coverage). **Context** = the resolved tone/verbosity/perspective controls + section budget + includeChronology flag + depth. **Summary** = section/evidence counts + confidence + renderComplexity. **No prose, no narration, no generated answers, no prompts, no invented ids** — every field is a structured id, enum, or number; empty assembly → empty render. Deterministic (assembly sections arrive already sorted → slice + map with `renderOrder` index; evidence Set drained by sort; structural ids; no clock/randomness → byte-identical output). **CRITICAL: this presentation engine deliberately does NOT feed the significance pipeline** (it must not rank memories; it is presentation prep, not a memory signal) — so **NO `significance-engine` change and NO prior deterministic engine changed this phase**; in `RemyRelationship` it is computed immediately after answer-assembly and `void`-ed (its consumer is the FUTURE conversational layer). INTERNAL (not shown); render path unchanged (exactly one `RemyMomentChip`). Types (`ConversationRender`/`ConversationRenderSection`/`ConversationRenderMetadata`/`ConversationRenderContext`/`ConversationRenderSummary`/`ConversationTone`/`ConversationVerbosity`/`ConversationPerspective`); exported from `@/lib/remy` for the future conversational layer. Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, no-language-generation, consumes-only-assembly, determinism, platform-integrity/pipeline, consumption-intent [verified the deliberate no-significance + `void` design is correct], regressions — with per-finding verification, 0 findings). Files: `lib/remy/core/{conversation-rendering-engine.ts (new), family-types.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts` (**significance-engine.ts and all prior engines byte-unchanged**). Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Answer Assembly Engine** — a PURE engine (`lib/remy/core/answer-assembly-engine.ts`, the FINAL deterministic intelligence layer before any conversational/LLM rendering) that assembles ONLY the structured, FACTUAL answer PACKAGE a FUTURE conversational layer will VERBALIZE: `buildAnswerAssembly({ answerPlan, questionUnderstanding, conversationFoundation, biography, reasoning, lifeStory, journeyAnalysis, graph, understandings, …optional }) → AnswerAssembly` (sections / chronology / evidence / memories / people / journeys / chapters / anchors / themes / references / coverage / context / summary). **Explicitly NOT chat, NOT GPT, NOT an LLM, and generates NO answers — no deferred AI was built.** **Sections** = the answer-plan steps rendered as structured sections (fixed `SECTION_KIND_OF_STEP` map; kinds memory/journey/chapter/timeline/relationship/theme/anchor/comparison/summary), real ids + weight/confidence/order only. **Chronology** = deterministic order from the real life-story chapters (ids/order/confidence — no generated timeline text). **Evidence**/**references** = real supporting entities (memory/journey/chapter/anchor/theme/person) aggregated + deduped + weight-ranked + bounded (each memory's real biography chapter added; memories additionally ranked by real graph connectivity). **Coverage** = memory/journey/chapter/theme/anchor/person coverage + timeline/answer completeness + confidence + context depth (all clamped 0–100). **No prose, no narration, no generated answers, no invented ids** — every field is a structured id, enum, or number; if data doesn't exist it produces ZERO output. Deterministic (sections from ordered steps + fixed map + sort/slice + order-index; evidence Map drained by `sort(weight desc, kind asc, refId asc)`; graph-ranked memories; structural ids `assembly-<slug>`; no clock/randomness → byte-identical output). **ALL 9 required inputs genuinely consumed** (answerPlan→sections, QU→confidence, conversation-foundation→confidence, biography→memory-chapters, reasoning→summary/themes/anchors, life-story→chronology/continuity, journey→totals, graph→memory ranking, understandings→totals/people — verified by the consumption lens). INTERNAL (not shown); sits after answer-planning in the pipeline; its per-memory section weight feeds the significance engine (a memory backing a strong assembled section = more significant) via the FINAL clean optional `SignificanceContext.answerAssemblyStrengthByMemoryId` extension. REQUIRED inputs = answer-plan + question-understanding + conversation-foundation + biography + reasoning + life-story + journey + graph + understanding (passed live); favourite/significant/relationship/emotional are OPTIONAL refinements. **This COMPLETES the deterministic intelligence stack** — the next Remy layer would be the conversational/LLM RENDERING layer that consumes this package (a separate approved phase). Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, determinism, no-fabrication, platform-integrity, pipeline-order, consumption, regressions — with per-finding verification, 0 findings). Files: `lib/remy/core/{answer-assembly-engine.ts (new), family-types.ts, significance-engine.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Answer Planning Engine** — a PURE engine (`lib/remy/core/answer-planning-engine.ts`) that builds the deterministic EXECUTION PLAN a FUTURE conversational layer will run AFTER Question Understanding: `buildAnswerPlan({ questionUnderstanding, conversationFoundation, biography, reasoning, lifeStory, journeyAnalysis, graph, understandings, …optional }) → AnswerPlan` (steps / sources / context / coverage / summary). **Explicitly NOT chat, NOT GPT, NOT an LLM, and produces NO answers — no deferred AI was built.** **Steps** = an ordered list of structured retrieval steps (kinds memory/journey/chapter/theme/anchor/person/timeline/relationship/event/summary/comparison/reference), each EXECUTES a real question intent (fixed `STEP_KIND_OF_INTENT` map; the `place` intent maps to NO step; the `reference` step kind is reserved and never produced) and points only at real ids copied from the intent focus; sorted by weight and `order`-numbered (capped `MAX_STEPS`). **Sources** = the real entity pool the steps draw from — journey/chapter/anchor/theme/person from the steps, each memory's real biography chapter, **memory sources ranked by real graph connectivity** (capped `MAX_MEMORY_SOURCES`), plus lifeStory milestones and optional significant memories / favourite people. **Coverage** = memory/journey/theme/person/timeline coverage; **Context**/**Summary** = structured metrics only. **No prose, no generated answers, no invented ids** — every step/source/metric is a structured id, enum, or number. Deterministic (steps from ordered intents + fixed map + sort/slice + order-index; sources from ordered steps + graph-ranked memories; structural ids `step-<kind>-<slug>`; no clock/randomness → byte-identical output). **ALL 8 required inputs genuinely consumed** (QU→steps, biography→chapters, graph→memory ranking, reasoning→summary/themes, life-story→milestones/continuity, journey→totals, conversation-foundation→confidence blend, understandings→totals/people — verified by the consumption lens). INTERNAL (not shown); sits after question-understanding in the pipeline; its per-memory step weight feeds the significance engine (a memory targeted by a strong retrieval step = more significant) via a clean optional `SignificanceContext.answerPlanStrengthByMemoryId` extension. REQUIRED inputs = question-understanding + conversation-foundation + biography + reasoning + life-story + journey + graph + understanding (passed live); favourite/relationship/significant/emotional are OPTIONAL refinements (forward-compatible). Foundation for a future question/answer/chat layer (to be built LATER in a separate approved phase). Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, determinism, no-fabrication, platform-integrity, pipeline-order, consumption, regressions — with per-finding verification, 0 findings). Files: `lib/remy/core/{answer-planning-engine.ts (new), family-types.ts, significance-engine.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Question Understanding Engine** — a PURE engine (`lib/remy/core/question-understanding-engine.ts`) that builds the deterministic layer a FUTURE conversational layer will use to convert a PARSED question into structured RETRIEVAL INTENT over the real intelligence stack: `buildQuestionUnderstanding({ conversationFoundation, biography, reasoning, lifeStory, journeyAnalysis, graph, understandings, …optional }) → QuestionUnderstanding` (intents / focus / constraints / references / context / summary). **Explicitly NOT chat, NOT GPT, NOT an LLM, and takes NO free-text question — no NL parsing, no deferred AI was built.** **Intents** = the answerable retrieval intents (kinds memory/person/journey/theme/life-stage/relationship/timeline/event/date/**place**/comparison/summary/reference), each derived from a REAL upstream entity (topics/anchors/journeys/chapters/milestones/influences/relationship-strengths/graph-degree memories) with its own structured `focus` + `weight` + `confidence`; **the `place` kind is NEVER produced** (schema stores no locations — a supported kind with no real backing yields ZERO intents rather than a fabricated one — a strong no-fabrication signal). **Focus** = real structured ids (memory/journey/chapter/anchor/theme/person/life-stage); **Constraints** = structured only (date-range/life-stage/person/theme/journey); **References** = pointers ONLY to real memory/journey/chapter/theme/anchor/person; **Context**/**Summary** = structured metrics only. **No natural language, no generated text, no invented ids** — every output is a structured number, a real id, or an enum. Deterministic (all foci sorted via `makeFocus`; intents sorted (weight desc, id asc) + per-kind capped + globally capped `MAX_INTENTS`; structural ids `intent-<kind>-<slug>`; no clock/randomness → byte-identical output). INTERNAL (not shown); sits after conversation-foundation in the pipeline; its per-memory intent weight feeds the significance engine (a memory in a strong retrieval-intent focus = more significant) via a clean optional `SignificanceContext.questionStrengthByMemoryId` extension. REQUIRED inputs = conversation-foundation + biography + reasoning + life-story + journey + graph + understanding (passed live); favourite/relationship/significant/emotional are OPTIONAL refinements (forward-compatible; favourites → person references, significant → seeds memory intents). Foundation for a future question/retrieval/chat layer (to be built LATER in a separate approved phase). Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, determinism, no-fabrication, platform-integrity, pipeline-order, consumption, regressions — with per-finding verification, 0 findings). Files: `lib/remy/core/{question-understanding-engine.ts (new), family-types.ts, significance-engine.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Conversation Foundation Engine** — a PURE engine (`lib/remy/core/conversation-foundation-engine.ts`) that builds the deterministic groundwork a FUTURE conversational layer will consume: `buildConversationFoundation({ journeyAnalysis, lifeStory, reasoning, biography, graph, understandings, …optional }) → ConversationFoundation` (topics / threads / references / context / summary). **Explicitly NOT chat, NOT GPT, NOT an LLM, NOT prompts, NOT generated text** — no deferred AI was built. **Topics** = REAL recurring subjects (kind anchor / theme / person / life-stage; each `≥ MIN_TOPIC_MEMORIES` real memories; `"other"` excluded, a theme already an anchor is not duplicated; people from real `understandings` + `reasoning.influences`/favourites); **Threads** = a topic's memories grouped by the real biography chapter they live in (`≥ MIN_THREAD_MEMORIES`); **References** = pointers ONLY to real memory/journey/chapter/anchor/theme/person ids (bounded — `MAX_TOPICS`/`MAX_THREADS`/`MAX_PEOPLE_TOPICS`/`MAX_MEMORY_REFS`); **Context**/**Summary** = structured metrics only. **No invented topics/threads/memories/people/dates, no narration/prompts/generated sentences** — every output is a structured number or a real id/enum. Deterministic (sorted topics/threads with total-order tiebreakers + fixed life-stage order + structural ids `topic-<kind>-<refId>`/`thread-<topicId>-<sectionId>`; no clock/randomness → byte-identical output). INTERNAL (not shown); sits after biography in the pipeline; its per-memory topic weight feeds the significance engine (a memory in a strong conversation topic = more significant) via a clean optional `SignificanceContext.conversationStrengthByMemoryId` extension. REQUIRED inputs = journey + life-story + reasoning + biography + graph + understanding (passed live); favourite/relationship/significant/emotional are OPTIONAL refinements (forward-compatible). Foundation for a future conversational / chat layer (to be built LATER). Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, determinism, no-fabrication, platform-integrity, pipeline-order, consumption, regressions — with per-finding verification, 0 findings). Files: `lib/remy/core/{conversation-foundation-engine.ts (new), family-types.ts, significance-engine.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Biography Engine** — a PURE engine (`lib/remy/core/biography-engine.ts`) that assembles a STRUCTURED (non-prose) representation of a life from the real journey/life-story/reasoning/graph/understanding layers: `buildBiography({ journeyAnalysis, lifeStory, reasoning, graph, understandings, …optional }) → BiographyAnalysis` (sections / periods / references / coverage / summary) — the foundation for a future biography/story-book renderer that resolves the references to real data. **Sections** mirror the real life-story chapters 1:1 (id/title[reused chapter title]/journeyIds/chapterIds/memoryIds/theme/lifeStage + `coverage` [breadth] + `confidence` [dated/media/peopled backing]); **Periods** group sections chronologically by life stage using ONLY real memory years (0 when undated — never invents a date); **References** point ONLY at real journey/chapter/anchor/theme/person/memory ids (bounded — `MAX_PEOPLE_REFS`/`MAX_MEMORY_REFS`); **Coverage** = structured metrics (memory/journey/chapter/life-stage/timeline coverage + confidence); **Summary** = dominantTheme/dominantAnchor/coveredYears/coverage/confidence. **No paragraphs, no narration, no fabricated memories/people/dates/chronology.** Deterministic (chapters-order sections + fixed stage-order periods; structural ids `section-<chapterId>`/`period-<stage>`; no clock/randomness → byte-identical output). INTERNAL (not shown); sits after reasoning in the pipeline; its per-memory section coverage feeds the significance engine (a memory in a well-covered biography section = more significant) via a clean optional `SignificanceContext.biographyCoverageByMemoryId` extension. REQUIRED inputs = journey + life-story + reasoning + graph + understanding (passed live); favourite/emotional/relationship/significant are OPTIONAL refinements (forward-compatible). Foundation for future biography / story-book / conversation surfaces. Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, determinism, no-fabrication, platform-integrity, pipeline-order, consumption, regressions — with per-finding verification, 0 findings). Files: `lib/remy/core/{biography-engine.ts (new), family-types.ts, significance-engine.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Reasoning Engine** — a PURE engine (`lib/remy/core/reasoning-engine.ts`) that REASONS over the real journey/life-story/graph/understanding layers to derive Remy's structural understanding OF a life: `buildReasoning({ journeyAnalysis, lifeStory, graph, understandings, …optional }) → ReasoningAnalysis` (anchors / themes / influences / relationshipStrengths / gaps / summary). **Life Anchors** = the dominant structural pillars (a theme with `≥ MIN_ANCHOR_MEMORIES` real journey-memories; `"other"` is never an anchor; `strength` + real-signal `confidence`); **Life Themes** = the real lifetime theme distribution + share; **Life Influences** = people with greatest real influence (memory/journey counts + graph degree + optional favourite bonus); **Relationship Strengths** = structured counts only (memory/journey/co-appearance — NO emotional reading); **Memory Gaps** = FACTUAL only (large year gaps, sparse/missing-between-present/weakly-documented life stages — and NEVER a guess at WHY). All scores structured numbers (never prose); no GPT, no fabricated anchors/people/dates/chronology, no inference beyond the real data. REQUIRED inputs = journey + life-story + graph + understanding; emotional/personality/relationship/favourite/significant are OPTIONAL refinements (forward-compatible; the required layers are passed live). INTERNAL (not shown); sits after life-story in the pipeline; its per-memory anchor strength feeds the significance engine (a memory anchoring a strong life pillar = more significant) via a clean optional `SignificanceContext.reasoningStrengthByMemoryId` extension. Foundation for future reasoning / conversation / biography surfaces. Independent MULTI-AGENT adversarial review CLEAN (7 independent lenses — purity, determinism, no-fabrication, platform-integrity, pipeline-order, consumption, regressions — with per-finding verification, 0 findings). Files: `lib/remy/core/{reasoning-engine.ts (new), family-types.ts, significance-engine.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Life Story Engine** — a PURE engine (`lib/remy/core/life-story-engine.ts`) that assembles the canonical, structured CHRONOLOGICAL life story from real journeys: `buildLifeStory({ journeyAnalysis, graph?, understandings?, chapters?, significantMemories? }) → LifeStoryAnalysis` (story / chapters / timeline / milestones / summary) — the source for future AI conversation / biography / timeline UI / story-book export / reasoning. Journeys are chronologically ordered (undated last) then chained into CHAPTERS by single-linkage: two adjacent journeys join only when their years are continuous (dated gap `≤ MAX_CHAPTER_GAP`; a dated gap `> MAX_HARD_GAP` ALWAYS splits — disconnected life periods never merged), their life stages compatible, AND a REAL relational signal supports it (shared people / journey-or-graph connection / same theme / shared life chapter). Chapters carry deterministic `continuity`/`centrality`; the timeline/milestones/summary reference only existing journeys/years/memories (no prose/narration; titles reuse real journey titles). Undated memories never fabricate a year (`startYear/endYear = 0`); no GPT, no invented chapters/years/events. REQUIRED input = `JourneyAnalysis`; graph/understandings/chapters/significant are OPTIONAL refinements (graph + understandings passed live). INTERNAL (not shown); sits after journey in the pipeline; its per-memory life-story centrality feeds the significance engine (more central to the story = more significant) via a clean optional `SignificanceContext.lifeStoryCentralityByMemoryId` extension. Foundation for future timeline / biography / story-book / reasoning surfaces. Independent MULTI-AGENT adversarial review CLEAN (4 independent lenses — purity/determinism, honesty/no-fabrication, platform-integrity, consumption/regressions — over all 12 verification points, 0 findings, per-finding verification). Files: `lib/remy/core/{life-story-engine.ts (new), family-types.ts, significance-engine.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Journey Engine** — a PURE engine (`lib/remy/core/journey-engine.ts`) that turns the understanding + graph layers into complete LIFE JOURNEYS: `buildJourneys({ understandings, graph, … }) → JourneyAnalysis` (journeys / connections / summary). A Journey is a structured collection of connected memories representing ONE continuous part of a life (School Years / University / Career / Family Holidays / Medical Journey / …). Journeys EMERGE from real signals only — dominant theme + life stage + shared people + chronological continuity + graph connectivity: memories are grouped by dominant theme, split into continuous segments by real year gaps (`> MAX_GAP_YEARS`, or a chapter change with a smaller gap when chapters are supplied), and a segment below `MIN_JOURNEY_SIZE` is left separate (never force-merged). Each journey carries deterministic `significance`/`continuity`/`importance` + a real `JourneyTimeline`; journeys link by real shared people/theme (`JourneyConnection`). Undated memories never fabricate a year. No GPT, no fabricated journeys/years/transitions/links. INTERNAL (not shown); sits after memory-graph in the pipeline; its per-memory journey significance feeds the significance engine (more journey-anchored = more significant) via a clean optional `SignificanceContext.journeyImportanceByMemoryId` extension. REQUIRED inputs = understanding + graph (its pipeline-position outputs); chapters/significant/emotional/favourites are OPTIONAL refinements (no-op today, forward-compatible). Foundation for future timeline / journeys / life-story / reasoning surfaces. Adversarial review CLEAN (12/12; no blocking issues). Files: `lib/remy/core/{journey-engine.ts (new), family-types.ts, significance-engine.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Memory Graph Engine** — a PURE engine (`lib/remy/core/memory-graph-engine.ts`) that turns the `MemoryUnderstanding[]` into a deterministic semantic GRAPH of how memories connect: nodes (per memory), edges (two memories share a REAL attribute — same-person/family/theme/chapter/year/category/event/life-stage, weighted → weak/moderate/strong), and theme clusters (Family Memories / Travel / School Years / Work Life / Medical Journey / Pet Memories / …). Real-data-only, no GPT, no fabricated links; edges pruned (`MIN_EDGE_WEIGHT`) + deterministically capped (`MAX_EDGES`). INTERNAL (not shown); sits after memory-understanding in the pipeline; its per-memory edge-degree feeds the significance engine (more connected = more significant) via a clean optional `SignificanceContext` extension. Foundation for future related-memories / journeys / semantic-search / reasoning. Adversarial review CLEAN (12/12; the one flagged double-count of same-event/same-category was fixed — category counted only when not already captured by the event). Files: `lib/remy/core/{memory-graph-engine.ts (new), family-types.ts, significance-engine.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Memory Understanding Engine** — a PURE engine (`lib/remy/core/memory-understanding-engine.ts`) at the FRONT of the relationship pipeline that transforms every REAL stored memory into a structured `MemoryUnderstanding[]` (per-memory themes, life stage, importance ordinary/important/major/legacy, attachment/emotional/relationship richness, event type, time span, structured relationship, confidence). Deterministic, real-data-only, **structured data only — no prose/GPT/fabrication**; life stage + themes inferred from real `ai_category` keywords (unknown/other fallback). INTERNAL (not shown in UI); computed first in `RemyRelationship` (pipeline: snapshot → memory-understanding → story → favourite → anniversary → significance → emotional → personality → relationship → priority → one renderer) and genuinely consumed (the emotional/personality richness ratios now derive from it), and exported from `@/lib/remy` for future engines. No snapshot/DB change (operates on the Phase-5-enriched `DatedMemory`). Adversarial review CLEAN (12/12). Files: `lib/remy/core/{memory-understanding-engine.ts (new), family-types.ts}`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Emotional Intelligence Engine** — Remy stops ranking memories by quantity and starts understanding PEOPLE + emotional SIGNIFICANCE. Three PURE core engines: `significance-engine` (ranks memories by significance — people/favourite/anniversary/chapter-size/attachments/ai-importance/revisits, NOT recency), `emotional-engine` (→ `EmotionalProfile`: most significant person/memory, strongest chapter, most active relationship, most revisited memory, most emotional category + 4 internal family-strength/life-continuity/relationship-health/memory-preservation scores), `personality-engine` (→ behavioural traits: family-historian/memory-guardian/story-teller/legacy-builder/care-champion/photo-collector/daily-rememberer/occasional-visitor). Raw scores are INTERNAL — only behaviours surface. `relationship-engine` now CONSUMES the `EmotionalProfile` (+ traits) → 5 new observations. Full pipeline wired in `RemyRelationship`: snapshot → story → favourite → anniversary → significance → emotional → personality → relationship → priority → one `<Remy>` renderer; the relationship snapshot loader was enriched with real `attachments`/`ai_importance`/`memory_person_links`/historical signals. Real data only. Adversarial review CLEAN (12/12). Files: `lib/remy/core/{significance-engine, emotional-engine, personality-engine (new), family-types, relationship-engine}.ts`, `app/api/remy/relationship-snapshot/route.ts`, `components/remy/companion/RemyRelationship.tsx`, `lib/remy/index.ts`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Living Relationship System** — Remy stops feeling reactive and begins a long-term relationship with the family. Six PURE core engines (no React/DOM/DB/timers/clock): `relationship-engine` (RelationshipSnapshot → warm long-term observations), `story-engine` (infer life chapters from decade + dominant category — no hardcoded names), `anniversary-engine` (real day-precision anniversaries only), `favourite-engine` (rank people by real `mention_count`), `legacy-engine` (deterministic life summary — timeline/chapters/key-people/major-events, NOT AI), `legacy-export` (structured object for a future PDF/book). `RemyRelationship` (mounted once) runs them **once per app-open** over a new read-only, auth-gated, workspace-scoped snapshot loader (`app/api/remy/relationship-snapshot` — memories + `people` + dated memories; never polled) and shows AT MOST ONE relationship moment. A shared `RemyMomentChip` + a `moment-gate` mutex + a generic `selectMoment<RankableMoment>` mean only ONE proactive moment shows globally (RemyRelationship yields to RemyMoments) with no duplicated logic. Relationship memory persisted (cooldowns/acknowledgements). Real data only — no fabrication. Adversarial review CLEAN (14/14). Files: `lib/remy/core/{family-types, relationship-engine, story-engine, anniversary-engine, favourite-engine, legacy-engine, legacy-export, priority-engine}.ts`, `lib/remy/companion/{persistence, moment-gate, day}.ts`, `app/api/remy/relationship-snapshot/route.ts`, `components/remy/companion/{RemyMomentChip, RemyRelationship, RemyMoments}.tsx`, `lib/remy/index.ts`, `app/(app)/layout.tsx`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): Companion Intelligence layer** — Remy notices meaningful things proactively (behavioural intelligence, NOT AI chat / notifications / polling). Two PURE core engines: `insights-engine.ts` maps a `CompanionSnapshot` → behavioural `Observation[]`; `priority-engine.ts` dedupes + drops cooling-down + ranks (urgency→importance) → **at most ONE** moment. `RemyMoments` (mounted once) runs **once per app-open** — a single read of a new read-only, auth-gated, workspace-scoped snapshot loader (`app/api/remy/companion-snapshot`, never polled) — then briefly shows one moment through the single `<Remy>` renderer (portaled, tap-to-dismiss, aria-live, reduced-motion-safe). Behavioural memory (`persistence.ts` `CompanionMemory`: last-visit day, acknowledged Nest stage, per-kind cooldowns) means greetings fire once/day and moments never repeat. Extends the ONE platform (single renderer + persistence + core); no second provider/bus/brain. Adversarial review CLEAN (12/12). Files: `lib/remy/core/{insights-engine.ts (new), priority-engine.ts (new)}`, `lib/remy/companion/persistence.ts`, `app/api/remy/companion-snapshot/route.ts` (new), `components/remy/companion/RemyMoments.tsx` (new), `lib/remy/index.ts`, `app/(app)/layout.tsx`. Validation: tsc clean · lint 0 errors · build ✓. **Prior task (also on this branch): App-wide Remy companion layer** — extended the ONE Remy platform (no second AI/renderer/provider/bus/brain; backwards-compatible) to make Remy a presence across the whole app. **(1) Screen awareness:** `RemyScreenAwareness` (mounted once) publishes a brief arrival reaction per screen via a pure route→event map (`lib/remy/core/screen-behavior.ts`); new `screen.*` events added to the vocabulary (`events.ts` + `emotion-engine.ts`). **(2) Milestone celebrations from REAL counts:** `RemyMilestones` compares the real workspace `memoryCount` against a persisted last-count (`lib/remy/companion/persistence.ts`) and emits `milestone.reached` on crossings (`lib/remy/core/achievements.ts` — first/10/50/100/500/1000 + Nest stage-ups; baselines on first load, no retroactive spam). **(3) Celebration surface:** `RemyCelebration` (mounted once) subscribes to the bus and plays a centre-stage feather-burst + sparkles + heart through the single `<Remy>` renderer using the **real `goldenFeather` asset**; portaled, pointer-events-none, aria-live, reduced-motion-safe. **(4) Reusable effects:** `components/remy/effects/RemyEffects.tsx` (framer-motion, centralized). **Regression found + fixed by adversarial review:** the celebration surface (a provider child) subscribing to the raw bus was draining the initial-mount replay buffer meant for the Brain → added a backwards-compatible **`{ replay: false }`** subscribe option (`event-bus.ts`) so the Brain (default replay) always gets the buffer regardless of order.

Files Changed (RC2 Security Hardening): NEW `lib/logger.ts`, `lib/security/rate-limit.ts`; EDIT `next.config.js`, `middleware.ts`, `lib/retrieve-memory-context.ts`, `app/(app)/dashboard/actions.ts`, `app/(app)/remy/story-action.ts`, `app/api/active-profile/route.ts`, `app/api/health/route.ts`, `app/api/send-notification/route.ts`, `app/auth/logout/route.ts`, `app/api/stripe/{checkout,portal,webhook}/route.ts`, `app/api/gdpr/{export,delete-account}/route.ts`, `app/api/memories/{create,upload-url,[id]/enrich}/route.ts`, `app/api/memory-chat/route.ts`, `lib/data/insights.ts`, `e2e/{cron-auth,profile-isolation,premium-enforcement}.spec.ts` + docs; DELETE `app/api/{search,create-reminder,send-reminders}/route.ts`. `ios/App/App.xcodeproj/project.pbxproj` left unstaged. (Earlier tasks on this same unpushed branch — incl. Memory-Intelligence-Engine-V2 [`761d3e0`], AI-Subscriptions-Quotas-Dashboard [`5b6a607`] — see git log.)

Commit: *(RC2 Security Hardening — this increment, on top of `761d3e0` Memory-Intelligence-Engine-V2; see git log)*

Validation: `npx tsc --noEmit` clean · `npm run lint` 0 errors (2 pre-existing unrelated warnings) · `npm run build` ✓ Compiled successfully · independent MULTI-AGENT adversarial review CLEAN (7 lenses; 0 confirmed blocking; all 6 non-blocking findings fixed + re-validated).

==================================================
## NEXT RECOMMENDED TASK
==================================================

**Build the UGC report-and-block moderation layer + EULA abuse clause (Apple Guideline 1.2).**

The single highest-priority engineering deliverable before App Store submission: because memories are shared into caregiver/family workspaces, RemyNest is a user-generated-content app that shares content between users, so Apple requires a report mechanism, a user-block mechanism, and an EULA abuse clause. It is the last App-Store-required engineering feature standing between the finished product and submission. (Not Restore Purchases — unnecessary in the web-only purchase model.)

==================================================
## MAINTENANCE PROTOCOL (how this file stays true)
==================================================

**Start of every session/task:** read this file → CLAUDE.md → HANDOFF_CURRENT.md → relevant architecture/feature docs → only the source files the task needs.

**Before suggesting any feature, check:** (1) does it already exist? (2) already completed? (3) intentionally deferred? (4) in DO NOT REBUILD? (5) already implemented differently? If any = yes → do NOT suggest it.

**After every completed task, automatically (never ask) — the mandatory 6-step workflow:**
1. **Verify against source code** (source is the only authority — never rely on memory or docs alone).
2. **Update this file** (`REMY_MASTER_STATE.md`): VERIFIED COMPLETE, Current/Next Task, Last Completed Task, launch % + project %, milestone, sprint, and the header (Commit / Branch / Repository Verified).
3. **Update `HANDOFF_CURRENT.md`** — keep it a lightweight continuation doc; it must never contradict this file.
4. **Update `CLAUDE.md` ONLY if architecture changed** → also append the decision to LOCKED ARCHITECTURE DECISIONS here.
5. **Verify documentation still matches source** (reconcile forward; source wins).
6. **Only then continue** to the next roadmap task.
