# RemyNest Asset Production — icons, store, marketing

Specs + audit + missing list for every launch raster. **The PNG raster pipeline is
COMPLETE + RUN (2026-06-23):** `sharp` is installed (devDep) and
`scripts/generate-brand-assets.mjs` (run via `node scripts/generate-brand-assets.mjs`)
generates — from the sage-nest `logo-mark-dark.svg` master — the **true-square PWA icons
192/512** (replacing the broken 1536×1024 dups), **maskable 192/512**, **App Store
1024**, **Play 512 + adaptive fg/bg 432**, **favicon 16/32/48 PNGs**, and **`app/favicon.ico`**
(assembled in-script, no extra dep). The stray non-square `public/{favicon,logo-icon}.png`
dups are deleted. Screenshots + photographic marketing graphics need a running app /
designer (specced below, not auto-generated). **PWA manifest is wired:** `app/manifest.ts`
now declares the square icons `purpose:"any"` + the maskable variants (`/brand/icons/
maskable-192/512.png`, `purpose:"maskable"`). **Remaining wiring (operator/native, not
generation):** the native iOS/Android app-icon + splash are still Capacitor defaults →
wire from these masters via `@capacitor/assets` + `cap sync` (native build). Source of
truth: `docs/brand/brand-guidelines.md` + `lib/brand/tokens.ts`.

Legend: ✅ exists · ⚙️ generated by the script · 📸 needs running app · 🎨 needs designer · ❌ missing.

## Phase 2 — App icon system
| Asset | Spec | Status |
|---|---|---|
| iOS **App Store icon** | 1024×1024 PNG, **opaque, NO alpha, NO pre-rounded corners** (iOS masks) | ⚙️ `public/brand/store/app-store-icon-1024.png` |
| iOS **app icon** (in-app) | native `ios/.../AppIcon.appiconset` single 1024 universal (modern Xcode) — replace the Capacitor-generated one with the brand 1024 | ⚙️→native (drop 1024 in / `@capacitor/assets`) |
| Android **adaptive icon** | foreground 432 (mark in inner **66%** safe zone, transparent) + background 432 (solid sage `#4F6B5B`); 108dp `mipmap-anydpi-v26` already wired | ⚙️ `play-adaptive-foreground-432.png` + `-background-432.png` |
| Android **Play icon** | 512×512 PNG (32-bit, ≤1 MB) | ⚙️ `public/brand/store/play-icon-512.png` |
| **PWA icons** | 192 + 512 PNG **true square** (replaces the broken 1536×1024 dups) + maskable 192/512 (safe-zone) | ⚙️ `public/icon-192/512.png` + `public/brand/icons/maskable-*` |
| **Favicon** | `app/favicon.ico` ✅ (16/32/48) + `app/icon.svg` ✅; optional PNG 16/32/48 | ✅ + ⚙️ (regen .ico from the new mark via png-to-ico) |
| apple-touch / OG | `app/apple-icon.tsx` (180 opaque) + `app/opengraph-image.tsx` (1200×630) via `next/og` | ✅ |

**Audit of existing:** ❌ `public/{icon-192,icon-512,favicon,logo-icon}.png` are all **1536×1024 non-square** (broken — regenerate true-square + delete the `favicon.png`/`logo-icon.png` dups). Native iOS AppIcon (`AppIcon-512@2x.png`) + Android mipmaps + splash exist but are **Capacitor-generated, predate the nest brand** → replace from the 1024 master.

## Phase 3 — Store screenshots (📸 — capture from a running build; specs only)
**Apple App Store** (per device family; PNG/JPEG, sRGB, no alpha, no rounded corners/status-bar mockups beyond Apple's frames):
- **6.7″** (iPhone 15/16 Pro Max): **1290×2796** (portrait). 3–10 shots. *Required.*
- **6.5″** (iPhone 11 Pro Max/XS Max): **1242×2688** or 1284×2778. *Required if 6.7″ not auto-scaled.*
- **iPad 12.9″**: **2048×2732** (portrait). *Required only if the app is offered on iPad.*

**Google Play:**
- **Phone screenshots**: 2–8, 16:9 or 9:16, min 1080px on the short side (e.g. **1080×1920**).
- **Tablet (7″ & 10″)**: same ratios, larger (e.g. **1600×2560**). *Required for tablet listing.*
- **Feature graphic**: **1024×500** PNG/JPG, no alpha (⚙️ a branded version can be script-generated from the logo; or 🎨).

**Recommended shot list (both stores):** (1) Dashboard / Remy greeting, (2) a multi-photo memory (the new grid), (3) the detail swipe gallery, (4) Reminders, (5) Storage/upgrade. Use the **sage/sand** chrome; one "hero" shot with the wordmark.

## Phase 4 — Marketing assets (🎨 / ⚙️ — requirements)
- **Social banners** (⚙️ branded type-cards from the logo, or 🎨): X/Twitter **1500×500**, Facebook cover **820×312** (safe 640×312), LinkedIn **1128×191** (no tagline), Instagram avatar = the 1024 icon. Square social post **1080×1080**.
- **Website hero graphics** (🎨): the landing hero (sage→sand gradient, the stacked logo + Remy companion + a memory-gallery device mock). Spec: ≥1600px wide, WebP, dark + light.
- **Product showcase graphics** (🎨): device mockups (phone in-hand) of the memory feed + gallery + Remy; 3-up feature strip.
- **Press kit** (📦): a `press-kit/` zip — logos (SVG + PNG, light/dark), the icon, the 6 Remy states, the color/type one-pager (from `brand-guidelines.md`), 2–3 product screenshots, a 100-word boilerplate (from `docs/compliance/00`).

## Generation
`scripts/generate-brand-assets.mjs` produces every ⚙️ raster above from the SVG
masters. The screenshots (📸) + photographic/illustrated marketing (🎨) are the only
items a coding agent cannot produce.
