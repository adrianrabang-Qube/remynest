# RemyNest Asset Production — icons, store, marketing

**STRATEGY-1 UNIFICATION (authoritative, 2026-07-18):** every identity raster derives
from the **canonical PURPLE fingerprint-heart-bird masters** —
`public/brand/store/app-store-icon-1024.png` (composed master; byte-copy of the
SHIPPED Build-19 iOS icon), `public/brand/remynest-mark.png` (transparent mark,
native 695×728), and `public/brand/remynest-lockup.png` (wordmark lockup). Run
`node scripts/generate-brand-assets.mjs` to regenerate ALL of: `app/icon.png`,
`app/apple-icon.png`, `app/favicon.ico` (+ favicon 16 [heart-crop]/32/48 PNGs),
PWA `public/icon-192/512.png` + maskable 192/512, Play 512 + adaptive fg/bg 432,
the Android launcher mipmaps (48–432), and `app/opengraph-image.png`. **The sage
generation path was REMOVED from the script** — the sage nest icon is retired as an
identity asset (operator corrections 2026-07-16 + 2026-07-18) and CANNOT be
regenerated from this pipeline; the sage SVGs under `public/brand/` are legacy
brand-kit references only. The iOS `AppIcon.appiconset` is the canonical ORIGIN
(installed 2026-07-16, shipped in Build 19) — never overwrite it from this script.
The Android adaptive background colour lives in
`android/app/src/main/res/values/ic_launcher_background.xml` (`#2E1B60`).
Screenshots + photographic marketing graphics still need a running app / designer
(specced below). Source of truth: `docs/brand/brand-guidelines.md` §2–§3.

Legend: ✅ exists · ⚙️ generated by the script · 📸 needs running app · 🎨 needs designer · ❌ missing.

## Phase 2 — App icon system (ALL DONE 2026-07-18, purple canonical)
| Asset | Spec | Status |
|---|---|---|
| iOS **app icon** (native) | `ios/.../AppIcon.appiconset` single 1024 universal — the **canonical composed master's origin** (approved warm-glow candidate C, shipped Build 19) | ✅ CANONICAL — do not overwrite |
| iOS **App Store icon** | 1024×1024 PNG, **opaque, NO alpha, NO pre-rounded corners** (iOS masks) | ✅ `public/brand/store/app-store-icon-1024.png` (byte-copy of the shipped icon) |
| Android **launcher** | `mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher{,_round}.png` 48–192 + `ic_launcher_foreground.png` 108–432 (mark @56% safe zone) + bg colour `#2E1B60` | ✅ pipeline (replaced the Capacitor template art) |
| Android **Play icon** | 512×512 PNG (32-bit, ≤1 MB) | ✅ `public/brand/store/play-icon-512.png` |
| Android **adaptive (Play)** | foreground 432 (mark in safe zone, transparent) + background 432 (the icon field) | ✅ `play-adaptive-{foreground,background}-432.png` |
| **PWA icons** | 192 + 512 true-square + maskable 192/512 (mark @60% on field) | ✅ `public/icon-192/512.png` + `public/brand/icons/maskable-*` |
| **Favicon** | `app/icon.png` (512 route) + `app/favicon.ico` (16 heart-crop / 32 / 48) | ✅ pipeline |
| apple-touch / OG | `app/apple-icon.png` (180 opaque) + `app/opengraph-image.png` (1200×630, canonical icon + lockup wordmark) + `.alt.txt` | ✅ static (the sage `next/og` .tsx routes were removed) |

## Phase 3 — Store screenshots (📸 — capture from a running build; specs only)
**Apple App Store** (per device family; PNG/JPEG, sRGB, no alpha, no rounded corners/status-bar mockups beyond Apple's frames):
- **6.7″** (iPhone 15/16 Pro Max): **1290×2796** (portrait). 3–10 shots. *Required.*
- **6.5″** (iPhone 11 Pro Max/XS Max): **1242×2688** or 1284×2778. *Required if 6.7″ not auto-scaled.*
- **iPad 12.9″**: **2048×2732** (portrait). *Required only if the app is offered on iPad.*

**Google Play:**
- **Phone screenshots**: 2–8, 16:9 or 9:16, min 1080px on the short side (e.g. **1080×1920**).
- **Tablet (7″ & 10″)**: same ratios, larger (e.g. **1600×2560**). *Required for tablet listing.*
- **Feature graphic**: **1024×500** PNG/JPG, no alpha (⚙️ a branded version can be script-generated from the logo; or 🎨).

**Recommended shot list (both stores):** (1) Dashboard / Remy greeting, (2) a multi-photo memory (the new grid), (3) the detail swipe gallery, (4) Reminders, (5) Storage/upgrade. Use the **sage/sand** chrome; one "hero" shot with the wordmark.

## Phase 4 — Marketing assets (🎨 / ⚙️ — requirements)
- **Social banners** (⚙️ branded type-cards from the logo, or 🎨): X/Twitter **1500×500**, Facebook cover **820×312** (safe 640×312), LinkedIn **1128×191** (no tagline), Instagram avatar = the 1024 icon. Square social post **1080×1080**.
- **Website hero graphics** (🎨): the landing hero (sage→sand gradient, the stacked logo + Remy companion + a memory-gallery device mock). Spec: ≥1600px wide, WebP, dark + light.
- **Product showcase graphics** (🎨): device mockups (phone in-hand) of the memory feed + gallery + Remy; 3-up feature strip.
- **Press kit** (📦): a `press-kit/` zip — logos (SVG + PNG, light/dark), the icon, the 6 Remy states, the color/type one-pager (from `brand-guidelines.md`), 2–3 product screenshots, a 100-word boilerplate (from `docs/compliance/00`).

## Generation
`scripts/generate-brand-assets.mjs` produces every ⚙️ raster above from the SVG
masters. The screenshots (📸) + photographic/illustrated marketing (🎨) are the only
items a coding agent cannot produce.
